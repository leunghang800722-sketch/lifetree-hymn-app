# backend/.env.example — DEEP-AUDIT-W2-EXEC-20260906 D-10 D2
#
# 複製呢個檔做 backend/.env,填入真值,`chmod 600 backend/.env`。
# 值全部留空——真值唔准寫入任何會 commit 嘅檔(呢個 .example 檔本身會
# commit,`backend/.env` 已經俾 .gitignore 擋住)。
#
# 呢五個 key 現時仲喺 `~/Library/LaunchAgents/com.hymnapp.backend.plist`
# 嘅 EnvironmentVariables 度(plist 值優先——`backend/lib/dotenv.js` 已有
# process.env 嘅唔覆蓋)。過渡期兩邊並存唔會撞;plist 幾時刪返嗰五個 key
# 係 Eric 嘅決定,見 DEEP-AUDIT-W2-REPORT-20260906.md §5 嘅指引。

JWT_SECRET=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_VERIFY_SERVICE_SID=
REGISTRATION_MODE=
