# 第五批 Code Review 修復規劃(BATCH5,2026-08-19,Fable 5)

> 背景:`FRONTEND-CODE-REVIEW-20260819.md` 係 master 報告;盤點 session(local_1f07a46e)已逐項 grep
> 確認本文件涵蓋嘅缺口全部未被任何 batch 處理過。Eric 已批開呢批。
> **本文件係規劃,未改任何一行 code。** 執行交 Sonnet5,照住做,去到 §7 停位就停。
> 規劃前已重讀晒全部涉及檔案嘅現行源碼(唔係淨係靠 review 報告嘅行號),下面行號以 2026-08-19
> 現行 working tree 為準。

---

## 0. TL;DR

- **前端 11 個 commit(F1–F11)+ 後端 4 個 commit(B1–B4)**,全部獨立 commit、獨立 revert。
- **S1 同 O9 一次過解**:FavoritesContext 用「ref 鏡像」pattern 重寫一輪(§2.1),functional-updater
  嘅 stale-closure 問題、value 冇 useMemo、replaceAllFavorites 令 AppState listener 重掛,三件事同一
  改動冚晒;PlaylistsContext(S5+O9)同款 pattern 照做一次(§2.2)。
- **§9-1(Android 拔耳機 vs D2)呢批唔改 code**——個「修法」(加 platform gate)係要先實測中招先啱做,
  盲改反而係賭 Android 冇呢隻病(§5.3 有詳細理由);歸入 consolidated testing,test-first。
- **§7 後端四項**:A/B/C/7.5 係 backend(B1–B4),**E 其實係純 client UI**(review §7.3-E 原文自己寫明
  「純 client UI,OTA 就推到」),所以 E 擺咗喺前端 F10——呢個係對 dispatch 指示嘅一個事實更正,見 §3.0。
- **#6(KAV 收斂)/#8(manifest 權限)/O6/O8 全套抽取/§7.3-D 押後**,理由逐個列咗喺 §5。
- 停位:全部 commit 完 + 驗收證據齊 → **停**。唔推 OTA、唔 restart backend、唔開 emulator、唔叫 Eric 驗機。
  裝置實測全部歸入 batch 完成後嘅 consolidated testing(跟 O1/O2 先例,O1-O2-REPLAN §6.1)。

---

## 1. 分批總覽

| Commit | 項目 | 檔案 | 風險 |
|---|---|---|---|
| F1 | S1+O9(favorites 半) | src/context/FavoritesContext.js | 低 |
| F2 | S5+O9(playlists 半) | src/context/PlaylistsContext.js | 低-中 |
| F3 | S2+S7 | src/screens/MineScreen.js, InviteFriendsSheet.js, AddFriendSheet.js | 低 |
| F4 | S3 | src/screens/AdminAddHymnScreen.js | 低 |
| F5 | S4+O5 | src/audioPrefetch.js | 低 |
| F6 | O3+O4 | src/screens/LibraryScreen.js | 中 |
| F7 | O7 | App.js, src/hooks/useCachedHymns.js | 低 |
| F8 | O11 | src/screens/PhoneLoginScreen.js | 低 |
| F9 | S6 | 5 個 Cover copy(LibraryScreen/MineScreen/HymnListScreen/PlaylistDetailSheet/SharedPlaylistSheet) | 極低 |
| F10 | §7.3-E(冷 start 分階段文案) | App.js(FullScreenPlayerOverlay 一帶) | 低 |
| F11 | §9-2 文檔化 | App.js 註解 + HANDOFF.md | 零(純註解/文檔) |
| B1 | §7.5 | backend/routes/stream.js | 低 |
| B2 | §7.3-B(/warm 讓路) | backend/routes/stream.js | 低 |
| B3 | §7.3-A(tee-into-buffer) | backend/routes/stream.js, backend/lib/resolveAudio.js | 中低 |
| B4 | §7.3-C(daily cron 預 resolve) | backend/server.js(+新 lib/warmLog.js) | 低 |

**點解咁拆:**

- F1/F2 同一套 pattern 但分兩個 commit——兩個 context 消費者唔同(favorites 係每行歌粒心;playlists
  係清單管理 UI),出事影響面唔同,分開先可以單獨 revert。
- F3/F4 都係「async response 落地前對返身份」呢族 guard,純加法、互相零依賴;F3 按檔案群組一個
  commit,F4(admin 專用畫面)另一個——admin flow 出事唔應該連累會員 flow 嘅 revert。
- F6(LibraryScreen 搜尋)係前端呢批唯一有行為改變嘅項(歌詞搜尋變 fallback 第二輪),自己一個
  commit,擺喺 F 批最後做。
- F1/F2 同 F6 **唔好夾埋一齊做**:F6 讀 hymns 陣列起 index,F1/F2 掂 context identity——雖然檔案唔重疊,
  但一齊改會令「粒心著唔著/搜尋出唔出」兩類 symptom 撈亂,分開 commit 方便斷症。
- B1→B2→B3→B4 由細到大:B1 幾行、B2 十行內、B3 係呢批最大嘅改動(所以排喺 B1/B2 之後,前面兩個
  commit 順手幫 B3 探熟晒 stream.js 現場)、B4 獨立於前三個。
- **B 批同 F 批完全唔重疊檔案**,可以任意先後;建議先做 F 批(多而細),後做 B 批(少而要專心)。

---

## 2. 前端逐項實施細節

### 2.1 F1 — FavoritesContext:S1 + O9(一次過,唔使兩次動同一份 code)

**現場**([FavoritesContext.js](frontend/hymn-app/src/context/FavoritesContext.js)):
`toggleFavorite`(27-50 行)直接讀 closure 嘅 `favorites`;`replaceAllFavorites`(58-70)dep array 有
`[favorites]` → 每次 favorites 變都係新 function → App.js 兩個 AppState effect 跟住拆掛重掛;provider
value(73 行)係 inline object,冇 useMemo。

**做法——「ref 鏡像」pattern**(唔用「側效搬入 functional updater」嗰條路:persist + outbox
`enqueue` 呢啲側效唔可以擺入 updater 入面俾 React 隨時重跑;ref 鏡像令讀寫係同步 read-your-writes,
double-tap 兩下 handler 順序讀 ref,第二下一定睇到第一下嘅結果,race 結構上消失):

1. 加 `const favoritesRef = useRef([]);`。**鐵律:每一個寫 state 嘅位都要同步寫埋 ref**,一共三個位:
   - mount load effect(22-24 行):`favoritesRef.current = parsed;` 然後先 `setFavorites(parsed)`
   - `toggleFavorite`:改成讀 `favoritesRef.current` 計 `newFavorites`/`added`,
     然後 `favoritesRef.current = newFavorites; setFavorites(newFavorites);`,persist 同 enqueue 邏輯逐行不變
   - `replaceAllFavorites`:`byLocalId` 改由 `favoritesRef.current` 起 Map,設 ref 再 setState,
     dep array 由 `[favorites]` 改成 `[]`(→ function 永久穩定 → App.js AppState effect 唔再重掛)
2. `toggleFavorite` 包 `useCallback(..., [])`;`isFavorite` 改成 `useCallback((id) =>
   favoritesRef.current.some(f => f.id === id), [])`(消費者 re-render 時機由 value memo 嘅
   `favorites` dep 揸住,唔靠 isFavorite identity)。
3. value 包 useMemo:
   `useMemo(() => ({ favorites, toggleFavorite, isFavorite, replaceAllFavorites }), [favorites, toggleFavorite, isFavorite, replaceAllFavorites])`
   (後三個已係穩定 identity,實際只有 favorites 會觸發)。

**驗收(execution session 自己做):**
- grep 證明 `FavoritesContext.js` 入面冇任何函數體直接讀 state 變量 `favorites`(除咗 value memo);
  三個寫位 ref/state 成對出現。
- 寫個 20 行 node harness(scratchpad,用 O1-O2 嗰套 react-dom+jsdom 方法,mock MMKV/userSync):
  同一個 tick 內連 call 兩次 `toggleFavorite(同一首歌)` → assert 最終 favorites 冇嗰首歌(toggle on
  再 toggle off),而唔係出兩條記錄。呢個 harness 唔使入 repo(同 O2 嗰個唔同,呢度冇訂閱時序陷阱,
  一次性驗證夠皮),輸出留低喺 commit message 或者回報入面。

### 2.2 F2 — PlaylistsContext:S5 + O9(同款 pattern)

**現場**([PlaylistsContext.js](frontend/hymn-app/src/context/PlaylistsContext.js)):
`createPlaylist`(64)/`importPlaylist`(98)用 `pl_${Date.now()}` 做 id;`addToPlaylist`/`removeFromPlaylist`/
`renamePlaylist`/`deletePlaylist` 全部 dep `[playlists]` 讀 closure;`applyServerPlaylist`(143-150)自己
已經用 functional setState(檔內註解寫明點解),係現有正確範本;value(157-161)inline object。

**做法:**

1. 加 `playlistsRef`,鐵律同 F1:寫 state 必寫 ref。寫位:mount load effect、`persist()`
   (44-47 行——喺 persist 入面加 `playlistsRef.current = next;` 一句就冚咗大部分 mutator)、
   `applyServerPlaylist`(改寫成:讀 `playlistsRef.current` 計 next → 行 `persist(next)`,唔再用
   functional updater——ref 已經保證新鮮,而且統一晒全檔一種寫法;MMKV 寫入照舊喺 persist 度)。
2. 全部 mutator(`createPlaylist`/`addToPlaylist`/`importPlaylist`/`renamePlaylist`/`removeFromPlaylist`/
   `deletePlaylist`)改讀 `playlistsRef.current`,dep array 收縮到 `[persist, syncUpsert]`(兩個本身
   `[]` dep,穩定)。
3. **S5 id 撞號**:`pl_${Date.now()}` 改成
   `` `pl_${Date.now()}_${Math.random().toString(36).slice(2, 8)}` ``(create 同 import 兩個位)。
   已核實 backend 唔會拒:users.db `playlists.id` 係 TEXT、PK 係 `(user_id, id)`、無格式驗證
   ([lib/userDb.js:48-60](backend/lib/userDb.js:48)),註解嗰句「client 生成嘅 TEXT」順手更新埋。
   舊格式 id 繼續有效(冇 migration,冇人重寫舊 id)。
4. value 包 useMemo(dep `[playlists]` + 穩定 function 們)。`MAX_PLAYLIST_SONGS` 係 module const,照擺。

**驗收:**
- grep 證明檔內冇 mutator 再讀 closure `playlists`;`pl_${Date.now()}` 零殘留。
- harness(同 F1 一齊嗰個 scratchpad 項目):同一 tick call 兩次 `createPlaylist` → 兩個 id 唔同;
  `createPlaylist` 完即刻(同一 tick)`addToPlaylist(新id, 歌)` → return `{ok:true}`(呢個係檔頭註解
  講嘅 firstSong 歷史問題,ref pattern 應該令佢直接好返——驗到就順手喺註解記低,驗唔到都唔阻收貨,
  firstSong 機制照保留唔剷)。

### 2.3 F3 — MineScreen + 好友 sheets:S2 + S7

**S2(私隱,MineScreen.js:83-93)**——`loadFriends` 冇取消/對驗,登出或切帳戶時 in-flight response
返嚟照寫上手帳戶好友名單。`loadFriends` 唔止 effect 用,accept/reject/unfriend 都 call,所以唔可以
淨係 effect-scoped cancelled flag,要用 **seq counter**:

```js
const friendsLoadSeq = useRef(0);
const loadFriends = React.useCallback(() => {
  const seq = ++friendsLoadSeq.current;
  if (!user) { setFriendsData({ friends: [], incoming: [], outgoing: [] }); return; }
  setFriendsLoading(true);
  friendsList(getToken ? getToken() : null)
    .then((r) => { if (friendsLoadSeq.current !== seq) return; setFriendsData({...}); })
    .catch(() => {})
    .finally(() => { if (friendsLoadSeq.current === seq) setFriendsLoading(false); });
}, [user, getToken]);
```

登出/切帳戶時 `user` 變 → 現有 `useEffect(() => { loadFriends(); }, [user])` 會即刻再 call 一次
→ seq 自動 +1 → 舊 response 作廢。唔使另外加 invalidate 位。

**S7a(MineScreen handleAccept/handleReject 冇 busy guard)**——加一個 `friendActionBusyRef`:
handler 開頭 `if (friendActionBusyRef.current) return; friendActionBusyRef.current = true;`,
`.finally(() => { friendActionBusyRef.current = false; })`。三個 handler(accept/reject/unfriend 個
confirm onPress)都罩。唔加 UI state(disable 樣式係 polish,唔屬呢批)。

**S7b(InviteFriendsSheet.js:27-35)**——`load()` 又係 effect 同 `handleGenerate` 兩邊 call,照抄
S2 嘅 seq counter pattern(唔用 FriendSharesSheet 嗰種 effect-scoped flag,因為呢度有 effect 以外
嘅 caller)。sheet close(`visible` 變 false)嗰陣 seq +1 作廢 in-flight(加個
`useEffect(() => { if (!visible) seqRef.current++; }, [visible])`)。

**S7c(AddFriendSheet)**——被外部 `setVisible(false)` 閂時唔 reset 舊 state。加
`useEffect(() => { if (!visible) resetAllLocalState(); }, [visible])`,reset 內容照抄佢自己個
`close()` handler 入面已有嘅清理句(執行時開檔對返,唔好自己發明新清理範圍)。

**唔做**:review S7 建議嗰個 `useAuthedFetch` 統一 hook——新抽象係 batch 尾聲先值得,呢批用
最小 targeted guard,每個位照抄檔內已有嘅正確範本(delisted effect / FriendSharesSheet),
維持「一眼睇得出同隔籬一致」。

**驗收**:grep 每個 async handler 有 seq/busy guard;肉眼 code review 對照 FriendSharesSheet 範本。

### 2.4 F4 — AdminAddHymnScreen:S3

**現場**(117-162 行):`runPreview` await 完直接 setState,冇 stale check;連貼兩條 URL 慢 A 蓋快 B。

**做法**:`previewSeqRef`,`runPreview` 開頭 `const seq = ++previewSeqRef.current;`,await 之後每個
setState(`setDup`/`setPreview`/`setError`/`setChecking(false)`)前 guard
`if (previewSeqRef.current !== seq) return;`。URL 變化 effect(166-183)入面 `reset()` 嗰條路同埋
每次 debounce fire 前都 `previewSeqRef.current++`(其實 runPreview 自己 ++ 已經冚咗後者,淨係
`!id → reset()` 嗰支要補一句)。`retry`(185-189)行 runPreview,自動受保護。

**驗收**:grep `previewSeqRef` 出現喺全部 await-後 setState 之前;肉眼追一次「貼 URL A → 未返
→ 貼 URL B」時序,confirm 入庫用嘅 `preview.youtube_id` 只可能係 B。

### 2.5 F5 — audioPrefetch:S4 + O5

**S4(downloadOne 冇 timeout,198-249 行)**——半死連線(socket 開住零 bytes)會令
`currentDownloadId` 永遠唔清,成個 session 預載全滅。現有 code 已經有 AbortController(208-209),
淨係加 timer:

```js
const DOWNLOAD_TIMEOUT_MS = 90 * 1000;
let timedOut = false;
const timeoutId = controller ? setTimeout(() => { timedOut = true; try { controller.abort(); } catch (_) {} }, DOWNLOAD_TIMEOUT_MS) : null;
```

- timer 要冚埋 `await response.arrayBuffer()`(半死連線正正係卡喺 body 度)——即係 fetch 之前開波,
  `finally` 先 `clearTimeout`。
- catch 入面而家「aborted 一律當 aborted-for-stream」要改:`timedOut` 先行判斷,diag 出
  `'timeout'`(行現成 diagFail 通道),同用戶主動 cancel 分開,唔好污染診斷。
- `controller === null` 嘅環境(AbortController 唔存在)就唔加 timeout,行為照舊——iOS Hermes
  實際有 AbortController(隔籬 cancelIfDownloading 一直用緊),呢個只係防禦分支。

**O5(touchedThisSession 冇上限,174 行)**——改 bounded LRU:

```js
const TOUCHED_MAX = 12; // 夠冚「播緊嗰首 + 隊列下 2 首 + 聽日 2 首」有突
function touch(id) {
  touchedThisSession.delete(id);
  touchedThisSession.add(id);
  while (touchedThisSession.size > TOUCHED_MAX) {
    touchedThisSession.delete(touchedThisSession.values().next().value);
  }
}
```

兩個 `touchedThisSession.add(...)` call site(getLocalUri 180、downloadOne 237)換成 `touch(...)`。
Set 保留插入順序,delete+add 就係 LRU touch。174 行嗰段註解入面「最壞情況係 cache 短暫超 cap」
嗰句順手改返準確(而家真係短暫,唔再單向增長)。**唔做** review 提嘅另一方案(App.js 傳入
「播緊+下2首」名單)——要改兩個檔案嘅 API,LRU 一個檔案內搞掂,效果一樣。

**驗收**:grep `TOUCHED_MAX`/`clearTimeout` 到位;node 快測 LRU helper 邏輯(純 Set 操作,抽出嚟
測都得,或者 inline 眼驗)。

### 2.6 F6 — LibraryScreen:O3 + O4

**O3(_searchBlob 夾全首歌詞 + 6232 個 clone)**——改動兩點:

1. **唔 clone**:剷 `augmented`(76-84 行),改起 side index:
   ```js
   const blobIndex = useMemo(() => {
     const m = new Map();
     for (const h of hymns) m.set(h.id, norm(h.title) + norm(h.display_title) + norm(h.title_en) + norm(h.artist) + norm(h.album) + norm(h.performer));
     return m;
   }, [hymns]);
   ```
   注意 blob **唔再包 `norm(h.lyrics)`**。原本用 `augmented` 嘅落游全部改用 `hymns`
   (`kidsBase`/`searched` 嘅 base),filter 改讀 `blobIndex.get(h.id)`。
2. **歌詞搜尋變第二輪 fallback**:主輪(title/artist/album/performer)結果係 0 先掃歌詞:
   ```js
   const lyricsIndexRef = useRef({ src: null, map: null });
   function getLyricsIndex(hymns) {   // lazy,一個 hymns reference 只起一次
     if (lyricsIndexRef.current.src !== hymns) {
       const m = new Map();
       for (const h of hymns) { if (h.lyrics) m.set(h.id, norm(h.lyrics)); }
       lyricsIndexRef.current = { src: hymns, map: m };
     }
     return lyricsIndexRef.current.map;
   }
   ```
   `searched` 入面:主輪 filter 完 `if (out.length === 0 && nq)` 先行歌詞輪(對 `base` 掃
   `getLyricsIndex(hymns).get(h.id)?.includes(nq)`)。
   - **行為改變(要喺 commit message 寫明)**:以前「主愛」會夾埋標題命中+歌詞命中一齊出;
     而家標題有命中就唔出歌詞命中。呢個係 review O3(a) 明文建議嘅取捨,搜尋欄 placeholder
     「搜尋歌名、歌手、歌詞、專輯」照留(歌詞 fallback 仍然搵到)。
   - 歌詞 index 得 733KB 總量,lazy 起一次之後每 keystroke 只係 `includes`,冇 regex。

**O4(debounce)**——`query` 拆做兩個 state:`queryInput`(TextInput 即時,clear 掣/`hasQuery` 用佢)
+ `query`(debounced,`searched`/`orgs`/`filterLabel` 用):

```js
useEffect(() => {
  const t = setTimeout(() => setQuery(queryInput), 200);
  return () => clearTimeout(t);
}, [queryInput]);
```

清除掣 `setQueryInput('')` 順手即時 `setQuery('')`(清空唔使等 200ms)。chip AND 語義、B11 空結果
提示邏輯逐行不變。

**驗收**:
- grep:`_searchBlob` 零殘留、`norm(h.lyrics)` 只出現喺 lazy index、`augmented` 剷咗。
- node 快測(純函數,唔使 React):抽 `norm` + filter 邏輯餵 20 首假歌,驗「標題命中行主輪」
  「標題 0 命中歌詞有 → fallback 輪出」「兒童 sub-lang AND 唔受影響」。
- **手動追一次 kids 鏈**:`kidsBase`/`kidsSubLangs`/`searched` 換 base 之後 dep array 要跟住改啱,
  呢度係 F6 最易手滑嘅位。

### 2.7 F7 — fetchWithTimeout ×2:O7

兩份一模一樣咁改([App.js:303-311](frontend/hymn-app/App.js:303) 同
[useCachedHymns.js:14-19](frontend/hymn-app/src/hooks/useCachedHymns.js:14)):

```js
async function fetchWithTimeout(url, ms = 8000) {
  const controller = new AbortController();
  const t = setTimeout(() => controller.abort(), ms);
  try {
    return await fetch(url, { signal: controller.signal });
  } finally {
    clearTimeout(t);
  }
}
```

- 逾時 throw 嘅係 AbortError(唔再係自製 Error)——**兩邊全部 caller 已核實只 catch 完回
  null/空(safeFetchHymnDetail、fetchAllHymns、fetchVersion),冇人讀 error message**,語義安全。
- App.js 304 行嗰句「avoids AbortController compatibility issues」註解係過時判斷(audioPrefetch
  喺 prod iOS 用緊 AbortController 冇事),剷咗佢,換一句講「timeout 而家會真 abort 底層 fetch,
  慢網唔會再同 retry 疊住拉兩份全量」。
- 收益:逾時後底層連線真係斷(唔再背景繼續拉幾 MB 全量)+ timer 唔漏。

**驗收**:grep `Promise.race` 喺兩檔零殘留;node 快測新 helper(mock fetch 永不 resolve →
8ms timeout reject AbortError;正常 resolve → timer 清走,`setTimeout` spy 對數)。

### 2.8 F8 — PhoneLoginScreen:O11

1. `startCooldown`(68-73):開新 interval 前補
   `if (timerRef.current) clearInterval(timerRef.current);`。
2. `GenderChips`(226-239)搬出 module scope:
   `function GenderChips({ gender, onSelect })`,兩個(定一個,執行時 grep 佢 render 咗幾多個位)
   使用位改傳 props `<GenderChips gender={gender} onSelect={setGender} />`。`styles` 係 module-level
   StyleSheet,搬出去照用到。唔好用「inline JSX 攤開」方案——搬 module scope 改動最細、對 diff 最友好。

**驗收**:grep `clearInterval` 喺 startCooldown 入面;`const GenderChips`/`function GenderChips`
唔再喺 component body 之內(縮進層級可證)。

### 2.9 F9 — 5 份 Cover copy 嘅 failed reset:S6

五個檔案(LibraryScreen / MineScreen / HymnListScreen / PlaylistDetailSheet / SharedPlaylistSheet)
嘅 `Cover` copy,各補一句(照抄 AdminAddHymnScreen 現成範本,prop 名執行時逐檔對返,多數係
`youtubeId`):

```js
useEffect(() => { setFailed(false); }, [youtubeId]);
```

**唔做** O8(抽公用 `<Cover>`)——review §5 原本判斷「共用 worktree 下 conflict 風險大過收益」
仍然成立(而家仲有歌詞三線 R1/R2/R3 喺呢個 worktree 度郁),O8 連同呢五句嘅收歸留返俾佢。
注意有啲檔案個 component 可能未 import `useEffect`,要補 import。

**驗收**:grep 五個檔案都有 `setFailed(false)` + dep `[youtubeId]`(或該檔實際 prop 名)。

### 2.10 F10 — 冷 start 分階段 loading 文案:§7.3-E

> ⚠️ **對 dispatch 指示嘅事實更正**:呢項唔係 backend 改動。review §7.3-E 原文:「純 client UI,
> OTA 就推到」。佢改嘅係 App.js 嘅播放器 loading 表現,所以歸前端批、獨立 commit;§7 真正嘅
> backend 改動係 A/B/C/7.5(B1–B4)。

**做法**(執行時先開 App.js 定位 FullScreenPlayerOverlay/MiniPlayer 嘅 buffering 顯示位,呢度
定設計唔定行號):

- 喺 overlay 內部(**唔准掂 provider 層**——O1 啱啱先拆走每秒 re-render,唔可以喺 PlayerCtx 度
  加每秒變嘅嘢)用 local state:`trackState` 入 Buffering/Loading 家族 → 記 `bufferStartRef` +
  開一個 8 秒 `setTimeout` → 到點 `setSlowHint(true)`;state 離開 buffering 或者轉歌(currentHymn
  變)→ clear timer + `setSlowHint(false)`。
- 文案:buffering 但未過 8 秒 → 照舊(現有 spinner/字樣唔郁,或者加「連接中…」如果而家乜都冇);
  過咗 8 秒 →「網絡較慢,仲努力緊…」。
- 純 UI:唔准掂 watchdog、retry、expectPlayingRef、任何 TrackPlayer call。
- MiniPlayer 唔使做(細位冇位擺文案)。

**驗收**:grep 證明改動只喺 overlay component 範圍 + 冇新增任何 `TrackPlayer.` call;timer 有
成對 clear(effect cleanup)。

### 2.11 F11 — §9-2 文檔化(純核實,零行為)

1dde53d 嘅 `autoHandleInterruptions: true`(App.js:632-642)喺 Android map 落 `handleAudioFocus=true`
(RNTP MusicService.kt:189),即係嗰次 `fix(ios)` OTA 起 Android ExoPlayer 自己 handle audio focus,
行為改變未聲明。呢批做兩樣:

1. App.js:632 嗰段註解補明:「⚠️ 呢個唔係 iOS-only option——Android 邊 map 做 handleAudioFocus=true,
   1dde53d 起已生效;audio focus 場景(電話/第二個 app 搶聲/拔耳機)同 D2 嘅互動見
   FRONTEND-CODE-REVIEW-20260819 §9,實測歸 consolidated testing」。
2. HANDOFF.md 加一條平台紀律(連埋 §9-3 嗰單 bare/CNG 唔對稱一句帶過:Android config 要手改
   manifest,iOS 改 app.json 就得——呢句 review §9-3 建議過立規,順手一齊寫低,零 code)。

---

## 3. 後端逐項實施細節(B1–B4)

### 3.0 總則

- 全部係 `backend/` 改動,**唔使新 build**(唔掂 native/app),但**全部要等 Eric 一次過批先
  restart 出街**——commit 完就擺喺度,唔准自行 restart(deploy gate 照行,而且要跟
  「唔喺 Eric 真機 QA 期間部署」紀律,揀 OCR 唔忙時段)。
- **唔准起第二個 backend instance 做測試**——server.js boot 會行 PRECACHE(打 YouTube),同
  prod 爭 IP/CPU。驗證限於 `node --check`、純函數 harness、code review;真 live 驗證項寫喺
  §6.3 留返俾 restart 嗰次做。
- 四個 commit 每個獨立 revert;B3 依賴 B2 唔存在(兩個都掂 /warm 附近但唔同段),任何一個
  單獨 revert 都唔會斷第二個。

### 3.1 B1 — §7.5:buffered fast-path 續播失敗要 `res.destroy()`

**現場**([stream.js:279-302](backend/routes/stream.js:279)):`wantsBeyondBuffer` 個 rest fetch
失敗(catch / 非 200/206)會跌落底 `finishLog(res.statusCode); return res.end();`——header 應承咗
(Content-Range/Content-Length)嘅長度大過實際 body,AVFoundation 當錯誤(-12935 同源),log 仲
報成功。

**做法**:將「續播失敗」路徑同「全部喺 buffer 內、正常收工」路徑分開:

```js
if (wantsBeyondBuffer) {
  try {
    ...現有 rest fetch + pipe,成功 return 照舊...
    try { await rest.body?.cancel?.(); } catch (_) {}
  } catch (_) {}
  // 行到呢度 = header 應承咗嘅 bytes 送唔齊 —— 硬斷條線,俾 client 見到係
  // 傳輸中斷(佢識自己 retry),唔好扮完整收工兼 log 成功。
  finishLog(res.statusCode, { aborted: true });
  return res.destroy();
}
finishLog(res.statusCode);
return res.end();
```

(`finishLog` 有 logged flag,destroy 觸發 'close' 唔會重覆 log。)

**驗收**:`node --check routes/stream.js`;肉眼追三條路:①全在 buffer 內 → end;②續播成功 →
pipe return;③續播失敗 → destroy。三條路各自 log 一次、唔重覆。

### 3.2 B2 — §7.3-B:/warm 讓路

**現場**([stream.js:83-89](backend/routes/stream.js:83)):/warm 個 sequential loop 唔理
`anyStreaming()`,用戶開始聽咗照逐首 12MB 扯,同正播嗰條 stream 爭 VPN 頻寬。

**做法**(`isStreaming`/`anyStreaming` resolveAudio.js 已 export,加入 import):

```js
for (const yt of ytIds) {
  try {
    const url = await resolveAudioUrl(yt);
    const verifiedUrl = await preVerifyUrl(yt, url);
    // 有人聽緊:resolve+preVerify(平)已經做完落 cache,貴嘅 warmBuffer 讓路。
    // 每個 iteration 即場 check,唔係入 loop 前 check 一次——中途開始播都讓。
    if (isStreaming(yt) || anyStreaming()) continue;
    await warmBuffer(yt, verifiedUrl);
  } catch (_) {}
}
```

**驗收**:`node --check`;肉眼確認 resolve/preVerify 仍然無條件行(呢個係設計:URL cache 照填,
下次播嗰陣至少 resolve 段係熱嘅)。

### 3.3 B3 — §7.3-A:冷路徑 tee-into-buffer(呢批最大改動)

**目標**:GET /api/stream 冷路徑 pipe 俾 client 嗰陣,順手將頭 N MB 留低入 bufferCache + 背景補
尾巴,令 AVFoundation 第 2/3/4 條 probe 好似 warm 歌咁秒答。

**設計——邏輯集中喺 resolveAudio.js,stream.js 只做收集**:

1. **resolveAudio.js 加一個 export**:

   ```js
   // 冷路徑 stream 順手收落嚟嘅頭截,採納入 bufferCache(§7.3-A tee)。
   // buf 必須由 byte 0 開始(caller 保證)。同 warmBuffer 共用 tail 補攞邏輯。
   export async function adoptStreamedHead(youtubeId, url, buf, totalLength, contentType)
   ```

   內容:①已有同 url 嘅未過期 entry 就 return(唔好蓋 warmBuffer 嘅完整品);②
   `zeroFragmentedMp4Durations(buf)`(照 warmBuffer 做法,存入前就地修);③如
   `totalLength && buf.length < totalLength` → 喺 `withWarmLock` 入面補攞尾巴(**將 warmBuffer
   而家 415-427 行嘅 tail fetch 抽做共用 helper `fetchTailBuf(url, totalLength, headLen)`**,
   兩邊共用,唔准抄第二份);④`touchBufferEntry` + `evictBufferCacheOverflow`,TTL 照用
   `BUFFER_TTL_MS`。⑤export 埋 `WARM_CAP_BYTES`(stream.js 收集上限要用)。

2. **stream.js 冷路徑收集**:只喺 `startsAtZero && !isHead && upstream body 存在` 先 tee
   (中段 range 對頭截 buffer 冇用)。喺 `Readable.fromWeb(upstream.body)` 之後、pipe 之前:

   ```js
   const teeChunks = [];
   let teeBytes = 0;
   let teeDone = false;
   const finishTee = () => {
     if (teeDone) return;
     teeDone = true;
     if (teeBytes < MIN_TEE_BYTES) return; // 太少(client 極早 abort),唔值得入 cache
     const headBuf = Buffer.concat(teeChunks);
     adoptStreamedHead(hymn.youtube_id, url, headBuf, teeTotalLength, teeContentType).catch(() => {});
   };
   body.on('data', (chunk) => {
     if (teeDone) return;
     teeChunks.push(chunk);
     teeBytes += chunk.length;
     if (teeBytes >= WARM_CAP_BYTES) finishTee(); // 夠 cap 就收皮,唔好無上限食記憶體
   });
   body.on('end', finishTee);
   body.on('error', finishTee);   // abort 前收埋幾 MB 一樣有用——下條 probe 就係要呢啲
   res.on('close', finishTee);
   ```

   - `MIN_TEE_BYTES = 256 * 1024`(細過呢個就算,連 moov probe 都唔夠幫)。
   - `teeTotalLength`/`teeContentType` 喺 header forward 段順手計(照 warmBuffer 嘅
     content-range → content-length fallback 公式);totalLength 攞唔到照 adopt
     (getBufferedChunk 嘅 `clientRange && !totalLength` 分支自然會行返冷路,無害)。
   - **關鍵安全性論證(commit message 要寫)**:額外 'data' listener 只旁聽,唔碰
     pause/resume/pipe backpressure;fMP4 startsAtZero 分支本身嗰個 'data' handler 照舊
     (tee 收 raw bytes,adopt 入面自己重跑 zeroFix,同 warmBuffer 語義一致);bufferCache
     嘅 url-match(getBufferedChunk 核對 `c.url !== url`)已經防咗 format 錯配。
   - 記憶體:峰值多咗 ≤12MB × 並發冷 stream 數;MAX_BUFFER_ENTRIES=8 嘅 LRU 照頂住 cache 本身。

3. **plain `body.pipe(res)` 嗰條路(非 startsAtZero)唔 tee**——中段 range 唔係由 0 開始,
   砌唔成頭截,直接唔郁佢。

**驗收**:
- `node --check` 兩個檔。
- 純函數 harness(scratchpad node script):直接 import resolveAudio.js 嘅 `adoptStreamedHead`
  唔容易(module 頂層有 side effects/依賴),所以驗收改為:①抽出嚟嘅 `fetchTailBuf` 用 stub
  fetch 測一次(200/206/非2xx/throw 四支);②`adoptStreamedHead` 嘅「已有 entry 唔蓋」「短過
  totalLength 先補尾」兩個判斷肉眼 review + 喺 §6.3 留 live 驗證步驟。
- grep:tail fetch 邏輯全 backend 得一份(`fetchTailBuf`)。

### 3.4 B4 — §7.3-C:daily cron 預 resolve「今日+聽日」精選

**設計**(記錄用 JSON,唔掂 hymns.db,冇 lock 問題):

1. **新檔 `backend/lib/warmLog.js`**:`recordWarmIds(ids)` 將 id 合併入
   `backend/data/warm-daily.json`(形狀 `{ "2026-08-19": [id,...] }`,per-day dedupe、cap 50、
   只保留最近 3 日,fs 寫入 best-effort try/catch,同步寫都得——每日十零個 id,量極細)。
2. **stream.js /warm route** 開頭(攞完合法 ids 之後)`recordWarmIds(ids)` fire-and-forget。
3. **server.js** 加每日 06:30(backend 部機本地時)排程:`setTimeout` 算到下一個 06:30 再自我
   續期(唔用 node-cron,唔加依賴)。job 內容:
   - 讀 warm-daily.json,攞「噚日 ∪ 今日」id(cap 40,噚日優先——嗰批就係「今日為你預備」
     嘅歷史命中);
   - 逐個(concurrency 1):`anyStreaming()` 係 true 就 `continue`(有人咁早聽緊,唔爭);
     由 id 查 youtube_id(讀 db 現成 getDb 路徑)→ `resolveAudioUrl` + `preVerifyUrl`
     **唔做 warmBuffer**(review C 原文係「預 resolve」——消滅 ③ 段 yt-dlp 冷 spawn 就夠,
     唔好朝早扯 bytes;bytes 級數嘅 warm 由 B3 tee 同用戶自己開 App 嘅 /warm 冚);
   - 每個之間 sleep 2s(溫柔對 shared home IP;resolveAudioUrl 現有 429 全局冷卻照罩);
   - 頭尾各 log 一行(幾多個 id、幾多成功)。
4. URL 壽命 ~4.5h:06:30 resolve,朝早黃金時段(07:00-11:00)內有效,啱設計目標
   (「當日第一個開 App 嘅用戶」)。

**驗收**:`node --check` 三個檔;warmLog 純函數(merge/dedupe/prune)用 node 快測;排程計時
函數(「而家→下一個 06:30 嘅 ms」)抽純函數測三個時點(今日未到/啱過/跨月)。

---

## 4. 各項點樣算「證明改咗」——驗收清單總表

| 項 | 證據(execution session 交貨要齊) |
|---|---|
| S1 | F1 grep + double-tap harness 輸出(toggle×2 → 冇重複記錄) |
| S2 | F3 seq guard grep + 肉眼時序追蹤記錄 |
| S3 | F4 seq guard grep |
| S4 | F5 `DOWNLOAD_TIMEOUT_MS`+`clearTimeout` grep,catch 分支 timeout/abort 分流 |
| S5 | F2 grep(`pl_${Date.now()}` 零殘留)+ 同 tick create×2 id 唔同 harness |
| S6 | F9 五檔 grep |
| S7 | F3 三個 sheet/handler guard grep |
| O3 | F6 grep(`_searchBlob` 零殘留)+ 純函數搜尋行為測試輸出 |
| O4 | F6 `queryInput`/debounce effect grep |
| O5 | F5 `TOUCHED_MAX` LRU grep |
| O7 | F7 兩檔 `Promise.race` 零殘留 + helper 快測輸出 |
| O9 | F1/F2 value useMemo + mutator useCallback + `replaceAllFavorites` dep `[]` grep |
| O10 | F-storage:`grep -rn "new MMKV()" src App.js` 淨返 `src/storage.js` 一處 |
| O11 | F8 grep(clearInterval / GenderChips module scope) |
| §9-2 | F11 diff(只有註解+HANDOFF) |
| §7.5 | B1 三路徑肉眼追蹤 + node --check |
| §7.3-B | B2 diff + node --check |
| §7.3-A | B3 fetchTailBuf 單元測 + tee 安全性論證寫入 commit message |
| §7.3-C | B4 warmLog/計時純函數測 + node --check |
| §7.3-E | F10 diff 證明只掂 overlay UI、零 TrackPlayer call |

> ⚠️ O10 喺 §1 個表漏咗行:**F-storage 係獨立 commit**(新 `src/storage.js` + 7 個檔換用:
> useCachedHymns / FavoritesContext / PlaylistsContext / userSync / playLog / autoplayPrefs /
> homePrefs)。全部本身就係 default MMKV instance,收歸零行為改變;7 個檔各自嗰段 try/catch
> fallback 收埋入 storage.js 一份。排喺 F1/F2 **之後**做(等 context 兩檔先安定),或者索性排
> F 批最尾。

**全批底線(冇得傾):**

```bash
cd frontend/hymn-app && npx expo export --platform android && npx expo export --platform ios
```

兩平台 production bundle 零 error 先准收工(呢個係之前 batch 嘅既定底線,docs/HISTORY.md 有先例)。
backend 三檔 `node --check` 全過。

---

## 5. 押後項(呢批唔做)+ 理由

### 5.1 #6 KeyboardAvoidingView 收斂(四套做法統一)
純建議、零 bug 實證,而且鍵盤行為係「靜態 review 睇唔出、要逐部機試」嘅典型——收斂完每個表單
都要裝置驗一次先安心。等 consolidated testing 做完、code 靜咗先開,做嗰陣用 AddToPlaylistSheet
嗰套(實測修正最多)做 baseline。

### 5.2 #8 Android manifest 三個用唔著權限
要出新 APK build 先生效,唔屬 OTA 範圍。歸入下一個 native build 批(同 D7 依賴清理一齊——
嗰批本身要 prebuild+重 build),出 build 前照 review 講法確認冇 native lib 依賴。

### 5.3 §9-1 Android 拔耳機/audio focus vs D2 —— **test-first,呢批唔改 code**
Review 建議嘅修法係「中招就幫 D2 個自動 play() 加 `Platform.OS === 'ios'` gate」——即係**修法
本身以實測中招為前提**。而家盲加 gate 等於喺 Android 度直接熄咗 D2 守衛,而 D2 當初要 cover
嘅「native 靜靜熄 playWhenReady」病歷喺 Android 側從來冇實測排除過;賭錯咗就係用一個「疑似」
bug 換一個「已知」保護。所以:code 唔郁,§6.3 嘅 consolidated testing 清單加兩條場景(拔耳機、
開第二個播放器搶 focus),中招先開一個一行 commit 加 gate(修法已經寫好喺呢度,執行零成本)。
H2/RemoteDuck 嗰半(markRemotePauseExpected)上批已做,唔喺呢批範圍。

### 5.4 O6(audioPrefetch streaming download)/ O8(抽公用 Cover/Heart)/ §7.3-D(resolver 換代)
維持 review §5「冇建議做」判斷:O6 要重做 abort 語義;O8 掂 8 個檔案,共用 worktree conflict
風險大過收益(S6 用五句 one-liner 頂住咗最痛嗰忽);§7.3-D 等 YouTube 反 bot 環境靜返先。

---

## 6. 執行紀律、停位、rollback

### 6.1 共用 worktree 紀律(照抄 review §6,一隻字都唔少)
- commit 一定逐個檔案列 pathspec:`git commit -m "..." -- frontend/hymn-app/src/context/FavoritesContext.js`
- 唔准 `git add .` / `git add -A` / 目錄 pathspec
- commit 前 `git status` 核對:working tree 有歌詞三線(R1/R2/R3)+OCR job 嘅檔案喺度郁緊,
  **只准 commit 自己呢批列明嘅檔案**
- 剷檔(呢批冇檔要剷)如有都要 `git rm <逐個檔案>`

### 6.2 停位(跟 O1/O2 先例,O1-O2-REPLAN §6.1)
- F1–F11 + F-storage + B1–B4 全部 commit 完、§4 驗收證據齊 → **停**。
- 唔推 OTA、唔 restart backend、唔開 emulator/simulator、唔叫 Eric 驗機。
- 任何一項 harness 轉唔到綠、grep 證明對唔上、或者發現現場同本規劃描述唔符(例如行號/結構
  已被其他 session 改咗)→ **嗰項停低回報,唔准自行改設計硬推**;其餘唔受影響嘅項照做。
- backend 批 commit 完之後,restart 排期由 Eric 拍板(一次過),restart 嗰次先做 §6.3 live 驗證。

### 6.3 Consolidated testing 追加清單(呢批產生嘅裝置/live 驗證項,唔喺執行輪做)
1. 【真機】狂撳粒心(double-tap)→ 最愛列表冇重複、outbox 冇孖單(S1)
2. 【真機】登入 A → 開好友 tab → 即登出/切 B → 冇殘留 A 嘅好友資料(S2)
3. 【emulator】admin 連貼兩條 URL → preview/入庫一定係後嗰條(S3)
4. 【真機 iOS】飛行模式一半嘅半死網下,預載卡 90 秒後自動放棄、隊列繼續(S4)
5. 【真機】搜尋打字流暢度 + 「標題冇命中先出歌詞命中」行為同 Eric 確認接受(O3/O4)
6. 【真機】重發驗證碼連撳 → cooldown 唔漏 interval(O11);性別 chips 揀完唔跳(GenderChips)
7. 【Android 真機】播歌→拔耳機/藍牙斷開;播歌→開第二個播放器搶 focus——兩個場景音樂唔應該
   自己響返;中招 → 開 §5.3 嗰個一行 gate commit(§9-1)
8. 【真機 iOS,backend restart 後】OTA 清 cache 冷開第一首:分階段文案有出(F10);第二次
   probe 起 log `mode=warm`(B3 tee 生效);開 App 即刻播歌,`/warm` log 見讓路(B2);
   朝早 06:30 後 backend log 見 cron resolve 一行(B4)
9. 【live log】隊 buffered fast-path 續播失敗個案:log 唔再報成功 206/200(B1)

### 6.4 Rollback 預案
- 每個 commit 自包含,單獨 `git revert` 即返上一狀態;F1/F2 revert 唔影響對方;B3 revert 後
  B2 照企(兩者唔同段);F-storage revert 會令 7 檔返返各自 `new MMKV()`,無資料影響
  (同一 default instance,key 冇變)。
- B 批未 restart 前 revert 零成本(prod 根本未行過新 code)。

---

## 7. 開放問題(唔阻執行,執行完回報時等 Eric 拍板)

1. **F6 搜尋行為改變**(歌詞命中變 fallback)——已按 review 建議落實,但最終體感要 Eric 真機
   撳過先算數(§6.3-5);如果 Eric 想要返「溝埋出」,備選係歌詞命中加喺主輪結果後面
   (append 而唔係 fallback),一個細 commit 改到。
2. **B4 cron 時間 06:30**——係按「URL 壽命 4.5h + 朝早黃金時段」推算,如果 Eric 知道實際
   高峰唔同(例如夜晚),改一個 const 就得。
3. backend restart 排期(B1–B4 + 之前未出街嘅 backend commit 一次過)。
