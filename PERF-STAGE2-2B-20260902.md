# PERF-STAGE2-2B-20260902 — frontend（iOS 模擬器）

執行者：Sonnet 5。範圍：`PERF-STAGE2-EXEC-20260902.md` §2B（D-1、F-1、F-2、F-3、F-4；F-5 唔做）。
中途收到 Opus 5 course-correction（F-1 兩段 timeout 設計、F-2 降級做診斷、量度改用 interleaved A/B + performance.now()），全部已按新指示執行，詳見各項小節。

裝置：iPhone 17 模擬器 `E0416618-B662-41D2-A253-5260FA0CF556`（iOS 26.5），全程只開呢一部。
**執行者唔判 PASS/FAIL/快/慢/冇問題 —— 呢欄留白俾 Opus 5。**

原始證據全部喺 `ops/perf/stage2-20260902/2b-*`。

---

## 0. Build provenance

| Build | commit | main.jsbundle | 用途 |
|---|---|---|---|
| D-1 | `fcfb62e` | 3,718,093 B | D-1 診斷 + 本報告全部「改前(BEFORE)」對比基準 |
| AFTER | `8a2e729` | 3,720,516 B（+2,423 B） | F-1+F-2(診斷)+F-3+F-4 全部落地後 |

兩個 build 都係 `xcodebuild ... -configuration Release -sdk iphonesimulator ... -derivedDataPath <scratchpad>/derivedData build`（重用 1B 嘅 derivedData，增量編譯），patched copy `EXUpdatesCheckOnLaunch=NEVER`，bundle id `com.hymnapp.praise`。

**deviceId 過濾聲明**：每次 `simctl uninstall` 之後 deviceId 會重新生成（存喺 app 沙盒入面），所以本報告全部量度都係用「host 記錄嘅 launch 時間戳 + backend log clientTs 時間窗」對應返個別 run，並用 perfHome 嘅 `chips=` 值有冇小數點（`now()`=performance.now() vs 舊 `Date.now()`）做二次確認邊個 run 屬邊個 build——單靠時間窗對應喺兩個 build 交替、每次都重裝嘅情況下試過對錯（見「限制」§L1），加咗呢個二次校驗先可信。

---

## 1. D-1 診斷 — hymns fetch 三段拆時 + 首頁 section 耗時 + hidden tab 成本

儀器：`useCachedHymns.js` 將 `r.json()` 拆做 `r.text()`+`JSON.parse`，中間插 mark；`HomeScreen.js` 四個 `useMemo`(chips/pages/todayPicks/recent) 入面用 `now()` 記實際耗時；`LibraryScreen.js`/`MineScreen.js` render function 頭尾記耗時。三個 run 嘅原始 beacon 喺 `ops/perf/stage2-20260902/2b-d1-beacons-s1.log`(S1)、`2b-d1-beacons-s2.log`(S2)。

### 1.1 S1 冷開(uninstall→install→launch,3 run)—— hymns fetch 三段

| run | hymnsMs | ttfb(headers) | body(r.text) | pars(JSON.parse) | bytes |
|---|---|---|---|---|---|
| 1 | 3,748 | 553 | 3,170 | 25 | 3,657,732 |
| 2 | 6,703 | 524 | 6,154 | 25 | 3,657,732 |
| 3 | 6,570 | 537 | 6,009 | 24 | 3,657,732 |

**發現**：JSON.parse 只佔 24-25ms(<1%),完全唔係瓶頸。ttfb 穩定喺 ~520-550ms。**body(網絡傳輸)佔成個 fetch 嘅 85-92%**,係唯一有意義嘅拉扯項——3.66MB 響應喺呢部機/呢條網絡要 3.2-6.2 秒先傳完,呢個係網絡層問題,前端拆 fetch 邏輯(F-1)幫唔到呢一段本身嘅速度,淨係幫「逾時保護」啱唔啱。

### 1.2 S2 熱開(terminate→launch,3 run)—— 首頁 section 耗時 + hidden tab 成本

| run | secChips | secPages | secToday | secRecent | 四項總計 | lib(Library render,有真資料) | mine(render,有真資料) | home mark(warm mount) |
|---|---|---|---|---|---|---|---|---|
| 1 | 3 | 3 | 15 | 6 | 27 | 181 | 0 | 870 |
| 2 | 3 | 3 | 15 | 5 | 26 | 174 | 1 | 846 |
| 3 | 3 | 3 | 15 | 5 | 26 | 178 | 0 | 869 |

**發現(呢個係決定 F-3/F-4 範圍嘅關鍵數字)**:
1. 四個 `useMemo` 加埋淨係 ~26-27ms,對比 `home` mark(warm mount,~846-870ms)嘅殘餘 ~840ms,**呢幾個 section 早已經係「純展示」問題,唔係 compute-bound**——即係話 F-3 原本設想嘅「對 compute-bound section 加 useMemo」呢條路冇嘢好做(佢哋本身已經有 useMemo,且已經好快)。
2. **`LibraryScreen` 首次帶真資料(6405 首)render function 本身要 174-186ms**,`MineScreen` 同一批量度 <1ms——兩者相差兩個數量級,直接決定 F-4 範圍(見 §4)。

---

## 2. F-1 — hymns fetch 拆兩段 timeout(course-corrected 設計)

**設計變更**(Opus 5 中途 course-correction,推翻執行單原文「8s→30s」):執行單原文會令真斷網要等 30+30=60s 先報錯。改法係同一條 request(同一個 `AbortController`)拆兩段獨立 timeout:headers 未到 8s 內照 abort(斷網偵測能力不變),headers 一到就換一個新嘅 30s 專登俾 body。

**中途發現(非預期,但有實證價值)**:覆查舊代碼(`fetchWithTimeout` 個 `finally { clearTimeout(t) }`)先發現,`fetch()` 呢個 promise 一係 headers 到就 resolve,`finally` 亦跟住即刻執行——**即係話舊碼嘅單一 8s timeout 由頭到尾都只不過保護緊 ttfb(headers 到嘅時間),從來冇保護過 body 下載**(body 冇任何逾時,理論上可以永久掛住)。呢個先係 F-1 修嘅嘢真身:唔係「延長一個原本存在嘅 8s cap」,而係「第一次幫 body 下載加返一個 30s 嘅逾時保護」。

### 2.1 Interleaved A/B(before=D-1 build,after=F-1 build,each ×5,uninstall→install→launch 交替)

原始 raw:`ops/perf/stage2-20260902/2b-f1-ab-{before,after}-run{1-5}.log` + `2b-f1-ab-beacons.log`。

| run# | BEFORE hymnsMs | BEFORE ttfb/body/pars | AFTER hymnsMs | AFTER att/ok1 | AFTER a1t/a1b/a1p |
|---|---|---|---|---|---|
| 1 | 10,028 | 543/9,461/24 | 6,560 | 1/1 | 511/6,026/23 |
| 2 | 5,734 | 541/5,169/24 | 4,389 | 1/1 | 516/3,851/22 |
| 3 | 5,959 | 513/5,422/24 | 4,562 | 1/1 | 518/4,019/25 |
| 4 | 3,808 | 542/3,241/25 | 4,314 | 1/1 | 526/3,764/24 |
| 5 | 4,776 | 525/4,227/24 | 5,434 | 1/1 | 532/4,879/23 |
| **median** | **5,734** | — | **4,562** | — | — |

**F-1 主要量度指標「第一次嘗試成功率」**:AFTER build 5/5 run `ok1=1`(第一次嘗試就攞到 6405 首,冇觸發 retry)。BEFORE build 冇呢個指標(D-1 build 未加呢個 note),但 5/5 run `n=6405` 全部成功,冇任何一次觸發重試——**呢次測試嘅網絡條件下,body 傳輸全部喺 30s 內完成,甚至喺舊碼冇保護嘅情況下都冇撞到「掛住唔停」嘅場景**,所以呢個 interleaved 測試冇能夠重現 Opus 5 提出嘅「timeout 誤殺」場景,兩邊 hymnsMs 嘅差異(median 5734→4562)較大機會係網絡本身波動(BEFORE run1 嘅 body=9,461ms 明顯係單一離群值,拖高成組 median)而唔係代碼改動本身嘅效果。

---

## 3. F-2 — 降級做診斷(course-corrected)

Opus 5 中途明文批准:喺 `PlayerProvider` function 第一行加 `useRenderCount('PlayerProvider')`(純計數器,`App.js` 入面呢一行係 F-2 對 PlayerProvider 做嘅**唯一**改動),用嚟同 `AppContent` 嘅 render 次數比較,決定 `PlayerCtx.Provider` value 要唔要 `useMemo`。

**S5(播「愛是不保留」,t=60s perfRenders)**:`PlayerProvider=9 AppContent=9`(完全相等)。raw:`ops/perf/stage2-20260902/2b-f2-s5-beacons.log`。

按 Opus 5 定義嘅判斷規則:「相同→可做 value useMemo(預期收益細,可跳過)」——本次量度顯示 PlayerProvider 冇被 AppContent 拖住多 render,**冇實質理由做呢個 useMemo,依決策規則保持跳過,`PlayerCtx.Provider` value 冇加 useMemo**。呢個係診斷結論,唔係「useMemo 冇用」嘅價值判斷——留返俾 Opus 5 覆核。

---

## 4. F-3(SongCard → React.memo)+ F-4(Library lazy-mount)— 改前/改後

兩項一齊落喺同一個 AFTER build,冇獨立 build 隔開 F-3/F-4 各自貢獻,量度到嘅係**兩者合計**嘅效果(見「限制」§L2)。

### 4.1 F-4 觸發門檻覆核

D-1 §1.2 量到 Library render(有真資料)174-186ms > 執行單「<50ms 就唔做」門檻 → 做;Mine 同一批 <1ms < 50ms 門檻 → 唔做,維持恆常 mount。呢個係決定 F-4 範圍嘅唯一準則,已喺 commit message 記錄。

### 4.2 Interleaved A/B(v2,修正版——見「限制」§L1)

方法:每個 cycle 做 uninstall→install→**verify(get_app_container 讀 main.jsbundle 大細核對)**→launch(seed,12s populates cache)→terminate→launch(measure,26s)→idb tap 「詩歌庫」(x=201,y=807pt)→2s。ababab 交替,each ×5。10/10 cycle 嘅 `installed_size==expected_size` 全部 `match=YES`(見 `ops/perf/stage2-20260902/2b-s2s3v2-*-run*.log`)。raw beacon:`2b-s2s3v2-beacons.log`。

| run# | BEFORE home mark(warm mount) | BEFORE Library tapToMount/tapToPaint | AFTER home mark | AFTER Library tapToMount/tapToPaint |
|---|---|---|---|---|
| 1 | 851 | 35/59 | 376 | 238/537 |
| 2 | 858 | 44/73 | 361 | 237/539 |
| 3 | 858 | 40/73 | 371 | 221/522 |
| 4 | 856 | 41/72 | 414 | 236/539 |
| 5 | 855 | 33/56 | 378 | 250/554 |
| **median** | **856** | **40/72** | **376** | **237/539** |

**兩項數字都清楚、一致(5/5 run 方向一致,冇一次例外)**:
- `home` mark(warm mount):median 856ms → 376ms,差 480ms。BEFORE 嘅 856ms 同 1B baseline(median 938ms,S2 5 run)量級一致,確認儀器冇變(比較有效)。
- Library tapToPaint:median 72ms → 539ms,**升咗 467ms**——呢個係 F-4 lazy-mount 嘅預期代價(第一次撳「詩歌庫」先真係 mount+layout,原本呢部分成本喺開機已經做完,而家搬咗去用戶真係撳嗰刻)。執行單要求「如實記升幅」,呢度如實記錄。

**歸因限制**:`home` mark 嘅 480ms 差,由於 F-3(SongCard memo)理論上唔應該影響首次 mount(React.memo 唔會令首次 render 快—— 佢淨係幫手跳過不相關嘅 re-render),而 F-4 直接將 Library 嘅 ~180ms render 工作由開機期(同 HomeScreen 爭緊同一條 JS thread)搬走,**480ms 嘅差較大機會主要係 F-4 帶嚟**,但冇獨立(淨 F-3 或淨 F-4)build 隔離驗證,呢個歸因係推論,唔係實測分拆。

**F-3 獨立效果**:冇對應嘅量度方法(perfRenders 計嘅係 `Home` 呢個 component 自己嘅 render 次數,唔計 descendant `SongCard` 嘅 re-render/reconciliation 節省)。呢一項嘅預期效益(減少切 chip/拖 pager 引致嘅不相關 re-render 使 18 張卡逐張重新起)喺現有儀器下量唔到,淨係靠代碼審查確認改動本身安全(props 拆 list/onSelect 語義同原本 onPress 一致,冇改抽歌邏輯)。

---

## 5. 正控(唔改用戶可見結果)

「今日為你預備」用 `dailyPickBalanced(pool, 'today', 6, LANGS)`,種子係 `'today'`(日期 seed,非隨機),`hasAlbum()` filter 邏輯喺 D-1/F-3 改動前後一個字冇動——D-1/F-1/F-2/F-3/F-4 全部改動都冇碰 `homeChips.js`/`dailyShuffle.js`。S2 warm run 3(AFTER build)`perfHome` beacon 顯示 `today=15.99ms`(耗時)但呢個 span 只計時間唔計內容;抽歌內容嘅正控嚴格嚟講需要同一日兩個 build 各截一張「今日為你預備」畫面比對——受限於連續執行嘅時間跨度(全程喺 2026-09-02 同一日內完成,`dailyPick` 用日期做 seed,理論上同日結果應該一致),但**本報告冇實際截圖比對兩個 build 嘅「今日為你預備」畫面**,呢個係執行漏做嘅一步(見限制 §L4),邏輯正控(冇改抽歌 function 本身)已確認,視覺正控未做。

---

## 限制

- **L1(方法論教訓,已修正)**:第一版 warm(S2/S3)interleaved 測試用「唔 uninstall,直接 install 另一個 build 覆蓋」嚟交替 before/after,10 個 run 入面有 9 個嘅 `perfHome` beacon 都顯示 AFTER build 嘅簽名(`now()` 小數點)——即係 `simctl install` 喺兩個 bundle id/version 相同嘅 build 之間**冇實際更換咗二進制檔**(可能係 CoreSimulator 嘅去重快取)。已改用**每個 cycle 都 uninstall + install + 用 `get_app_container` 核對 `main.jsbundle` byte 大細**嘅版本重做,10/10 cycle 全部 `match=YES` 先寫入本報告 §4.2。**呢個發現本身係本次任務嘅一個方法論教訓,記錄低俾之後嘅 A/B 測試參考**:凡係共用 bundle id 嘅 iOS 模擬器 A/B,一定要驗證安裝咗嘅二進制,唔可以淨係信 `simctl install` 嘅退出碼。
- **L2**:F-3/F-4 合併喺同一個 build 度量,冇獨立 build 拆開驗證各自貢獻(見 §4.2 歸因限制)。
- **L3**:F-1 嘅 interleaved 測試(§2.1)喺呢次測試嘅網絡條件下,兩個 build 全部 5/5 run 都第一次嘗試就成功,冇觸發過 retry 路徑,亦冇觀察到 body 下載超過 8s 嘅場景——**冇能夠實測「舊碼冇保護 body timeout」呢個發現喺真實斷網/慢網環境下嘅後果**,呢個係代碼審查層面嘅發現(§2 中途發現段),唔係喺呢次測試環境下嘅實測後果。
- **L4**:「今日為你預備」內容正控冇做視覺截圖比對(只確認咗抽歌邏輯代碼一個字冇改),見 §5。
- **L5**:D-1/F-1 嘅 section span(`chips`/`pages`/`today`/`recent`)喺 S1(冷開)嗰陣,如果 hymns 仲未到(`hymns.length===0`)就唔會 span(避免記低一個「零資料」嘅 0ms 假象),所以 §1.1 冷開表冇呢幾個 section 嘅數字——呢啲數字淨喺 S2(warm,hymns 一開始就有數據)先攞得到,已喺 §1.2 交代。
- **L6**:全部量度都喺同一部模擬器、同一條本機網絡(連緊 backend localhost:3001)做,冇模擬 Eric 真機嘅蜂窩網絡/tunnel 延遲,§1.1/§2.1 嘅絕對數字(ttfb ~520-550ms、body 3-9s)只反映呢個測試環境,唔可以直接當做真機數字。
- **L7**:`rss=na` 欄喺 perfMarks beacon 已經拎走(換位俾新欄位,見 commit `4321f46`),1B baseline 表入面嘅 RSS 數字全部嚟自獨立嘅 host-side `ps -o rss=` 量度(唔受呢個影響),但之後如果想再拎 perfMarks beacon 嘅 RSS 就要另外加返。

---

## 儀器/程式碼改動清單(commit)

| commit | 內容 |
|---|---|
| `d51c3bc` | D-1 —— hymns fetch 三段拆時 + 首頁 section span + hidden tab render cost(第一版) |
| `fcfb62e` | D-1 fix —— section span 淨計「真資料」嗰次(write-once 陷阱修正),**呢個 = 全報告 BEFORE build** |
| `b9e0f64` | F-3 —— SongCard 改 `React.memo`(props 拆 list/onSelect) |
| `d547279` | F-4 —— Library tab lazy-mount,Mine 維持恆常 mount |
| `4321f46` | F-1 course-correction —— hymns fetch 拆兩段 timeout(8s headers/30s body)+ 逐 attempt 記 marks |
| `297bf52` | F-2 降級做診斷 —— PlayerProvider 加 `useRenderCount` 純計數器 |
| `8a2e729` | section span 改用 `performance.now()`,**呢個 = 全報告 AFTER build** |

全部 commit 都係 pathspec-only(`git commit -m … -- <paths>`)。`git diff --stat -- App.js` 喺每次改動前都覆核過,冇夾帶其他 session 嘅未 commit 改動。冇掂 `PlayerProvider`(420-2958 行)入面任何起播/stall/nudge/watchdog/HLS/queue 邏輯——F-2 嘅一行 `useRenderCount` 係 Opus 5 明文批准嘅唯一例外。
