# 播放隊列三場景行為規格 — 規劃書 (QUEUE-BEHAVIOR-3-SCENARIOS-PLAN)

> 撰寫日期:2026-07-30
> 撰寫:Claude (Fable 5,規劃層)
> 狀態:**§2 已拍板——Eric 2026-07-30 確認推翻 BUG3(a)**(原話:「bug3(a)既做法
> 唔ok,一次過放4百幾首歌入去,單曲+30首類似就可以」),已授權直接交 Sonnet 5
> 執行 → Opus 5 驗收
> 來源:Eric 2026-07-30 三場景完整規格(經 Dispatch)
> 前作:`QUEUE-UX-4FIXES-PLAN.md`(四項已落地 + Opus 5 驗收完,5726bce)
> 性質:純規劃,呢個 session 冇改過任何 code。

---

## 目錄

0. Eric 三場景規格(原話 + 解讀)
1. 全部 UI 入口現況總表(逐個對返場景,唔靠估)
2. **核心衝突:場景 A 推翻 BUG3(a)** —— 我嘅判斷 + 俾 Eric 拍板
3. 改動方案(如果 Eric 照我建議行)
4. 唔使改嘅嘢(新規格同已落地嘅四項修復其實係一致)
5. 執行前置:working tree 有未 commit 嘅 App.js 改動
6. 風險同細節位
7. 驗收矩陣
8. 開放問題(唔阻執行,但要話返俾 Eric 知)

---

# 0. Eric 三場景規格(原話 + 解讀)

Eric 原話分三段:

> 「現在我解釋清單,當我重新開 app 清單唔會有上一次記錄,所以應該好似之前未有
> 會員系統之前咁,我隨便按一首歌去播放,之後下面會有 30 首類似歌曲放入播放頁清單」
>
> 「如果我自己建立左清單或按最後清單播放就只播我的清單內容,唔好加類似歌去後面,
> 播完就停」
>
> 「如果我播緊自己清單或最愛清單,我去首頁或詩歌庫按其中一首歌播放,就會立即播放
> 這首歌,播放頁清單在最上顯示呢首歌,播完繼續播返我之前播緊既自己清單」

解讀做三個場景:

- **場景 A(瀏覽揀歌)**:唔係播緊自己清單嗰陣,喺首頁/詩歌庫「隨便按一首歌」
  → 隊列 = [嗰首歌] + 30 首類似歌尾巴(「自動播放：」分隔線隔開)。
  呢個就係 code 入面 `playSingle()` 條「單曲 + 隨機接續」路。
- **場景 B(播自己嘅清單)**:自訂清單 /「最愛」→ 淨係播清單本身,唔加尾巴,
  播完就停。(「最後清單」三個字有歧義,見 §8-1。)
- **場景 C(播緊 B 時瀏覽插播)**:播緊自己清單/最愛,去首頁/詩歌庫撳一首歌
  → 即刻播嗰首、企喺隊列最頂、有「即將播放」分隔線,播完接返原本清單。
  呢個就係已落地嘅 browseTap 插播 + insertBoundary 分隔線。

開頭嗰句「重新開 app 清單唔會有上一次記錄」係前提,唔係新要求——4FIXES 第 4 項
(e087e7f:冷啟動清場 + swipe 走即停 + 剷「繼續收聽」)已經做咗,唔使再郁。

---

# 1. 全部 UI 入口現況總表(2026-07-30 working tree 實讀 code)

「idle」= 冇嘢播/播緊隨機接續隊列;「播緊 B」= 播緊自訂清單/最愛呢類明確清單。

| # | 入口 | 而家傳嘅 opts | idle 時行為 | 播緊 B 時行為 | 對應場景 | 符合新規格? |
|---|------|--------------|------------|--------------|---------|------------|
| 1 | 首頁・今日為你預備(`HomeScreen.js:338`) | 冇(`play(h, todayPicks)`,explicit 唔傳) | 單曲+30尾 | 插播+分隔線 | A / C | ✅ |
| 2 | 首頁・最近加入(`HomeScreen.js:350`) | 冇 | 單曲+30尾 | 插播+分隔線 | A / C | ✅ |
| 3 | **首頁・即刻揀歌 撳一行歌**(`HomeScreen.js:287`) | `explicit:true, playlist:activeChip.songs, browseTap:true` | **成個分類做隊列**(例:兒童 476 首),冇 30 尾 | 插播+分隔線(browseTap) | ?(BUG3(a)) | ❌ **同場景 A 衝突,見 §2** |
| 4 | 首頁・即刻揀歌「播晒 N 首」掣(`HomeScreen.js:318`) | `explicit:true`(冇 browseTap) | 成個分類做隊列 | 換走隊列 | 「按清單」型 | ✅(呢個掣本身就係「播成個分類」) |
| 5 | 首頁・隨心聽(`HomeScreen.js:208`) | `explicit:true`(洗牌全庫) | 全庫洗牌隊列 | 換走隊列 | 「按清單」型 | ✅ |
| 6 | 睇晒 N 首 分類詳情頁撳歌(`App.js:2326`,直接 `playQueue`) | 冇 opts,直入 playQueue | 成個分類做隊列 | **換走隊列(冇插播)** | 「按清單」型 | ✅(見 §8-3 一個小問號) |
| 7 | **詩歌庫/搜尋 撳一首歌**(`LibraryScreen.js:191`) | `explicit:true, playlist:shown, browseTap:true` | **成個 filter 後清單做隊列**,冇 30 尾 | 插播+分隔線(browseTap) | ?(BUG3(a)) | ❌ **同場景 A 衝突,見 §2** |
| 8 | 我的・最愛 撳一首/播全部(`MineScreen.js:122,133`) | `explicit:true, playlist:favorites` | 最愛隊列,冇尾,播完停 | 換走隊列(揀咗另一清單,合理) | B | ✅ |
| 9 | 自訂清單詳情頁 撳一首/播全部(`PlaylistDetailSheet.js:77`) | `explicit:true, playlist:songs`(e4162c8 剷咗 appendAutoplayTail) | 清單隊列,冇尾,播完停 | 換走隊列 | B | ✅ |

**結論:九個入口有七個已經完全符合新規格。唯二衝突就係 #3(即刻揀歌行)同
#7(詩歌庫/搜尋)嘅 idle 行為——即係 BUG3(a)。**場景 C(播緊 B 時插播)喺呢兩個
入口係靠 `browseTap:true` 做緊,行為正確;衝突淨係喺「idle 撳落去攞到成個分類/
成個 filter 清單」呢一 half。

---

# 2. 核心衝突:場景 A 推翻 BUG3(a) —— 判斷 + 俾 Eric 拍板

## 2.1 歷史脈絡(邊個幾時拍板咗啲咩)

1. **v231(舊行為,「未有會員系統之前」)**:詩歌庫/首頁撳一首歌 = 單曲 + 30 首
   隨機接續。呢個就係 Eric 而家話想返轉頭嗰個行為。
2. **2026-07-25 `796e3ea`(BUG3(a) P0,APP-AUDIT-OPUS5 體檢派落嚟)**:當時嘅
   投訴係「撳詩歌庫一首歌,睇落成個詩歌庫得 31 首」——於是詩歌庫改成
   `explicit:true + playlist:shown`(成個 filter 清單做隊列)。
3. **`53006b2` / `94acb23`**:分別俾詩歌庫同「即刻揀歌」行補 `browseTap:true`,
   令「播緊清單時撳呢度」變插播唔換隊列(即場景 C)。
4. **2026-07-30 Eric 三場景規格**:場景 A 明確要求「隨便按一首歌…之後下面會有
   30 首類似歌曲」——同 BUG3(a) 嗰下改動正面相反。

## 2.2 我嘅判斷:**照 Eric 最新規格行,推翻 BUG3(a)**(#3、#7 兩個入口 revert 返單曲+尾巴)

理由:

- Eric 開句係「現在我解釋清單」——呢個係佢畀嘅**完整行為規格**,唔係逐個 bug 報,
  應該當最高優先權嘅最新指示。
- 場景 A 嘅文字描述(「隨便按一首歌…30 首類似歌曲放入播放頁清單」)一對一咁對應
  緊 `playSingle()` 條路,冇歧義。
- 佢仲特登講明「好似之前未有會員系統之前咁」——即係佢記得舊行為、想要返嗰個。
- 分類/成個庫想「照次序播晒」嘅需求,入口 #4(播晒 N 首掣)、#5(隨心聽)、
  #6(睇晒詳情頁)全部仲喺度,冇少到——BUG3(a) 想解決嘅「睇唔到成個分類」
  其實由呢啲入口服務緊。

**但要 Eric 知道代價**(當日 BUG3(a) 就係為咗呢樣改):revert 之後,喺詩歌庫用
filter(例如淨係睇兒童歌)撳一首歌,隊列會係「嗰首 + 30 首類似」,**唔係**
「兒童分類跟住嗰啲歌」;30 首尾巴係按自動播放 flavor 由全庫加權抽,唔會限返喺
你當時 filter 緊嗰個分類。如果想聽成個分類,要用「播晒 N 首」掣或者「睇晒」頁。

## 2.3 如果 Eric 唔想全 revert(後備選項,唔建議)

保留 #3/#7 嘅「成個分類做隊列」,咁場景 A 就淨係適用於 #1/#2(今日為你預備/
最近加入)——但咁樣同佢原話「我去首頁或詩歌庫按其中一首歌」嘅描述唔對齊,
遲早又報一單。所以先至建議全 revert。**呢個係本文件唯一要 Eric 拍板嘅決定。**

## 2.4 拍板結果(2026-07-30)

Eric 確認照 §2.2 建議行:「bug3(a)既做法唔ok,一次過放4百幾首歌入去,單曲+30首
類似就可以」——**推翻 BUG3(a),§3 全套照做**,後備選項 §2.3 作廢。

---

# 3. 改動方案(照 §2.2 建議行嘅話)

改動細到得兩個 call site,**`App.js` 嘅 playSingle/playQueue/handlePlayHymn 一行
都唔使改**:

## 3.1 `src/screens/LibraryScreen.js:191`

```js
// 由:
onPress={() => onPlayHymn && onPlayHymn(item, { explicit: true, playlist: shown, browseTap: true })}
// 改做(唔傳 opts,行返 playSingle 條路):
onPress={() => onPlayHymn && onPlayHymn(item)}
```

## 3.2 `src/components/home/HomeScreen.js:287`(即刻揀歌 撳一行歌)

```js
// 由:
onPress={() => onPlayHymn && onPlayHymn(h, { explicit: true, playlist: activeChip.songs, browseTap: true })}
// 改做:
onPress={() => onPlayHymn && onPlayHymn(h)}
```

## 3.3 點解剷咗 `browseTap` 場景 C 冇事(關鍵理解,executor 必讀)

場景 C 嘅插播有**兩條**實現路:

- `playQueue()` 嘅 browseTap 分支(`App.js:943-966`)——服務「explicit 入口」;
- `playSingle()` 自己嘅插播分支(`App.js:828-848`)——服務「冇 opts 入口」:
  一入嚟見到而家播緊明確清單(`headLen > 1`)就砌 `[新歌, ...原清單餘下]`,
  一樣傳 `insertBoundary: 1`,一樣有「即將播放」分隔線。

#3/#7 改成冇 opts 之後,佢哋嘅插播由第一條路轉去第二條路,**行為結果一樣**
(即刻播、企最頂、分隔線、播完接返原清單)——`53006b2`/`94acb23` 想達到嘅目的
用另一條路保住咗,唔係俾人剷走。

## 3.4 註解同死碼處理

- `LibraryScreen.js:182-188` 同 `HomeScreen.js:193-198` 兩段 BUG3(a) 註解**必須**
  改寫:寫明「2026-07-30 Eric 三場景規格推翻 BUG3(a):瀏覽撳歌=單曲+30 尾;
  想聽成個分類用『播晒』/『睇晒』」。唔改註解,第時邊個睇到又會「修返」。
- 改完之後,成個 app **冇 caller 再傳 `browseTap`**。`playQueue()` 嘅 browseTap
  分支照 `appendAutoplayTail` 先例**刻意保留做死碼機關**(加返一句註解講明),
  唔好順手剷——第時有 explicit 入口要插播行為就用得返。
- `App.js:938-942` browseTap 段註解入面「詩歌庫/搜尋(`opts.browseTap`)」嘅
  描述要跟手更新(而家詩歌庫已經唔行呢條路)。

## 3.5 唔准掂嘅嘢

- `playSingle()` / `playQueue()` 嘅 headLen/insertBoundary/isDifferentExplicitQueue
  判斷邏輯——一行都唔改(4FIXES §3.4 鐵律繼續生效)。
- 入口 #1/#2/#4/#5/#6/#8/#9 全部唔郁。
- 4FIXES 落地嘅四項(e4162c8/8ec6759/08fc6f3/e087e7f + 5726bce)唔郁。

---

# 4. 唔使改嘅嘢(新規格 = 確認已落地行為)

逐句對返 Eric 規格,以下已經係現況,executor 唔使做任何嘢:

| Eric 講嘅 | 已落地喺 | 狀態 |
|-----------|---------|------|
| 「重新開 app 清單唔會有上一次記錄」 | e087e7f(冷啟動清場 + swipe 走即停 + 剷繼續收聽),Opus 5 驗過 | ✅ |
| 場景 B:自己清單/最愛唔加尾、播完停 | e4162c8(剷 appendAutoplayTail)+ 最愛本身一直冇尾 | ✅ |
| 場景 C:插播、隊列最上顯示、播完接返 | 53006b2/7d1ee17/94acb23 插播邏輯 + 08fc6f3「即將播放」分隔線 + 5726bce 鬼影修復 | ✅ |
| 場景 A 喺 今日為你預備/最近加入 | v231 原有 playSingle 行為 | ✅ |
| 場景 A 喺 詩歌庫/即刻揀歌行 | — | ❌ 本文件 §3 要做嘅嘢 |

---

# 5. 執行前置:working tree 有未 commit 嘅 App.js 改動

而家 `frontend/hymn-app/App.js` 有 **32 行未 commit 嘅改動**(嚟自 3dd0a28 驗收
session 揪出嘅兩個 bug:autoplay toggle 會剷走 explicit queue 剩低嗰截 +
rebuildTail 尾巴撞 id 出重複 key)。呢啲改動同本規劃**方向一致、冇衝突**
(佢哋令 toggle 喺 explicit queue 上面行為正確,正正係場景 B 嘅配套)。

Executor 開工前:

1. 先將呢 32 行改動**獨立 commit**(佢哋係另一單 fix,唔好同本規劃嘅改動夾埋);
2. 呢個 repo 多 session 共用 worktree——**唔准 `git add -A`**,只准 add 明確
   改過嘅檔,commit 前 `git status` 核對(工作紀律,之前試過夾 commit)。

---

# 6. 風險同細節位

| # | 風險/細節 | 講法 |
|---|----------|------|
| 1 | 場景 A 嘅尾巴受「自動播放」toggle 控制:關咗 toggle,瀏覽撳歌就淨係嗰首、冇 30 尾(AUTOPLAY-MIX-PLAN「關咗就係關咗」,舊拍板) | 維持——Eric 規格冇提 toggle,預設開嗰陣行為同佢描述一致。驗收要開/關各試一次 |
| 2 | 尾巴唔會跟詩歌庫當時嘅 filter(全庫按 flavor 加權抽) | 舊行為本來就係咁,§2.2 已寫明代價俾 Eric 知 |
| 3 | 播緊「睇晒/隨心聽/播晒」呢類非自己清單嘅 explicit 隊列時,去詩歌庫撳歌一樣會插播(playSingle 插播分支唔分「自己清單」定「分類清單」) | 係現有行為嘅自然延伸,Eric 規格冇反對;唔另做特別分支(分辨「邊個清單先算自己嘅」要傳新 flag,冇需要唔好加複雜度) |
| 4 | 播緊 B 播到**最後一首**時瀏覽撳歌:原清單冇「餘下」,插播分支唔會觸發,會直接換做「單曲+30 尾」 | 現有 edge case,兩條插播路一直都係咁(resumeRemainder 為空就正常換隊列),唔改;驗收唔好當 bug 報 |
| 5 | revert 後 `browseTap` 變零 caller 死碼 | 刻意保留(§3.4),照 appendAutoplayTail 先例加註解 |
| 6 | 又一次「推翻之前拍板」——BUG3(a) 係 7/25 P0 | 全靠註解防返修:§3.4 兩處註解要寫埋日期同呢份文件名 |

---

# 7. 驗收矩陣(Opus 5 用)

**場景 A(idle 瀏覽撳歌,自動播放 toggle 開):**
1. 詩歌庫撳一首 → 隊列 = 嗰首 + ~30 首,「自動播放：」分隔線喺 index 1 前。
2. 詩歌庫落 filter(揀語言/歌手/搜尋)再撳一首 → 一樣係單曲+尾巴(唔係成個 filter 清單)。
3. 即刻揀歌 chip 撳一行歌 → 單曲+尾巴(唔係成個分類 476 首)。
4. 今日為你預備/最近加入撳卡 → 單曲+尾巴(迴歸,本身就啱)。
5. toggle 關咗再做 1-4 → 淨係得嗰一首,冇尾巴。

**場景 B:**
6. 自訂清單「播全部」→ 得清單啲歌,播完停,⏭ 喺最尾冇反應(預期)。
7. 最愛「播全部」→ 同上。

**場景 C(播緊 B 時):**
8. 播緊自訂清單 → 詩歌庫撳一首 → 即刻播、隊列頂、下面「即將播放」分隔線、
   播完接返原清單下一首。
9. 播緊最愛 → 即刻揀歌撳一行 → 同上。
10. 插播播完行過分隔線 → 分隔線消失(5726bce 鬼影修復迴歸)。

**冇郁但要迴歸:**
11. 「播晒 N 首」掣/「睇晒」頁/隨心聽 → 照舊成個清單做隊列、冇尾、播完停。
12. swipe 走 app → 音樂停;重開 → clean state(e087e7f 迴歸)。
13. 播緊清單開/關自動播放 toggle → 清單剩低嗰截唔會俾人剷走(§5 未 commit 嗰單 fix 嘅迴歸)。

---

# 8. 開放問題(唔阻 §3 執行)

1. **「最後清單」係咩?**Eric 原話「自己建立左清單或按最後清單播放」——我當佢係
   指「最愛清單」(同場景 C 佢自己講「自己清單或最愛清單」對得上)。如果佢其實
   係想要「一撳就播返上次播嗰個清單」嘅捷徑,咁同「關 app 冇記憶」嘅拍板有張力,
   要另開需求傾,唔喺今次範圍。
2. **場景 A 嘅 toggle 前提**(§6-1):維持「自動播放關咗就冇尾巴」——如果 Eric
   想「瀏覽撳歌永遠有尾巴、唔理 toggle」,要話返嚟。
3. **「睇晒」頁喺播緊 B 時撳歌會換走成個隊列**(#6,冇插播)——我當佢係「揀咗
   成個分類清單」嘅明確動作,唔改。如果 Eric 覺得嗰度都應該插播,係一行改動
   (`App.js:2326` 加 `browseTap` 語義),隨時可以補。
