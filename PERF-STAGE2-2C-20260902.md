# PERF-STAGE2-2C-20260902 — backend 跟進(Sonnet 5,C-1..C-6)

執行者:Sonnet 5。範圍:跟進 `PERF-STAGE2-2A-OPUS-20260902.md` 對 2A(A-1..A-6)
嘅驗收意見四項——C-1(A-6 落地)、C-2(A-4 carve-out)、C-3(A-3 bytes/abort)、
C-4(dataVersion 自動追檔)——加後續追加嘅 C-5(三條 hymns route 預壓縮
cache),再跟進 `PERF-STAGE2-2C-OPUS-20260902.md`(Opus 5 對 C-1..C-5 嘅
獨立驗收:五項全 PASS,但 C-4/C-5 合共五個保留 + 兩處報告錯字)嘅
C-6(五個保留一次過修 + 兩處報告更正)。

**鐵律遵守聲明**:全程零 restart、零部署、冇掂 Cloudflare、冇寫過真
`backend/hymns.db`(C-4/C-6 harness 用 scratchpad copy,收工已刪、
`md5` 開工/收工一致 = `8dfa11315ec95563248c28a9d0f52c2f`)、冇起第二個真
`server.js`(唔跑 precache burst)、冇碰 frontend(C-5 落地之後見到
frontend session 已經自己 commit 咗 C-1 client 端消化,冇撞)。所有量度
用 slice committed 原文 + `new Function` 注入嘅方式(同 Opus 5 2A/2C 驗收
同一手法),harness 全放 scratchpad,收工前已刪 scratch DB copy/lock
檔/殺晒 harness process。commit:`78e7acb`(C-2)、`8d7a2d4`(C-1)、
`c55cfa9`(C-3)、`ab78c98`(C-4)、`0519814`(C-5)、`feb0060`(C-6)。

---

## C-1 — A-6 落地:`/api/hymns?lite=1` + `/api/hymns/lyrics`

### 改動

`backend/server.js` `/api/hymns` handler 加 `lite = req.query.lite === '1'`
分支:SELECT 欄位表淨係喺 `lite` 分支剷走 `lyrics`(其餘 16 欄/次序不變),
獨立 `hymnsLiteResponseCache` slot(dataVersion-keyed,同 full 個 slot 分開,
唔會互相覆蓋)。新增 `/api/hymns/lyrics`:`SELECT id, lyrics FROM hymns
WHERE lyrics IS NOT NULL AND lyrics != ''`,回 `{ data: {id: lyrics}, dataVersion }`,
獨立 cache slot。兩條都行現有 A-2 compression(路徑冇入 exclusion list,
唔使額外改動)。

### 正控:預設(冇 `?lite=1`)一個 byte 都冇變

方法:分別 slice `git show HEAD(78e7acb,即 C-1 之前)` 同改動後 `server.js`
嘅 compression(lines 48-55)+ `/api/hymns` route 原文,起兩個獨立 express
instance(真 `getDb`/`getDataVersion`,讀真 hymns.db),兩邊都用
`Accept-Encoding: ''` 打 `/api/hymns`:

```
BEFORE bytes= 5567648
AFTER  bytes= 5567648
diff before.json after.json → 零輸出(identical)
```

raw:`ops/perf/stage2-20260902/2c-c1-byteforbyte-guard.log`

### bytes / gzip / cache-hit ms(三個 endpoint)

harness slice `server.js` compression middleware(48-55)+ 全套 hymns
routes(292-406)原文,注入真 `getDb`/`getDataVersion`,起 express instance
(唔起真 server)。原始表:

| endpoint | raw bytes | gzip bytes(手動 `zlib.gzipSync` 核對) | raw miss ms | raw hit ms(x3 median) | gzip hit ms(x3,冇 gzip buffer cache) |
|---|---|---|---|---|---|
| `/api/hymns`(full) | 5,567,648 | 1,474,227 | 178 | ~17 | ~117 |
| `/api/hymns?lite=1` | 2,839,533(−49.0%) | 371,984(−74.8%) | 79 | ~8 | ~30 |
| `/api/hymns/lyrics` | 2,702,660 | 992,859 | 25 | ~7 | ~86 |

数字同 `PERF-STAGE2-2A-OPUS-20260902.md` §6 實測完全一致(唔係外推)。

⚠️ gzip hit ms 冇因為 A-1/C-1 cache 而縮短——因為 cache 存嘅係
**未壓縮** JSON string,compression middleware 冇 buffer cache,每個帶
`Accept-Encoding: gzip` 嘅 request 都要重新壓一次。呢個係 Opus 5 §2.3
已經指出嘅已知 trade-off(A-2 本身嘅性質),唔係 C-1 引入嘅新問題;
執行單冇要求呢班加埋 gzip buffer cache,冇做。

### 正控:field 內容/正確性

```
full rows=6405  lite rows=6405  lyrics map keys=5387
lite[0] has 'lyrics' key = false
lite keys count(17) = full keys count(18) − 1
lyrics map key count(5387) = full 非空 lyrics 數(5387)  ← 相符
dataVersion 三條 route 一致
```

### 正控:cache 跟 dataVersion(三條 route 獨立 slot)

```
r1(V1,冷): computeCount {full:1, lite:1, lyrics:1}
r2(V1,應該 hit): computeCount 不變
r3(dataVersion 變 V2): computeCount {full:2, lite:2, lyrics:2}(各 +1)
r4(V2,應該 hit): computeCount 不變
```

raw:`ops/perf/stage2-20260902/2c-c1-{bytes,positive-control}.log`

---

## C-2 — A-4 carve-out:還原 `/api/audio/cache/{stats,warm-stats}`

`backend/routes/audio.js` 逐字還原 `ebe29ba~1`(410 化之前)嘅
`/cache/stats`、`/cache/warm-stats` handler(`cache`/`failCache`/
`FAIL_TTL_MS`/`FAIL_TTL_PLAYBACK_MS` 由 `lib/resolveAudio.js`,
`getOpsMetrics` 由 `lib/opsMetrics.js`);`/:youtubeId` 維持 410(冇
consumer,A-4 原有分析不變)。`export { cache }` 冇還原(全 repo grep
確認冇 importer)。

harness(dynamic import `routes/audio.js`,唔起真 server):

```
/cache/stats       -> 200 {"cacheSize":365,"failCacheSize":0,...}
/cache/warm-stats  -> 200 {"since":"...","total":{...}}
/PG_J_0gsMXA       -> 410 {"error":"Gone",...}
```

raw:`ops/perf/stage2-20260902/2c-c2-carveout.log`

---

## C-3 — access log bytes/abort

### 改動

Opus 5 §3.3/§3.6 兩個保留:

1. **bytes 一律出 `-`**:因為 middleware 讀 `res.getHeader('content-length')`,
   compression 對壓縮過嘅 response 一律剷走呢個 header(chunked encoding
   冇 Content-Length 語意)。修法:middleware 自己喺 `res.write`/`res.end`
   層面累加實際寫出嘅 bytes,唔再讀 header。**呢個要求 middleware 一定要
   搬去 `app.use(compression(...))` 之前註冊**——compression 喺自己個
   middleware function 入面同步 monkey-patch `res.write`/`res.end`;如果
   access-log middleware 仲擺喺 compression 後面(A-3 原位),佢包嘅係
   compression 決定點壓縮之前嘅原始 chunk;擺喺前面,compression 會將
   access-log 嘅 wrapper 當「原始 res.write」嚟保存,自己壓完先轉頭
   call 返去,咁 access-log 先攞到最後寫落 socket 嗰啲(已壓縮)bytes。
   已刪走原本擺喺 compression 之後嘅舊版 middleware(避免兩份重複)。
2. **`ms` 淨聽 `finish`**:client 中途 destroy,server 都會 fire `finish`
   (代表「寫晒落 kernel socket buffer」,唔係「client 收晒」)。而家
   `finish`/`close` 兩個都聽,邊個先到用邊個,`close` 喺 `finish` 之前
   到就標 `aborted=1`(`done` 旗標防止兩個都 fire 就 log 兩次)。

### 正控/負控(synthetic harness,slice `server.js` lines 37-125 原文)

```
負控:/api/stream/999、/api/hls/999.m3u8、/api/client-log POST → 0 條 [access] 行
正控:/api/version → 20b(唔係 -)
/api/hymns(合成大 payload)identity → 5822155b(唔係 -,真 raw bytes)
/api/hymns(合成)gzip → 24927b(內容高度重複,壓縮率極高,純示範機制)
```

### 真 `/api/hymns` gzip bytes(A-6 route + access-log slice 一齊起)

```
GET /api/hymns 200 233ms 1474227b        ← 第一次(miss)
GET /api/hymns 200 117ms 1474227b        ← cache hit,bytes 一樣
```

同 Opus §2.3 實測嘅 gzip bytes 分毫不差,而家 access log 見到真數字。

### abort 模擬

用 raw TCP socket(`net.connect`)打一條逐格慢速輸出嘅測試 route,收夠
100000 bytes 就 `socket.destroy()`:

```
GET /api/slow 200 12ms 100000b aborted=1
```

`aborted=1` 正確標低,`bytes` 反映實際寫出(冇因為 client 冇收就報大)。

raw:`ops/perf/stage2-20260902/2c-c3-{synthetic,real-hymns-bytes}.log`

---

## C-4 — dataVersion 自動追 out-of-process 寫入

### 讀 `lib/hymnDb.js` 確認嘅前提(冇改呢個檔,淨係讀)

- `saveDb()`(:24-29)用 `fs.writeFileSync(tmp)` + `fs.renameSync(tmp, DB_PATH)`
  ——**atomic rename 寫**,`stat` 一見到新 mtime/size 就實一係完整新版本。
- 跨 process 寫手(夜晚 growLibrary/fetchLyrics job、admin script)由
  `acquireDbLock()`(:637)到成個 run 嘅所有 `saveDb()` 做完至
  `releaseDbLock()`,lock 檔(`hymns.db.lock`,:571)全程存在。
- ⇒ 兩個前提都成立,**可以實作**(執行單:如果唔成立就唔准做,寫分析
  代替)。

### 實作

`lib/serverDb.js` 加 `maybeReload()`:`statSync(DB_PATH)` 攞新
signature,同而家 `dataVersion` 一樣就乜都唔做;唔一樣先 check
`hymns.db.lock` 存唔存在,存在(writer 中途)就乜都唔做等落次
request,唔存在先 `reloadDb()`。`/api/version`、`/api/hymns`(含 lite)、
`/api/hymns/lyrics` 三個 handler 開頭 call。

### harness(copy hymns.db 去 scratchpad 做 DB_PATH,唔掂真檔)

方法:`cp backend/hymns.db` 去 scratchpad;對 `lib/serverDb.js` 原文做
**一個** text-substitution(淨係換 `DB_PATH` 嗰個 literal 指去 scratch
copy,其餘邏輯逐字唔改)寫成 scratch module import。

**`/api/version` 端到端**:
```
v1(baseline)                          = 1788338447512.3489-61054976
touch mtime(冇鎖) → v2                = 1788338452647-61054976   (changed=true)
再 touch(有鎖) → v3                    = 1788338452647-61054976   (仍等於 v2,guard 生效)
解鎖 → v4                              = 1788338457676-61054976   (changed=true)
```

**`/api/hymns` cache×dataVersion 交互**(slice 真 server.js hymns routes
292-406,注入 scratch getDb/getDataVersion/maybeReload):
```
r1(baseline,miss)          computeCount=1
r2(同檔,repeat)             computeCount=1(hit,不變)
r3(touch,冇鎖)               computeCount=2(+1,追到新版一次)
r4(repeat)                  computeCount=2(hit,不變)
r5(touch,有鎖)               dataVersion 企定唔變,computeCount=2(guard 擋住)
r6(解鎖,repeat)              computeCount=3(+1,追到延遲咗嘅新版)
```

**stat 成本**:10,000 次 `statSync` 平均 1.228µs/call(遠低於 <0.1ms 要求)。

raw:`ops/perf/stage2-20260902/2c-c4-{version-endpoint,
hymns-cache-interaction,statcost}.log`。收工已 `rm` scratch DB copy +
lock 檔;`md5 backend/hymns.db` 覆核真檔存在、全程冇被 harness 寫過
(harness DB_PATH 由頭到尾指嘅係 scratchpad copy,唔係真檔路徑)。

### 已知殘餘限制(冇喺呢次修)—— Opus 5 2C §4.5 已更正

⚠️ **呢段原文寫錯咗機制**(Opus 5 2C §4.5 指出,依家改正):原句話
「`maybeReload()` 淨係喺三條 hymns route call,其餘 route(例如
`/api/home/*`)仲係食緊 `getDb()` 嘅 lazy-load 快取,唔會因為
out-of-process 寫入而自動 refresh」——兩點都唔啱:

1. `routes/home.js` 喺 Stage 3(`b13088f`)已經**完全冇咗 `getDb`
   import**,而家零 DB 存取(`import express/fs/path/url` 淨係得呢幾
   個)。攞佢做「仲食緊 getDb 快取」嘅例子係攞咗個唔存在嘅例子。
2. 更重要:`reloadDb()` 清嘅係 `lib/serverDb.js` **單一個 singleton
   `dbPromise`**,而 `server.js` 傳俾 `hlsRoutes(getDb)` /
   `streamRoutes(getDb)`、`routes/me.js` 等全部食**同一個** singleton。
   即係只要三條 hymns route 任何一條觸發過 `maybeReload()` 令佢
   reload,**其餘所有靠呢個 singleton 嘅 route 下次 `getDb()` 都會攞到
   新鮮 DB**——真正嘅殘餘限制係「邊條 route 會**觸發**檢查」(而家淨係
   `/api/version`/`/api/hymns`/`/api/hymns/lyrics` 三條會主動
   `statSync` 去問「有冇變」),唔係「邊條 route 會**見到**新資料」。
   (`routes/search.js` 自己註明唔經呢個 singleton、每次自己開 DB,係
   另一件獨立嘅事,1A A2 已記,唔受呢個更正影響。)

原句嘅方向對用家有利(講大咗個限制),但機制講錯咗,第日會誤導人去
改 `routes/home.js`——而家已經冇 DB 存取,改極都冇用。

---

## C-5 — 三條 hymns route 加預壓縮 cache(gz buffer)

### 動機

C-1 個 cache slot 淨係存 raw JSON string。每個帶 `Accept-Encoding: gzip`
嘅 request(即係所有真 App 流量)都要 compression middleware 重新壓一次
——C-1/C-3 harness 已經量到:cache hit(identity)`/api/hymns` ~17ms、
`?lite=1` ~8ms、`/api/hymns/lyrics` ~7ms,但帶 gzip 就變返 ~117ms/~30ms/
~86ms。慳到嘅 cache 命中時間有七成幾俾 gzip CPU 攞返(Opus 5 §2.3 已經
指出呢個 trade-off)。

### 改動

`server.js` 三個 cache slot(`hymnsResponseCache`/`hymnsLiteResponseCache`/
`hymnsLyricsResponseCache`)由 `{ dataVersion, json }` 擴做
`{ dataVersion, json, gz }`——cache miss(重新 SELECT+stringify)嗰刻順手
`zlib.gzipSync(Buffer.from(body, 'utf8'))` 一次,連 gz buffer 一齊存低。

新增共用 helper `sendHymnsCache(req, res, cache)`,三條 route 嘅 hit/miss
分支全部改用佢送 response:

```js
function sendHymnsCache(req, res, cache) {
  res.set('Cache-Control', 'private, max-age=0, must-revalidate');
  res.set('ETag', `W/"${cache.dataVersion}"`);
  res.set('Vary', 'Accept-Encoding');
  res.set('Content-Type', 'application/json');
  if (req.acceptsEncodings('gzip') === 'gzip') {
    res.set('Content-Encoding', 'gzip');
    return res.send(cache.gz);
  }
  return res.send(cache.json);
}
```

`req.acceptsEncodings('gzip')` 喺完全冇 `Accept-Encoding` header 嗰陣答
`false`(同 compression 套件自己嘅 `enforceEncoding='identity'` default
一致),唔會send錯 encoding 俾一個冇話識 gzip 嘅 client。

**ETag 改用 dataVersion**(`W/"<dataVersion>"`),raw 同 gz 兩個
representation 共用同一個 tag,理由兩條(寫喺 code comment):
1. RFC 7232 §2.1——weak validator 本身就係為咗畀「語意相同、byte 可以
   唔同」嘅 representation(content-negotiation 正正係典型例子)共用一個
   validator 而設計,`W/` 前綴已經表明唔保證 byte-for-byte,淨係保證
   「同一個資源狀態」。
2. dataVersion 本身已經係一個同 hymns.db 內容一一對應嘅 freshness
   token(A-1 cache key 用緊嗰個),攞嚟做 ETag 唔使喺 5.5MB/1.4MB buffer
   上額外行一次 hash。

### 雙重壓縮 guard(點解唔會 double-gzip)

`compression` 套件原碼(`node_modules/compression/index.js` `onHeaders`
callback)入面:
```js
var encoding = res.getHeader('Content-Encoding') || 'identity'
if (encoding !== 'identity') { nocompress('already encoded'); return }
```
我哋喺 `res.send()` 之前已經 `res.set('Content-Encoding', 'gzip')`,
compression 喺 `onHeaders`(即 headers 準備 flush 嗰一刻,同步 hook)見到
呢個 header 就即刻 `nocompress`,唔會再壓一次,亦唔會剷 Content-Length
(嗰個剷除動作淨係喺「真係要壓縮」嗰條分支先執行)。

### harness(slice server.js 原文注入真 getDb/getDataVersion/maybeReload/zlib)

**雙重壓縮 guard 實測**——三條 route 帶 `Accept-Encoding: gzip` 嘅 response
wire `Content-Length` header:

```
/api/hymns          content-length(header)= 1474227
/api/hymns?lite=1   content-length(header)=  371984
/api/hymns/lyrics   content-length(header)=  992859
```

三個數同手動 `zlib.gzipSync` 算出嚟嘅 gz buffer 大細一致——如果俾
compression 再壓一次,呢個 header 唔會存在(重新壓縮嗰條分支會
`removeHeader('Content-Length')`,response 會變 chunked)亦唔會係呢個
乾淨數字。

**gzip hit ms(x3,改前→改後)**:

| endpoint | 改前(C-1/C-3 harness) | 改後 x3 | 改後 server 側 `[access]` ms |
|---|---|---|---|
| `/api/hymns` | ~117ms | 18 / 18 / 21 | 0-1ms |
| `/api/hymns?lite=1` | ~30ms | 10 / 7 / 6 | 0-1ms |
| `/api/hymns/lyrics` | ~86ms | 9 / 10 / 9 | 0-1ms |

`[access]` server 側 ms(C-3 落地嘅真實寫出時間)由 ~106-119ms 跌到
0-1ms——gzip hit 而家淨係送一個已經喺 memory 嘅 buffer,幾乎零 CPU。

**正控:raw vs gzip 解壓後 byte-for-byte 相同**:
```
/api/hymns          raw.len=5567648 gzip-decoded.len=5567648 identical=true
/api/hymns?lite=1   raw.len=2839533 gzip-decoded.len=2839533 identical=true
/api/hymns/lyrics   raw.len=2702660 gzip-decoded.len=2702660 identical=true
```

**miss 路徑 gzipSync 一次嘅 ms**(log 行 `🗜️ ... gzip precompute`):
```
full:    147ms → 1,474,227 bytes
lite:     30ms →   371,984 bytes
lyrics:   86ms →   992,859 bytes
```
呢個成本淨係喺 dataVersion 變咗(admin 寫入/夜晚 job 之後)先觸發一次,
唔係每個 request 都計。

**條件 GET(If-None-Match)驗證陷阱記錄**——第一次用 `fetch()` 手動 set
`If-None-Match` 測,得到 200 唔係 304;查落去發現係 **`fetch()` API 自己
嘅行為**:一旦 caller 手動 set 咗條件 headers,undici 會自動幫個 request
加多 `Cache-Control: no-cache`(避免瀏覽器自己個 HTTP cache 短路真正嘅
conditional round-trip),而 `fresh` 套件見到 client 送 `no-cache` 會
**刻意**判 stale(RFC 2616 §14.9.4:client 話明要 no-cache 就一定要俾
佢)——呢個唔關 server 事,係測試工具嘅偽陽性,改用 `http.get`(冇呢個
自動注入)先攞到真結果:

```
etag= W/"1788313502009.5908-61054976" conditional status= 304 content-length= undefined
```

304 正確,ETag 機制(A-5 遺留、C-5 改用 dataVersion)喺新 code 底下依然
生效。

raw:`ops/perf/stage2-20260902/2c-c5-precompressed-cache.log`

---

## C-6 — Opus 5 2C 五個保留一次過修

跟進對象:`PERF-STAGE2-2C-OPUS-20260902.md`(獨立驗收,五項 C-1..C-5 全
PASS,但 §4.3/§4.4 兩個 C-4 保留 + §5.3/§5.4/§5.5 三個 C-5 保留)。commit
`feb0060`。

### (a) ETag route-scoped(§5.3 保留一)

**問題**:C-5 三條 hymns route 共用同一個 `W/"<dataVersion>"` ETag。改前
(C-5 之前)三條靠 express auto-hash content 計 ETag,必然唔同;C-5 之後
一模一樣——如果第日 CF 開 query-string-insensitive cache rule,或者前端
加 conditional GET,`?lite=1` 同 full 就會撞埋,而共用 ETag 會令呢個撞車
由「必然被 ETag 攔住」變成「靜靜哋出錯資料」。

**改動**:`sendHymnsCache(req, res, cache, tagSuffix)` 加多一個參數——
full 唔傳(維持 `W/"<dataVersion>"`,同 C-5 出街時嘅值一樣,唔算改動),
`?lite=1` 傳 `'lite'`(`W/"<dataVersion>-lite"`),`/api/hymns/lyrics`
傳 `'lyrics'`(`W/"<dataVersion>-lyrics"`)。raw/gz/br 三個
representation 依然共用同一個 route 嘅 tag——Opus 5 §5.3(a) 已經核實過
呢點喺 C-5 之前就存在、唔算問題(`Vary` 已經 set,合規 cache 唔會攞錯)。

**harness**(真 hymns routes,slice 委原文):
```
etag full   = W/"1788341216528.777-61054976"
etag lite   = W/"1788341216528.777-61054976-lite"
etag lyrics = W/"1788341216528.777-61054976-lyrics"
full===lite? false   full===lyrics? false   lite===lyrics? false

正控:用 full 嘅 tag 打 ?lite=1 → 200(唔係 304)✅
對照:用自己嘅 tag 打自己(full/lite/lyrics 三條)→ 304 ✅(冇壞現有行為)
```
raw:`ops/perf/stage2-20260902/2c-c6-etag-brotli-async.log`

### (b) brotli(§5.4 保留二)

**問題**:C-5 一見到 client 收 gzip 就即刻 `Content-Encoding: gzip`,
compression middleware 就 `nocompress('already encoded')`,連帶 br 都
冇機會行。Opus 5 實測同一份 body gzip=1,474,223B、brotli(compression
套件 default quality)=1,198,686B,br-capable client 改後平白多食 23%
wire。

**改動**:三個 cache slot 加 `br`(見底下 (c) 嘅
`scheduleHymnsCompression`,quality 5)。

**⚠️ 執行單原話用 `req.acceptsEncodings(['br','gzip'])` 揀 br 優先,
harness 測到呢個做法唔可靠,冇跟字面做**:讀咗 `node_modules/negotiator/
lib/encoding.js` 嘅 `compareSpecs`,tie-break 次序係
`q desc → specificity desc → 客戶端 header 入面個 encoding 出現嘅位置
asc → 我哋 provided array 嘅 index asc`——即係話客戶端自己喺 header
入面寫嘅次序係**第三個**tiebreak,贏我哋傳嘅 array 次序(最後先輪到)。
實測:

```
AE: gzip, deflate, br (冇 q value,client 淨係咁啱 gzip 寫喺前面)
  → req.acceptsEncodings(['br','gzip']) 揀 gzip,唔係 br(改前一版做法)
```

改為分開兩次獨立 acceptability check(`req.acceptsEncodings('br') ===
'br'`、`req.acceptsEncodings('gzip') === 'gzip'`),先自己喺 code 度
話事優先次序,唔理客戶端 header 寫法/次序——呢個先真正做到「br 優先」。

**harness 覆核(修正後)**:
```
AE: br 淨收               → CE=br   size=1,083,708   ✅
AE: gzip, deflate, br     → CE=br(修正後,改前會揀 gzip)✅
AE: br;q=1.0,gzip;q=0.9,deflate;q=0.8(Apple NSURLSession 標準 header)
                           → CE=br   size=1,083,708   ✅
AE: gzip 淨收             → CE=gzip size=1,474,227
正控:br 解壓後 vs raw byte-for-byte identical = true ✅
quality 5:1,083,708B,比 Opus 5 引用嘅 compression 套件 default(q4)
1,198,686B 再細,−26.5% vs gzip(Opus 5 quote:q4 −18.7% vs gzip)
```
raw:`ops/perf/stage2-20260902/2c-c6-etag-brotli-async.log`

### (c) miss 路徑唔准同步阻塞(§5.5 保留三)

**問題**:C-5 喺 miss 嗰刻用 `zlib.gzipSync`(阻 event loop)。Opus 5
實測一次 `/api/hymns` full miss:readFileSync 20.5ms + new SQL.Database
4.7ms + SELECT/stringify 99.5ms + **gzipSync 109.2ms** = 233.9ms
event loop 全程凍住;C-4 令 dataVersion 喺夜晚 job 每次 `saveDb()` 之後
都會變(以前淨係 restart 先變),即係夜晚 job 跑緊嗰陣,隨時有人打
`/api/hymns` 就凍一嚿。

**改動**:miss 分支唔再自己壓縮——即刻用 raw body 起 cache slot
(`{ dataVersion, json }`,冇 `gz`/`br`)送出去,呢一個 response 由
compression middleware 用返 C-5 之前嗰套 **async** 協商(`sendHymnsCache`
見到 `cache.gz`/`cache.br` 未有就自然 fallback 落 `res.send(cache.json)`,
冇自己 set `Content-Encoding`,compression 照舊行)。新增
`scheduleHymnsCompression(cacheEntry, getCurrentSlot, label)`:用
`zlib.gzip`/`zlib.brotliCompress`(**callback 版,行 libuv threadpool,
唔阻 event loop**,唔係 `*Sync`)背景起 `gz`/`br`,完成先寫返落 slot——
寫之前核對 `getCurrentSlot().dataVersion === dataVersion`(呢次 miss
開始嗰陣捕捉嘅值),避免起緊呢兩份 buffer 嗰段窗口 dataVersion 又變咗
(第二輪 miss 已經寫咗新 entry)導致將舊版 buffer 錯誤咁貼落新 entry。

**harness**(直接量 sync 鏈,唔經 HTTP 減少網絡雜訊):
```
=== 改前(C-5)同步鏈 ===
readFileSync(61MB)         6.7ms
new SQL.Database(buffer)   3.2ms
SELECT + stringify(full)  82.1ms
zlib.gzipSync(full)      104.7ms   ← C-5 新加,同步
────────────────────────────────
一次 miss 同步總時間(改前) 196.7ms(Opus 5 quote:233.9ms,同量級)

=== 改後(C-6)同步鏈 ===
readFileSync(61MB)         7.6ms
new SQL.Database(buffer)   1.8ms
SELECT + stringify(full)  63.6ms
────────────────────────────────
一次 miss 同步總時間(改後,gzip/br 已搬去 async) 73.0ms
慳咗 123.7ms 嘅同步阻塞

=== async 壓縮期間 event loop 有冇被阻(setImmediate 探針)===
gzip+brotli(async, Promise.all)實際完成時間 = 106ms
同一段時間 setImmediate probe 最大 lag = 1ms(遠細過 106ms)→ 冇阻
```
raw:`ops/perf/stage2-20260902/2c-c6-miss-sync-timing.log`(直接計時)、
`ops/perf/stage2-20260902/2c-c6-etag-brotli-async.log`(HTTP 端到端,
`max setImmediate lag` 喺呢個 log 度量到 ~112ms,包埋咗 readFileSync+
new Database+SELECT/stringify 三段唔可以再拆嘅既有同步成本,唔係 C-6
新增——同直接計時果份 73ms 級數對得上)。

### (d) `getDb()` in-flight dedupe(§4.3 保留一)

**問題**:`lib/serverDb.js` 個 `dbPromise` 變量存嘅其實**唔係 promise
係 Database object**,而且要兩個 `await` 之後先賦值(`await
initSqlJs()` 再 `fs.readFileSync`)。Opus 5 實測:同一個 event-loop
turn 落到嘅並發 request(boot 嗰刻、或者兩條 socket 嘅 data 啱啱好落
喺同一個 poll batch)會全部見到 `dbPromise` 仲係 `null`,各自
`readFileSync` 61MB + 起一份 `new SQL.Database`——C-4 之前呢個窗口幾乎
唔會撞到,C-4 之後夜晚 job 每次 `saveDb()` 落地之後嘅第一批 request
都會行呢條路,fire 嘅機會大咗。

**改動**:`dbPromise = (async () => {...})()` 一句同步賦值(IIFE call
完即刻攞到個 pending promise,唔使等內部 `await` 行完),`.catch` 失敗
就清返 `null` 俾落次 request 有得 retry(唔想因為呢次修令失敗永久卡住
一個 rejected promise)。

**harness**(spy `fs.readFileSync`,`Promise.all` 3 個並發 `getDb()`):
```
改前(HEAD,C-6 之前):readFileSync 被 call 3 次,三個唔同 object   ← 重現咗 Opus 5 個 bug
改後(C-6):          readFileSync 被 call 1 次,三個 resolve 同一個 object reference ✅
隔一個 macrotask 先再 call:readFileSync 累計仍然 1 次,拎返同一個 object ✅
```
raw:`ops/perf/stage2-20260902/2c-c6-getdb-dedupe.log`

### (e) `maybeReload()` 殘留 lock 告警(§4.4 保留二)

**問題**:`maybeReload()` 見到 lock 檔存在就即刻 `return`,**冇年齡
判斷、零 log**。`lockIsStealable()`(20 分鐘/2 粒鐘)嗰套邏輯只喺
`acquireDbLock()` 入面行,即係要「另一個 writer 開工」先會搶走殘鎖。
Writer crash/俾人 kill 之後留低個 lock 檔,而之後又冇 job 再開工的話,
`maybeReload()` 會由嗰刻起**永遠**唔會再 reload,悄悄退化返「淨係
restart 先追到新資料」嘅狀態,零告警。Opus 5 指出
`backend/hymns.db.lock.bak*`(2026-08-17)就係歷史上真係試過要人手搬走
殘鎖嘅證據。

**改動**:**唔改行為**(lock 存在照舊唔 reload,唔理鎖幾舊)——加一句
告警:lock 齡 > 30 分鐘先 `console.warn('[db] stale lock ...')`,每 10
分鐘最多印一次(唔想夜晚 job 跑緊嗰段合理存在嘅 lock 每個 request 都
嘈一次)。

**harness**(scratchpad DB copy,五步驟):
```
step1 touch DB(冇鎖)               → reload 成功,零 warning
step2 touch DB + 建 31 分鐘前嘅鎖   → dataVersion 企定唔變(冇 reload),
                                       warning 出咗 1 次(`[db] stale lock ...`)
step3 連續再 call maybeReload() x3  → warnings 仍然 1(10 分鐘 throttle 生效)
step4 對照:鎖存在但啱啱先建(年輕) → dataVersion 仍然企定唔變(行為冇變),
                                       warnings 仍然 1(唔誤警新鮮鎖)
step5 解鎖                          → dataVersion 立即追到新值
```
raw:`ops/perf/stage2-20260902/2c-c6-stale-lock-warn.log`

### 報告更正(Opus 5 指出嘅兩處,已喺上面 §C-1/§已知殘餘限制 段原地改)

1. **§C-1「俾前端睇」段**:「`/api/hymns`(冇 `?lite=1`)行為一個 byte
   都冇變」收窄成「**body** 一個 byte 都冇變」——C-5 之後 ETag/`Vary`/
   `Content-Encoding` 呢啲 header 唔再係「一個 byte 都冇變」,實際影響
   零(前端零 conditional GET 邏輯),但字面要準確。
2. **「已知殘餘限制」段**:改正咗兩點事實錯誤——`routes/home.js` Stage
   3 之後已經零 DB 存取(唔係「仲食緊 getDb 快取」),而且 `reloadDb()`
   清嘅係全部 route 共用嘅單一 singleton,真正嘅殘餘限制係「邊條 route
   會**觸發**檢查」唔係「邊條 route 會**見到**新資料」。

---

## 部署 gate dry-run(唔 approve)

```
$ ops/deploy/backend-restart.sh --dry-run
❌ abort:HEAD (ab78c988c1b1680f4796e3ef4ffde3007fb01776) 唔等於已批准嘅
   backend.sha (84f2e0315c949edf808d2ea6b89d36674fa195a3)。
   (列出 28 個未批准 commit,含本次 C-1..C-4 四個 + 2A 五個 + Stage 3 + 2B)
exit=1
```

Gate 行為符合預期(per-sha,HEAD 未批就擋);冇跑 `approve.sh`。

raw:`ops/perf/stage2-20260902/2c-backend-restart-dryrun.log`

---

## 前端(A-6 client)要點——俾負責 2B/frontend 嘅 agent 睇

- `GET /api/hymns?lite=1` —— envelope/欄位/kids→lang 墊/real_lang 同
  `/api/hymns` 一樣,**淨係冇 `lyrics` 呢個 key**(唔係空字串,係完全冇
  呢個 property,`hasOwnProperty` 會係 false)。首屏開機應該改用呢條,
  raw 5.57MB→2.84MB,gzip 1.47MB→372KB。
- `GET /api/hymns/lyrics` —— `{ data: { "<id>": "<lyrics text>" }, dataVersion }`,
  key 係 hymn id(字串形式,JS object key 一律字串),只包含非空 lyrics
  嘅 5,387 首;冇歌詞嘅 6,405−5,387=1,018 首**唔會**出現喺呢個 map,
  前端查唔到就當「冇歌詞」。⚠️ **更正**(Opus 5 2C §1.4 指出):`full`
  payload 個 `lyrics` 欄呢 1,018 首出嘅係 `lyrics: null`,DB 入面零首係
  `lyrics: ''`——即係「同而家 `lyrics===''` 語意等價」呢句唔準確,準確
  講法係「`full` 出 `null`,前端 `mergeLyrics()` 用 `lyricsMap[h.id] ??
  ''` 補完先變 `''`」。兩個都係 falsy,對前端邏輯零影響,但字面要改。
- 兩條都行同一個 dataVersion 機制,`dataVersion` 三條 route 應該一致
  (同一個 hymns.db 版本)——前端可以照舊用 `/api/version` 做 cache-bust
  判斷,唔使為呢兩條新 route 另開一套版本比對邏輯。
- `/api/hymns`(冇 `?lite=1`)**body 一個 byte 都冇變**,舊 client 完全
  唔受影響,前端可以慢慢遷移,唔使同一次 deploy 一齊做。⚠️ **更正**
  (Opus 5 2C §1.1 指出):原句寫「行為一個 byte 都冇變」冇收窄範圍——
  C-5 之後 **header 唔係**一個 byte都冇變:ETag 值由 express auto-hash
  改咗做 `W/"<dataVersion>"`、加咗 `Vary`、gzip 路徑多咗自己 set 嘅
  `Content-Encoding`。實際影響 = 零(`frontend/hymn-app/src` grep
  `If-None-Match`/`ETag` 零命中,冇 conditional GET 邏輯睇呢兩個
  header),但字面要收窄成「body byte-for-byte」。
- C-6 追加(見 §C-6):ETag 而家 route-scoped(full 用
  `W/"<dataVersion>"`、lite 用 `W/"<dataVersion>-lite"`、lyrics 用
  `W/"<dataVersion>-lyrics"`),三條同時加咗 brotli(client 送
  `Accept-Encoding: br` 會收 br,唔會固定收 gzip)——兩樣都對前端
  透明,標準 `fetch()`(或 RN 嘅 fetch polyfill)一路自動處理
  `Content-Encoding`/解壓,唔使改任何解析邏輯。如果前端有自己實作
  conditional GET(送 `If-None-Match`),用返正常 `fetch()`(唔好手動再
  加 `Cache-Control`)
  就會攞到 304。
