# PERF-STAGE2-2D-20260902 — 前端 A-6 client（開機 lite 先畫，歌詞背景補拉）

執行者：Sonnet 5。範圍：`PERF-STAGE2-EXEC-20260902.md` §0/§4 + `PERF-STAGE2-2A-OPUS-20260902.md` §6
（backend C-1 已落地，commit `8d7a2d4`，之後 backend 又追加咗 C-2/C-3/C-4，見 `1bc45d8` 之前嘅
log）。呢份報告只負責 **Stage 2D：消費 C-1 endpoint 嘅前端 client**。

裝置：iPhone 17 模擬器 `E0416618-B662-41D2-A253-5260FA0CF556`（iOS 26.5），全程只開呢一部。
**執行者唔判 PASS/FAIL/快/慢/冇問題 —— 呢欄留白俾 Opus 5。**

原始證據全部喺 `ops/perf/stage2-20260902/2d-*`。

---

## 0. Backend 合約（消費方，冇改 backend）

`GET /api/hymns?lite=1` → `{ data: [...冇 lyrics 欄嘅 hymn object...], dataVersion }`
`GET /api/hymns/lyrics` → `{ data: { [id]: lyrics }, dataVersion }`（只含 84.1%=5,387/6,405 非空 lyrics）
`GET /api/hymns`（冇 query）→ 行為完全不變（byte-for-byte，backend commit `8d7a2d4` 已驗）。

**live production backend（3001）到執行呢份報告嗰刻都未 restart**，所以 `?lite=1` 落 3001 會俾舊
`server.js` 忽略、照回 full payload（唔係 404）——見 §2「404 fallback」點處理呢個場景。

---

## 1. 改動

只改咗 `src/hooks/useCachedHymns.js`、`src/perfMarks.js`、`App.js`（僅 import 一行 +
`FullScreenPlayerOverlay` 入面兩處 `cur.lyrics` 讀法，喺 `PlayerProvider`(420-2958 行)範圍以外）。
`PlayerProvider` 本身一個字冇掂。

### 1.1 `useCachedHymns.js`

冷開無 cache / version 唔同嗰刻（`kickRefreshOnce` → `refresh()`，`!canSkip` 分支）：

1. `fetchPrimaryHymnsWithRetry()` 打 `${API_BASE}/api/hymns?lite=1`，沿用 F-1 嘅
   `fetchHymnsTwoStage`（8s headers timeout / 30s body timeout，同一個 `AbortController`）+
   失敗重試一次嘅邏輯（同舊 `fetchAllHymnsWithRetry` 一樣寫法，淨係 URL 唔同，舊 function 已刪走
   —— 冇第二個地方用緊佢）。
2. **即刻** `hymnsStore.setState({ hymns: primary.hymns })` 畫面（lite 版，或者見下面「404
   fallback」嘅 full 版）；`!hadCache` 就即刻收 `loading:false`——唔等歌詞。
3. **404 fallback**：`finalizePrimaryResult()` 檢查 `result.hymns[0]` 有冇 `lyrics` 呢個 key
   （`Object.prototype.hasOwnProperty.call`）。有 = 舊 backend 忽略咗 `?lite=1`、照回 full payload
   （呢份 response 已經齊晒 lyrics）→ `note('liteIsFull',1)`，跳過 `/api/hymns/lyrics`，直接當合併
   完成寫 MMKV。冇 = 真 lite → 行落一步。
4. 背景 `fetchLyricsMap()` 打 `/api/hymns/lyrics`（同一套兩段 timeout，但**唔重試**）：
   - 成功（`r.ok` 且 JSON 解析到）→ `map` 非 null（`{}` 都算成功,唔算 fail）。
   - 失敗（HTTP 錯誤 / abort / JSON 壞）→ `map:null`，行 fail 分支：`note('merged',0)` +
     `note('lyricsFail',1)`，MMKV 存返 lite 版 hymns，但**唔寫 `allHymnsVersion`**——落次開 App
     `cachedVersion` 讀唔到/對唔上，`canSkip` 判 false，會再嘗試一次成套 refresh。
5. 成功就 `mergeLyrics(primary.hymns, lyr.map)`：`hymns.map(h => ({ ...h, lyrics: lyr.map[h.id] ??
   '' }))`——**新 array + 新 object**，等 `LibraryScreen.js` 個 `lyricsIndexRef.current.src !== list`
   reference-identity 判斷同其他 `useMemo(deps=[hymns])` 正確重跑。`setState({hymns:merged})` 一次
   （`note('merged',1)`），MMKV 存合併後嘅 `allHymns` + `allHymnsVersion`。
6. 兩個 fetch 嘅 `dataVersion` 唔同（中途 DB 改咗）：`note('lyrVerMismatch',1)`，但**照用 lite
   嗰個 dataVersion 合併**，唔重新攞（執行單原文要求嘅行為）。
7. 新 export `getLyricsById(id)`：讀一個 module-level `lyricsMapStore`（喺 `fetchLyricsMap()`
   成功時 `Object.assign` 入去），俾 `App.js` 補「lite 已畫、用戶已撳播、lyrics 背景 fetch 仲未
   merge 入 `player.hymns`」嗰個窗口。

`notifyHymnsChanged`（admin 寫入後刷新用）**完全冇改**——照用舊 `fetchAllHymns()`（全量、含
lyrics），呢條路徑唔係執行單範圍。

### 1.2 `App.js`

```js
import { useCachedHymns, getLyricsById } from './src/hooks/useCachedHymns';
...
const lyricsText = formatLyrics(cur.lyrics || getLyricsById(cur.id));
...
const lyricsStanzas = formatLyricsStanzas(cur.lyrics || getLyricsById(cur.id));
```

兩處都喺 `FullScreenPlayerOverlay`（行 3178 起），喺 `PlayerProvider`(420-2958) 範圍以外。
`git diff --stat` 確認呢次改動只有呢 3 行 + import，`PlayerProvider` 零改動。

### 1.3 `perfMarks.js`

`schedulePerfMarksBeacon()` 個 detail 字串加 4 個欄，放喺 `byt=` 之後、`fetch=`（彈性最大、俾
300-char cap 切到都唔緊要）之前：

- `liteMs`：`durMark('hymnsStart','hymnsEnd')` 嘅別名（同 `hymnsMs` 同一個 span，分開個名淨係為咗
  報告清楚——full-fetch 路徑用 `hymnsMs` 講,lite 路徑用 `liteMs` 講，實際上係同一條 span）。
- `lyrMs`：新 `lyrStart`/`lyrEnd` mark 之間嘅耗時（背景 `/api/hymns/lyrics` fetch）。
- `lyrBytes`：`fetchLyricsMap()` 讀到嘅 `text.length`（⚠️JS 字元數,唔係 wire bytes,見「限制」§L1）。
- `merged`：0/1，合併完成先算 1。

---

## 2. A/B 方法（跟 2B §4.2 同一套教訓）

**BEFORE** = commit `1bc45d8`（2D 改動之前嘅 frontend 狀態，即 2B 嘅 AFTER）。
**AFTER** = commit `d375f9a`（本報告兩個 commit：`bda9f9e` 儀器 + `d375f9a` A-6 client）。

兩個 build 都係 `xcodebuild -workspace ios/Odely.xcworkspace -scheme Odely -configuration Release
-sdk iphonesimulator -derivedDataPath <scratchpad>/derivedData -destination 'generic/platform=iOS
Simulator' build`，**重用本 session 之前已經存在嘅 derivedData**（同 2B 一樣嘅增量編譯路徑，
BEFORE build 60s、AFTER build 17s）。`ios/` 全個目錄喺 `.gitignore`（`frontend/hymn-app/.gitignore:40`
`/ios`）,唔受 git 版本控制——所以呢次冇用 `git worktree`/checkout 去做隔離,而係喺**同一個共用
worktree**入面,對 3 個 JS 檔（`App.js`/`useCachedHymns.js`/`perfMarks.js`）短暫 `git checkout
1bc45d8 -- <paths>` → build → `git checkout HEAD -- <paths>` 還原,每次改動窗口都喺一次 build
之內（BEFORE 窗口 ~60s，AFTER 窗口 ~17s），build 完即刻還原、`git status --short` 核實乾淨先繼續
落一步。收工最終 `git status` 確認 working tree 同 `HEAD`(`d375f9a`) 完全一致（見下面「鐵律遵守」）。

`src/config.js` 嘅 `API_BASE` 喺兩次 build 期間都改咗做 `http://localhost:3002`（harness，見 §3），
build 完即刻 `git checkout HEAD -- src/config.js` 還原，**呢個改動冇 commit 過**（`git log -p` 對
`config.js` 冇呢個 URL 嘅任何蹤跡）。

`Expo.plist` 嘅 `EXUpdatesCheckOnLaunch` patch 做喺**已經 build 好嘅 .app 產物複本**上
（`PlistBuddy -c "Set :EXUpdatesCheckOnLaunch NEVER" BEFORE.app/Expo.plist`），源頭
`ios/Odely/Supporting/Expo.plist`（仍然係 `ALWAYS`）完全冇掂——`ios/` 本身唔受版本控制，呢個做法
令改動零機會影響到其他 session 嘅 `ios/` 狀態。

**L1 教訓（跟 2B 一樣做足）**：每個 cold cycle 都 `simctl uninstall` → `simctl install` →
`get_app_container` 讀 `main.jsbundle` 實際 byte 數同期望值核對——10/10 cold cycle 全部
`match=YES`（BEFORE 期望 3,720,425B、AFTER 期望 3,725,276B，兩個 build **byte 級唔一樣**，証明
BEFORE/AFTER 唔係同一份二進位）。Raw：`ops/perf/stage2-20260902/2d-run-{B,A}-cold-{1-5}.log`。

**Harness backend（port 3002）**：`ops/perf/stage2-20260902/2d-harness-3002.mjs`——精簡 express
app，逐字複製 `backend/server.js`（HEAD=`ab78c98`，即 C-1..C-4 全部落地之後）嘅
`/api/health`、`/api/version`(連 C-4 `maybeReload()`)、`/api/hymns`(連 C-1 lite 分支)、
`/api/hymns/lyrics`(C-1)、compression 中間件（A-2 設定）呢幾段 handler 原文，`import` 嘅係
`backend/lib/serverDb.js` 嘅真 `getDb`/`getDataVersion`/`maybeReload`（讀真 `hymns.db`，唔寫）。
**冇 `import server.js`**（避開 precache/keep-warm 背景 burst，跟執行單 §2A 指示）；`/api/client-log`
自己實作一個 mini 版（格式抄 `routes/clientLog.js` 嘅 `logLine()`，但唔 `import` 嗰個檔——避免佢
嘅 `appendClientLog()` 寫入 `backend/logs/client-log/` 持久化底）。全程放喺 scratchpad，**冇放過
`backend/` 底下**（memory: scratch script 放 backend/ 會令 `backend-restart.sh` 過唔到 deploy
gate）。兩個 build 嘅 `API_BASE` 都指向呢個 harness，令 bytes/時間差歸因得返落 frontend 改動本身。
Bytes 正控：`ops/perf/stage2-20260902/2d-harness-bytes.log`——同 backend commit `8d7a2d4`
既有證據（full raw=5,567,648B/gzip=1,474,227B、lite raw=2,839,533B/gzip=371,984B、lyricsOnly
raw=2,702,660B/gzip=992,859B）分毫不差重現。

---

## 3. S1 冷開 ×5 交錯（BABABABABA，10 cycle，每個 cycle uninstall→install→verify→launch→32s→terminate）

Raw：`ops/perf/stage2-20260902/2d-run-{B,A}-cold-{1-5}.log`（host 側，含 byte 驗證）+
`2d-s1-clientlog-all.log`（beacon，逐行含唯一 deviceId，10/10 一一對應返 10 個 cycle）。

| run# | BEFORE(full) hymnsMs | BEFORE home | BEFORE byt(chars,見L1) | AFTER liteMs | AFTER lyrMs | AFTER home | AFTER lyrBytes(chars) | AFTER merged |
|---|---|---|---|---|---|---|---|---|
| 1 | 177 | 246 | 3,657,734 | 69 | 244 | 253 | 1,308,674 | 1 |
| 2 | 175 | 223 | 3,657,734 | 69 | 246 | 253 | 1,308,674 | 1 |
| 3 | 174 | 250 | 3,657,734 | 69 | 244 | 238 | 1,308,674 | 1 |
| 4 | 175 | 231 | 3,657,734 | 73 | 242 | 250 | 1,308,674 | 1 |
| 5 | 183 | 229 | 3,657,734 | 70 | 244 | 286 | 1,308,674 | 1 |
| **median** | **175** | **231** | — | **69** | **244** | **253** | — | **5/5** |

**正控**：10/10 cycle 都 `n=6405`；AFTER 5/5 `merged=1`；BEFORE 全部係 full-fetch 路徑（冇
`liteMs`/`lyrMs`/`merged` 欄，`byt` 固定 3,657,734 字元 = 完整 full payload）；AFTER 全部
`byt=2,323,605`（lite payload）+ `lyrBytes=1,308,674`（lyrics payload）固定不變——同一個
`hymns.db` 快照,數字理應唔變,10 個獨立 cycle 全部一致,證明每次都係新鮮 install、乾淨量度。

**真 wire bytes（gzip，同一個 harness，curl 直測，見 §2/`2d-harness-bytes.log`）**：
BEFORE 單一 `/api/hymns` = 1,474,227B；AFTER `/api/hymns?lite=1` = 371,984B（−74.8%）+
`/api/hymns/lyrics`（用戶撳開一首歌先使得著，唔跟開機首屏一齊落）= 992,859B。**開機首屏要等嘅
wire bytes 由 1.47MB 跌到 372KB。**

**home mark（首屏有內容,performance.now() 量）喺呢個測試環境冇睇到改善,反而 median 231→253ms
（+22ms）**——呢個係如實記錄,唔係判 PASS/FAIL：BEFORE/AFTER 都打緊 `localhost:3002`（同一部機、
迴環網絡),full fetch(175ms)同 lite fetch(69ms)嘅絕對耗時差喺呢種環境下細到俾 JS 執行期本身嘅
波動(app/cont mark 都有 10-20ms 波幅)蓋過。呢個結果同 A-6 提案原本嘅論證邏輯一致——bytes
節省要喺真實(慢過迴環好幾個數量級嘅)網絡先會轉化做睇得出嘅 home mark 改善,呢點喺 2B `PERF-
STAGE2-2B-20260902.md` §1.1(ttfb 520-550ms/body 3-9s 嘅 tunnel 環境)已經定性咗,本報告嘅 loopback
harness 冇能力重現嗰個效應——見「限制」§L2。

---

## 4. S2 熱開 ×3（每個 build:seed 1 次 populate cache,再 warm 3 次 launch→27s→terminate,唔 uninstall）

Raw：`ops/perf/stage2-20260902/2d-run-{B,A}-{seed-1,warm-{1-3}}.log` + `2d-s2-clientlog-all.log`。

| run# | BEFORE mmkvRead | BEFORE parse | BEFORE home | BEFORE verSkip | AFTER mmkvRead | AFTER parse | AFTER home | AFTER verSkip |
|---|---|---|---|---|---|---|---|---|
| 1 | 17 | 19 | 407 | 1 | 18 | 19 | 429 | 1 |
| 2 | 16 | 19 | 384 | 1 | 17 | 19 | 418 | 1 |
| 3 | 17 | 19 | 403 | 1 | 18 | 19 | 425 | 1 |
| **median** | **17** | **19** | **403** | **3/3** | **18** | **19** | **425** | **3/3** |

**正控**：6/6 run `n=6405`、`verSkip=1`（cache-bust 判斷正確 skip 咗網絡）、`hymnsMs`/`liteMs`/
`lyrMs`/`lyrBytes`/`merged` 全部 `-`（warm 路徑冇打任何 hymns/lyrics 網絡請求——同代碼設計一致,
兩個 build 都一樣,冇因為改咗 useCachedHymns.js 就意外喺 warm 路徑多咗一次 fetch）。

`mmkvRead`(17→18)/`parse`(19→19)兩個欄幾乎一模一樣——熱開路徑(MMKV 讀+JSON.parse 一個已經合併好
歌詞嘅 array)冇因為呢次改動有實質變化,同執行單「確認熱開路徑零變化」嘅要求相符。`home` mark
median 403→425(+22ms)喺量級上同 S1 見到嘅差距接近,較大機會係同一種環境雜訊(app/cont mark 本身
run-to-run 都有 10-30ms 波幅),而唔係熱開路徑代碼本身變重咗——但冇做到独立嘅「淨换 useCachedHymns.js
唔换其他」對照,呢個歸因係推論唔係實測分拆(同 2B §4.2「歸因限制」性質一樣)。

---

## 5. 正控(用戶可見結果)

1. **`n=6405`**：S1 10/10、S2 6/6 全部一致，同 `hymns.db` 全庫數一致。
2. **lyrics map key 數 = 5,387**：`ops/perf/stage2-20260902/2d-harness-bytes.log` 直接 count
   `/api/hymns/lyrics` 嘅 `data` object key 數 = 5387，同 backend commit `8d7a2d4` 記錄嘅
   「84.1%(5,387/6,405)有非空 lyrics」一致。
3. **LibraryScreen 歌詞搜尋**：截圖 `ops/perf/stage2-20260902/2d-screenshots/2d-02-lyrics-search-hit.png`——
   用戶執行 AFTER build（打緊 3002 harness），搜尋 `Cover me with Your hand`（呢句歌詞喺 id=412
   「Through It All」歌詞第 5 行，**唔喺**個標題「Through It All」出現過），詩歌庫正確搜到呢首歌
   （唯一命中，`Hillsong Worship 1`）。呢個直接證明 lite+lyrics 合併之後嘅 array 有真歌詞內容,
   `LibraryScreen.js` 嘅 `lyricsIndexRef`(reference-identity 判斷)有跟住新 array 重建索引。
4. **首屏 lite 即畫**：截圖 `2d-00-home-lite-painted.png` / `2d-01-library-6405.png`——AFTER build
   開機即見「粵語敬拜(1093)」「即刻揀歌」等內容，詩歌庫顯示「6405 首」（合併後嘅正確總數）。
5. **播放器歌詞 pill 狀態**：截圖 `2d-03-player-lyrics-pill-enabled.png`——揀返上面嗰首確認有
   lyrics 嘅「Through It All」入播放器，「歌詞」pill 顯示**非灰態**（同「最愛」「分享」「清單」
   三個常開 pill 同一透明度，冇 `pillDisabled{opacity:0.45}` 嘅視覺差異）。`hasLyrics =
   !!lyricsText` 由 `formatLyrics(cur.lyrics || getLyricsById(cur.id))` 計，pill 非灰即代表
   `cur.lyrics`（player.hymns 入面已合併嘅版本）或 fallback 成功攞到咗真歌詞字串——同 §5.2/5.3 嘅
   backend/搜尋證據互相印證。**冇做到嘅**：實際揭開歌詞 modal、截圖入面真係見到歌詞逐行文字——見
   「限制」§L3（idb 對呢個畫面嘅 touch injection 完全唔生效，非代碼問題）。

---

## 限制

- **L1**：`byt`/`lyrBytes` 兩個 perfMarks 欄讀嘅係 `text.length`（JS 字串 UTF-16 code unit 數），
  唔係 wire bytes——呢個係沿用 D-1/2B 已有嘅寫法（`hymnsBytes` note 本身就係咁樣定義），2A/Opus
  已經喺 `PERF-STAGE2-2A-OPUS-20260902.md` §6.1 指出呢類「字元 vs bytes」混淆會令 CJK 內容嘅百分比
  睇落細咗——本報告冇改呢個既有欄位嘅語義（唔喺 2D 範圍），真 wire bytes 一律另外用 curl 對住
  harness 直接量（§2/§3/`2d-harness-bytes.log`），兩者唔可以混用比較。
- **L2**：全部量度都喺 loopback（simulator ↔ `localhost:3002`）做，冇模擬 Eric 真機嘅 tunnel/蜂
  窩網絡延遲——2B `PERF-STAGE2-2B-20260902.md` §1.1/限制 L6 已經指出呢類環境限制。A-6 嘅價值主張
  （bytes 少 74.8%）喺 loopback 環境下轉化唔到做睇得出嘅 `home` mark 改善（本報告 §3 甚至見到
  median 輕微變差 +22ms），呢個唔代表 A-6 喺真實網絡無效——只代表 **本次測試環境冇能力驗證呢個
  效應**，真正嘅時間收益要喺同 2B 相同嘅慢網/tunnel 環境下用同一套 harness 重新交錯量先答得到。
- **L3**：`FullScreenPlayerOverlay` 呢個畫面對 `idb ui tap`/`swipe`（各種 duration 0.1-0.5s 都試
  過）完全冇反應——包括最簡單嘅左上角「返回」chevron 都撳唔到，`idb ui describe-point` 對呢個畫面
  任何座標都回「No translation object returned...due to a fullscreen dialog」。呢個畫面全程包喺
  `GestureHandlerRootView`（`App.js:78/4565`）之下，較大機會係 gorhom 拖曳手勢（隊列 sheet）嘅
  gesture recognizer 攔截咗 idb 嘅合成觸控事件（Library/Home 兩個純 `TouchableOpacity`/`FlatList`
  畫面用 idb 完全冇問題,問題淨係出現喺呢一個有 bottom-sheet 手勢嘅畫面）——呢個係**工具限制**，
  唔係代碼改動嘅問題，`FullScreenPlayerOverlay` 呢次改動嘅兩行本身冇改任何觸控/手勢邏輯。因此
  §5.5 淨係影到「pill 非灰態」，冇影到揭開歌詞 modal 見到逐行文字嗰張截圖。
- **L4**：`home`/`liteMs` 等 span 喺 loopback 環境下嘅絕對數值(60-300ms 級)同 JS 引擎/GC/host
  CPU 排程嘅背景雜訊(10-30ms 級)已經係同一個數量級,S1/S2 表入面嘅 median 差距(+22ms)喺呢個雜訊
  水平下唔可信可以歸因做「AFTER 變重咗」——需要更多 run 或者更穩定嘅隔離環境先答得到呢條問題,
  本報告如實列出數字,唔下判斷。
- **L5**：warm 路徑(S2)嘅 seed 步驟只等 15 秒（少過 perfMarks beacon 嘅 25 秒窗口），冇捕捉到
  seed launch 本身嘅 beacon——呢個係刻意設計（seed 只係用嚟populate MMKV cache，唔關心佢自己
  嗰次嘅 marks），但如果 seed 未真係行完merge+MMKV寫（理論上 lite 69ms+lyrics 244ms+merge 遠低於
  15秒）就轉落 warm，會令 warm 讀到嘅其實係未合併嘅 lite-only cache——本報告冇獨立驗證 seed 15
  秒內實際上寫落 MMKV 嗰份 `allHymns` 已經係合併版（只係間接由 warm run 嘅 `verSkip=1`+`n=6405`
  推論冇出事,冇直接讀 MMKV 檔核對）。
- **L6**：S1/S2 全部都係 AFTER build 對 harness backend(3002,已有 C-1..C-4)做嘅測試；BEFORE build
  對住**同一個**已經有 C-1 endpoint 嘅 harness(BEFORE 代碼本身唔識打 `?lite=1`,一直行緊
  `/api/hymns` 全量路徑,呢個係預期行為,唔係 bug)——即係話 BEFORE 呢邊嘅表現量到嘅係「舊 client
  代碼 + 新 backend」嘅組合,同 production 現狀(舊 client + 未 restart 嘅舊 backend)唔完全一樣,
  但呢個正正係執行單要求嘅「兩個 build 對住同一個 backend 先歸因得到」嘅方法論。

---

## 儀器/程式碼改動清單（commit）

| commit | 內容 |
|---|---|
| `bda9f9e` | perfMarks 加 `liteMs`/`lyrMs`/`lyrBytes`/`merged` 四個 beacon 欄 |
| `d375f9a` | C-1 A-6 client 落地——`useCachedHymns.js` 改用 lite+lyrics 拆 fetch、`App.js` 兩處歌詞讀法加 fallback |

兩個 commit 都係 pathspec-only（`git commit -m … -- <paths>`，`--` 放最後）。`config.js` 嘅
harness URL 改動全程未 commit 過。

## 鐵律遵守聲明

全程零部署、零 backend restart（3001 全程未掂）、零 Cloudflare/hymns.db 改動、`PlayerProvider`
（420-2958 行）一個字未改（唯一改動係範圍外嘅 `FullScreenPlayerOverlay` 兩行 + import）。Harness
script 全程留喺 scratchpad，未放過 `backend/` 底下。全部 git 操作前都 `pgrep -x git` 確認冇鎖、
`git status --short` 核對乾淨；收工核實 `git status` 對 `frontend/hymn-app/App.js`、
`src/hooks/useCachedHymns.js`、`src/perfMarks.js`、`src/config.js` 四個檔全部乾淨、同 `HEAD`
(`d375f9a`) 完全一致。模擬器收工已 `simctl uninstall`+`shutdown`+殺 `idb_companion`+移除
`/tmp/claude-ios-cleanup.hold`（開工時已 touch），`printf` 核對收工後 `booted=0 idb=0 sim=0
devtools=0`。
