# 第六批細尾巴收乾規劃(BATCH6,2026-08-20,Fable 5)

> 來源:BATCH5 Opus5 驗收揪出嘅 3 個 P3 + B3 gate 拍板 + review §4 兩個賬面項(#6/#8)。
> 全部係「細改動、低風險」級數;呢份文件係執行藍本,交執行 session 照做。
> ⚠️ B1–B4 已經意外上咗 prod(backend 行緊呢啲 code),所以後端三項改完
> **要過 deploy gate 先生效**,改 code 本身唔會即時影響 live。

## 0. TL;DR

| # | 項目 | 檔案 | 性質 | 出街方式 |
|---|------|------|------|----------|
| C1 | B3 gate:tee 補尾巴讓路(**拍板:加**) | `backend/lib/resolveAudio.js` | 後端 | backend restart(deploy gate) |
| C2 | P3-3:`teeChunks` 用完清空 | `backend/routes/stream.js` | 後端 | backend restart(deploy gate) |
| C3 | P3-2:warmLog `dateKey` 改本地時區 | `backend/lib/warmLog.js` | 後端 | backend restart(deploy gate) |
| C4 | P3-1:`slowHint` 轉歌唔 reset | `frontend/hymn-app/App.js` | 前端 JS | **OTA 得** |
| C5 | §4-#6:KAV 統一(**拍板:只郁 AuthScreen 一行**) | `frontend/hymn-app/src/screens/AuthScreen.js` | 前端 JS | **OTA 得** |
| C6 | §4-#8:manifest 剷 3 個權限 | `frontend/hymn-app/android/app/src/main/AndroidManifest.xml` | **native** | **同 Batch4(iOS 專項)夾埋一齊出新 build,唔開新 OTA-only 批** |

每項獨立 commit、精確 pathspec(共用 worktree 紀律,見 §6)。

**唔做**:§9(Android 拔耳機/audio focus D2 guard)——要真機/Android emulator 先驗到,
Eric 之後安排嘅係 iOS Xcode 模擬器實測(唔同平台),留到有 Android 測試能力先做,唔盲加。

---

## 1. C1 — B3 `anyStreaming()` gate(拍板:**加,但只 gate 補尾巴嗰下 fetch**)

### 1.1 拍板理由

B3 嘅 tee 分兩部份,頻寬成本完全唔同:

1. **旁聽收 head bytes**(`stream.js` 個 `'data'` listener)——呢啲 bytes 本身就係
   正播嗰條 stream 度過緊嘅,tee 只係抄一份落記憶體,**零額外頻寬**。呢部份唔使 gate,
   gate 咗反而係自廢武功(成個 B3 嘅價值就喺呢度)。
2. **`adoptStreamedHead()` 入面嘅 `fetchTailBuf()`**——呢個係**額外開一條
   backend↔googlevideo 連線**扯最多 512KB。雖然已經排喺 `withWarmLock` 隊入面
   (唔會同 warmBuffer 平行),但佢冇好似 B2(`/warm` route)咁「有人聽緊就讓路」——
   `finishTee` 喺 12MB cap 中途爆、或者 client abort 嗰刻 fire,嗰陣**幾乎實係有人
   聽緊**(好可能就係產生呢個 tee 嘅同一條 stream),即刻補尾巴就係同正播爭 VPN 頻寬。

所以:**只喺 `fetchTailBuf` 之前加 `anyStreaming()` check,聽緊就跳過補尾巴,
head 照 adopt**。

跳過尾巴嘅代價好細:

- 絕大部份詩歌 3–8MB < 12MB cap,tee 經 `'end'` 收工時 `buf.length === totalLength`,
  `fetchTailBuf` 本身就 return null——gate 對呢啲歌**零影響**。
- 淨係 >12MB 大檔或者 client 早 abort 嘅 partial tee 先有尾巴要補;跳過嘅後果係
  嗰首歌嘅尾巴 range probe 行返冷路徑,即係 BATCH5 之前嘅原有行為,唔係 regression。
- entry 冇 `tailBuf` read path 一早容忍(abort tee 補尾失敗本身就會產生 tail-less
  entry,`getBufferedChunk`/stream fast-path 照用 head、尾巴 range 行冷路徑)。
- 之後用戶開 App `/warm` 打嚟,`warmBuffer` 係**無條件 `touchBufferEntry` 蓋寫**嘅
  (`resolveAudio.js:436`),會補返完整版連尾巴。唔會「一 tail-less 就 25 分鐘冇得救」。

### 1.2 同 live tee 行為有冇衝突(因為 B1–B4 已經上咗 prod)

冇。呢個改動係**純減法**(少咗一個情況下嘅一次 fetch):

- `bufferCache` entry 形狀零變(`tailBuf: null` 本來就係合法值)。
- `adoptStreamedHead` 嘅「已有未過期同 url entry 就唔蓋」邏輯不變。
- `withWarmLock` 隊列語義不變——check 寫喺 lock 入面(即係 `adoptStreamedHead`
  嘅 async body 度),**執行嗰刻先 check**,唔係入隊嗰刻 check:tee fire 時聽緊、
  排到隊佢時已經冇人聽 → 照補;相反排到時啱啱有人開播 → 讓。同 B2「每個
  iteration 即場 check」同一個哲學。

### 1.3 點改(`backend/lib/resolveAudio.js`,`adoptStreamedHead` 入面)

而家(462–485 行附近):

```js
const tail = (totalLength && buf.length < totalLength)
  ? await fetchTailBuf(url, totalLength, buf.length)
  : null;
```

改成:

```js
// BATCH6 C1:B2 同款「有人聽緊就讓路」——head 係正播 stream 順手抄嘅,零
// 額外頻寬,照 adopt;補尾巴係額外一條 upstream 連線,聽緊就跳過(entry
// tail-less 係合法狀態,尾巴 range 行返冷路徑;之後 /warm 嘅 warmBuffer
// 會無條件蓋寫補完整)。喺 lock 入面執行嗰刻先 check,唔係入隊嗰刻。
const tail = (totalLength && buf.length < totalLength && !anyStreaming())
  ? await fetchTailBuf(url, totalLength, buf.length)
  : null;
```

`anyStreaming` 同一個檔案已經 export(76 行),module 內直接用,唔使加 import。

**Commit**:`git commit -m "..." -- backend/lib/resolveAudio.js`

**驗收**:`node --check backend/lib/resolveAudio.js`;肉眼追三個 case:
①歌細過 cap(totalLength ≤ buf.length)→ 行為同以前完全一樣;
②大檔+冇人聽 → 照補尾;③大檔+有人聽 → 跳補尾、head 照入 cache。

---

## 2. C2 — P3-3:`teeChunks` 喺 `finishTee` 之後清空

### 2.1 問題

`backend/routes/stream.js:508-534`:`finishTee` 做完 `Buffer.concat(teeChunks)` 之後,
`teeChunks` 個 array 冇清,而 `finishTee`/`'data'` listener 呢啲 closure 掛喺
`body`/`res` 度直到 response 完先釋放。撞 12MB cap 嘅大檔,`finishTee` 喺**播放中途**
就 fire 咗(cap 到即收皮),但嗰 ~12MB chunks 由嗰刻起一路吊住到成首歌播完——
白食 ~12MB(仲要係 concat 抄多咗一份之後,舊嗰份都唔放)。

### 2.2 點改

`finishTee` 兩條 return 路都清:

```js
const finishTee = () => {
  if (teeDone) return;
  teeDone = true;
  if (teeBytes < MIN_TEE_BYTES) { teeChunks.length = 0; return; } // 太少唔入 cache,但都要放走啲 chunk
  const headBuf = Buffer.concat(teeChunks);
  teeChunks.length = 0; // BATCH6 C2:concat 完即放,唔好等 response 完先由 closure 鬆手(大檔中途 cap 到會吊住 ~12MB)
  adoptStreamedHead(hymn.youtube_id, url, headBuf, teeTotalLength, teeContentType).catch(() => {});
};
```

`'data'` listener 有 `if (teeDone) return;` 頂住,清咗之後唔會再 push,零競態。
`headBuf` 係 concat 出嚟嘅新 Buffer,清 array 唔影響佢。

**Commit**:`git commit -m "..." -- backend/routes/stream.js`

**驗收**:`node --check backend/routes/stream.js`;肉眼確認三個 fire 路徑
(`end`/`error`/`res close`)都會行到清空,同埋 `teeDone` guard 保證清完唔會再 push。

### 2.3 同 C1 嘅次序

C1(resolveAudio.js)同 C2(stream.js)掂**唔同檔案**,冇 merge 衝突;
建議照 C1→C2 次序做,兩個 commit 分開。

---

## 3. C3 — P3-2:warmLog `dateKey` UTC/本地時區錯位

### 3.1 問題

`backend/lib/warmLog.js:15-17` 用 `toISOString()`(UTC)做日 key,但消費者
(`server.js` daily cron)係**本地 06:30**(部機 = 香港,UTC+8)trigger。
HK 00:00–07:59 嘅 `/warm` 記錄會落**前一日**個格;06:30 cron 行嗰刻
`dateKey(new Date())` 攞返嚟嘅「今日」其實係 UTC 嘅噚日——成個「噚日∪今日」
窗口漂咗 8 個鐘,推算錯一個日界。

### 3.2 點改

`dateKey` 改用本地日期組件,`pickWarmCandidates` 嘅 `setUTCDate` 改 `setDate`:

```js
// BATCH6 C3:改用本地日(部機 = 香港)。消費者(server.js daily cron)係本地
// 06:30 trigger,record 同 pick 必須同一個曆先對得上;之前用 toISOString()
// (UTC)令 HK 00:00-08:00 嘅記錄跌落前一日格。
function dateKey(d) {
  const y = d.getFullYear();
  const m = String(d.getMonth() + 1).padStart(2, '0');
  const day = String(d.getDate()).padStart(2, '0');
  return `${y}-${m}-${day}`;
}
```

```js
yesterdayDate.setDate(yesterdayDate.getDate() - 1); // 本地曆,同 dateKey 一致
```

其他嘢(`mergeWarmLog` 排序、cap、KEEP_DAYS prune)全部唔使掂——key 仍然係
`YYYY-MM-DD` 字串,ISO 排序 = 時序排序照成立。

### 3.3 舊數據唔使 migrate

`backend/data/warm-daily.json` 而家啲 key 係 UTC 日。切換嗰一日,HK 早上寫入嘅
記錄會同舊 UTC key 有一日重疊/錯位——呢份係 best-effort 觀察數據(cron 揀歌用),
最壞情況係切換頭一兩日候選漏咗幾首,`KEEP_DAYS=3` 三日內自我痊癒。**唔寫
migration script**,唔值得。

**Commit**:`git commit -m "..." -- backend/lib/warmLog.js`

**驗收**:`node --check backend/lib/warmLog.js`;一次性純函數快測(唔入 repo):

```bash
node -e "import('./backend/lib/warmLog.js').then(m => { const log = m.mergeWarmLog({}, '2026-08-20', [1,2]); console.log(JSON.stringify(m.pickWarmCandidates({'2026-08-19':[3],'2026-08-20':[1,2]}, new Date('2026-08-20T06:30:00+08:00'), 40))); })"
# 期望:[3,1,2] —— HK 06:30 嗰刻「今日」係 2026-08-20、「噚日」係 2026-08-19
```

(呢個測本身就係 P3-2 嘅實證:舊 code 喺 `2026-08-20T06:30+08:00` 會當
「今日=2026-08-19」,出 `[3]` 排先都係啱但「今日」格會攞錯 2026-08-19 做 today,
新 code 先攞啱。)

---

## 4. C4 — P3-1:`slowHint` 轉歌唔 reset(F10 漏咗 §2.10 嘅規格)

### 4.1 問題

`frontend/hymn-app/App.js:2319-2328`:effect dep 係 `[player.isLoading, cur.id]`。
轉歌嗰刻如果**仲係 loading 緊**(`isLoading` 冇變過 false),re-run 只會入
`if (player.isLoading)` 分支——timer 有 cleanup 重置,但 **`slowHint` 個 boolean
冇歸位**。上一首已經捱過 8 秒出咗「網絡較慢」,跳去下一首,overlay 一開波就
即刻顯示「網絡較慢,仲努力緊…」,而唔係由「正在載入音訊...」重新計過。
BATCH5 §2.10 原文寫明「離開 loading **或者轉咗歌**(cur.id 變)就清 timer 歸零」,
歸零嗰半漏咗。

### 4.2 點改

effect 開頭無條件 reset(React 對同值 setState 會 bail,唔會多 render):

```js
useEffect(() => {
  setSlowHint(false); // BATCH6 C4:effect 每次 re-run(isLoading 變 或 cur.id 變)都由頭計過——轉歌嗰陣仲 loading 緊,舊 code 唔會行 else 分支,上一首個「網絡較慢」會漏落新歌度
  if (player.isLoading) {
    slowHintTimerRef.current = setTimeout(() => setSlowHint(true), 8000);
  }
  return () => {
    if (slowHintTimerRef.current) { clearTimeout(slowHintTimerRef.current); slowHintTimerRef.current = null; }
  };
}, [player.isLoading, cur.id]);
```

(原本個 `else { setSlowHint(false); }` 分支俾開頭嗰句冚咗,直接刪。)
唔掂 provider、唔掂 watchdog/retry,同 F10 原設計一樣係 local component state。

**Commit**:`git commit -m "..." -- frontend/hymn-app/App.js`

**驗收**:肉眼追三個 case:①正常 loading 8 秒 → hint 出現(行為不變);
②loading 中途轉歌 → re-run 即 reset false + 重新計 8 秒;③離開 loading → false(不變)。
Metro export 見 §7 總驗收。

---

## 5. C5 — §4-#6:KeyboardAvoidingView 統一(拍板:**只郁 AuthScreen 一行,其餘維持現狀**)

### 5.1 現況盤點(五個用位,唔係四個)

| 檔案 | 做法 |
|------|------|
| `src/screens/PhoneLoginScreen.js:250` | `behavior={ios ? 'padding' : undefined}` |
| `src/screens/AdminAddHymnScreen.js:247` | `behavior={ios ? 'padding' : undefined}` |
| `src/components/AdminEditHymnSheet.js:160` | `behavior={ios ? 'padding' : undefined}`(Modal scrim 內) |
| `src/screens/AuthScreen.js:65` | `behavior={ios ? 'padding' : 'height'}` ← **唯一異類** |
| `src/components/AddToPlaylistSheet.js:71` | 手動 `Keyboard` listener,**有註釋寫明係刻意棄用 KAV**(「KAV 好唔穩,呢個手動做法可靠好多」) |

### 5.2 拍板

- **AddToPlaylistSheet 唔掂**——佢係文檔化咗嘅刻意決定,唔係 inconsistency。
- **AdminEditHymnSheet 唔掂**——佢包住 Modal scrim,結構同 full-screen 三個唔同,
  但 behavior 值已經同主流一致,冇嘢好統一。
- **唯一真異類係 AuthScreen 個 Android `'height'`**。manifest 已經係
  `windowSoftInputMode="adjustResize"`(`AndroidManifest.xml:26`),OS 本身會縮
  window,KAV 再用 `'height'` 疊多重會雙重補償(RN 已知 jank 源);其餘三個
  screen 用 `undefined`(Android 交返俾 adjustResize)一路行得好地地。
  改一個字:`'height'` → `undefined`,同主流睇齊。

呢個係低風險小修:AuthScreen(email 登入頁)而家主力係 PhoneLogin,曝光低;
改完同另外三個 screen 行同一條 code path。如果執行時肉眼 diff 以外想加多重保險,
可以喺 emulator 開一次 AuthScreen 睇鍵盤彈出唔遮 input 就夠,唔使專項驗收輪。

**唔抽公共 constant**(例如 `KAV_BEHAVIOR` util)——四個用位、一行 expression,
抽出嚟嘅 import 醬味大過得益,維持各檔自寫。

### 5.3 點改

`src/screens/AuthScreen.js:65`:

```js
<KeyboardAvoidingView style={styles.container} behavior={Platform.OS === 'ios' ? 'padding' : undefined}>
```

**Commit**:`git commit -m "..." -- frontend/hymn-app/src/screens/AuthScreen.js`

---

## 6. C6 — §4-#8:Android manifest 剷 3 個用唔着權限(**排入 Batch4 native build**)

### 6.1 點解要同 Batch4 夾埋

manifest 改動要**新 native build**(新 APK)先生效,OTA 推極都冇用。第四批
(iOS 專項)已經 ready 等 build——呢個 commit 而家落埋 repo,等 Batch4 開 build
嗰輪一齊入,**唔開新 OTA-only 批、亦唔為佢單獨開一輪 build**。

### 6.2 三個權限逐個處理法(唔可以齋刪行!)

呢個係 bare workflow,最終 manifest 係 **manifest merger** 合成:app main +
debug overlay + 各 library manifest。齋刪 main 嗰三行,library 帶入嚟嘅聲明
會照 merge 返落去(兩個 storage 權限行尾嗰個 `tools:replace="android:maxSdkVersion"`
就係鐵證——有 library 聲明緊同名權限先需要 replace)。正確做法:

```xml
<!-- BATCH6 C6:App 冇任何讀寫外部儲存/懸浮窗功能,剷走呢三個權限。
     tools:node="remove" 係叫 manifest merger 連 library manifest 帶入嚟嘅
     同名聲明都一齊剷,齋刪行剷唔乾淨。SYSTEM_ALERT_WINDOW 喺 debug overlay
     (src/debug/AndroidManifest.xml)有自己一份俾 dev 工具用,唔受影響
     (overlay priority 高過 main,remove 只向下游 library 生效)。 -->
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove"/>
<uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" tools:node="remove"/>
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove"/>
```

即係:原本 7/8/10 三行(連 `maxSdkVersion`/`tools:replace` 嗰啲 attr)換做上面
三行 remove 標記。`xmlns:tools` 已經喺 manifest 根度(第 1 行),唔使加。

⚠️ **執行時要驗多一步**:`tools:node="remove"` 對 debug overlay 嘅互動理論上係
「remove 只影響低 priority(library),overlay 高過 main 唔受影響」,但呢個要實證,
唔可以靠估——見下面驗收第 2 點。如果實測發現 debug build 冇咗 SYSTEM_ALERT_WINDOW
(dev menu/LogBox 會壞),fallback 係改用 `tools:node="remove"` 淨係落兩個 storage
權限、SYSTEM_ALERT_WINDOW 只齋刪 main 嗰行然後睇 merged 結果有冇 library 帶返入嚟。

### 6.3 驗收(唔使等成個 APK 砌完)

```bash
cd frontend/hymn-app/android && ./gradlew :app:processReleaseMainManifest
grep -c "READ_EXTERNAL_STORAGE\|WRITE_EXTERNAL_STORAGE\|SYSTEM_ALERT_WINDOW" app/build/intermediates/merged_manifests/release/AndroidManifest.xml
# 期望:0
```

同一招行埋 debug variant(`:app:processDebugMainManifest`),確認 debug merged
manifest **仲有** SYSTEM_ALERT_WINDOW(overlay 嗰份生效)。
最終保險:Batch4 出 release APK 嗰陣 `aapt dump permissions <apk>` 再核一次。

**Commit**:`git commit -m "..." -- frontend/hymn-app/android/app/src/main/AndroidManifest.xml`
(commit message 標明「⚠️需 native build,隨 Batch4 出」)

---

## 7. 出街分批 + 總驗收

### 7.1 分批

| 批 | 內容 | 出街動作 |
|----|------|----------|
| 後端批 | C1 + C2 + C3 | backend restart —— **必須行 deploy gate**,同埋跟返「Eric 真機 QA 進行中唔准 deploy」紅線 |
| OTA 批 | C4 + C5 | EAS Update(production channel),publish 前照 EAS 流程清場(唔夾其他 session 未 commit 嘢) |
| Native 批 | C6 | **唔單獨出**,commit 留喺 repo,Batch4(iOS 專項)開 native build 嗰輪一齊入 |

### 7.2 總驗收(執行 session 收工前全部要過)

```bash
# 後端三檔
node --check backend/lib/resolveAudio.js
node --check backend/routes/stream.js
node --check backend/lib/warmLog.js
# warmLog 純函數快測(§3.3 嗰條)
# 前端兩平台 production bundle
cd frontend/hymn-app && npx expo export --platform android && npx expo export --platform ios
# C6 merged manifest 驗證(§6.3,release + debug 兩個 variant)
```

---

## 8. 押後/唔做項

### 8.1 §9(Android 拔耳機/audio focus D2 guard)——呢批唔做

要真機或者 Android emulator 先驗到有冇副作用(audio focus 行為係 OS 層面嘢,
node --check/bundle 驗唔出)。Eric 之後嗰輪係 **iOS** Xcode 模擬器實測,平台唔啱。
留到有 Android 測試能力嗰輪,test-first,唔盲加。

---

## 9. 執行紀律、停位、rollback

### 9.1 共用 worktree 紀律(同 BATCH5 §6 一致)

- 呢個 worktree 可能有其他 session 同時做緊嘢。**每個 commit 用精確 pathspec**
  (`git commit -m "..." -- <file>`),嚴禁 `git add -A`/`git add .`/齋 `git commit -a`。
- Commit 前 `git diff -- <file>` 肉眼核對 diff 淨係自己嗰項嘅改動。
- 六個 commit 一項一個,次序:C1 → C2 → C3 → C4 → C5 → C6。

### 9.2 停位

- 改 code + commit + §7.2 驗收 → **停**。
- backend restart / OTA publish 係另一個決定,等 Eric 拍板逐批開(restart 會過
  deploy gate;OTA 前清場)。
- C6 唔存在「自己出街」呢回事,commit 完標記好就算,等 Batch4。

### 9.3 Rollback

六項全部係單檔單 commit,任何一項出事 `git revert <sha>` 即可,互相冇依賴。
唯一半依賴係 C1/C2 都圍住 tee 行為——revert 其中一個唔會整爛另一個
(C1 係 resolveAudio 入面嘅 fetch 條件,C2 係 stream.js 入面嘅記憶體釋放,正交)。
