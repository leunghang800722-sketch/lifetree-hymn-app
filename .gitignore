# Dependencies
node_modules/
.pnp
.pnp.js

# Expo
.expo/
.expo-shared/
dist/
web-build/

# Build outputs
*.apk
*.aab
*.ipa
android/app/build/
android/.gradle/
ios/build/
ios/Pods/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# 歌詞 OCR/whisper pipeline —— 大檔案唔入 repo(LYRICS-PIPELINE-PLAN §2④⑤)
backend/models/*.bin
backend/tools/ocrframe
backend/tools/*.o

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# admin audit log(MEMBERSHIP-PHASE2-ADMIN-PLAN §3.5)—— 產物,唔係 code
backend/logs/

# Environment
.env
.env.local
.env.*.local

# 會員系統獨立 DB(MEMBERSHIP-PLAN §2.1)—— 含密碼 hash/電話/email,唔可以入 repo
backend/users.db
backend/users.db.tmp
backend/users.db.lock
backend/backups/

# Backend specific
backend/node_modules/

# Media files (should not be in repo)
*.webm
*.mkv
*.mp4
*.mp3
*.m4a
*.wav
*.part

# DB backups from migrations
backend/hymns.db.bak*
backend/hymns.db.tmp

# resolve URL cache (URLs only, not audio) — PERF-FAST-START-PLAN §1a
backend/cache/

# yt-dlp cookies — 之前 commit 咗落 git 但 resolveAudio.js 完全冇用,純風險
# 冇好處(STREAM-403-FGS-CRASH-PLAN §1.2/§1.4)。舊 commit 仲留住呢個檔案,
# 當佢已經洩露處理。
backend/cookies.txt

# Claude agent auto-memory — belongs to the local session, not the project repo
memory/
