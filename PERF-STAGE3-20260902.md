# PERF-STAGE3-20260902 — dead code 清理（Sonnet 5 執行）

執行者：Sonnet 5。範圍：`PERF-STAGE3-EXEC-20260902.md` S3-1 至 S3-6。
本檔淨係列數字同證據，唔判 PASS/FAIL——「Opus5判定」一欄留白。

Stage 3 前 sha：`d547279b8a39db3994beb79aa0501af6f1c2cecb`。

**⚠️ 共用 worktree 聲明**：執行期間 2B（frontend F-1/F-2/instrument 三個 commit：
`4321f46`/`297bf52`/`8a2e729`）同一時段落咗 3 個 commit，同本次 S3-1..S3-6
六個 commit **交錯出現**喺 `git log`。本檔全部行數/檔案統計只計本次 Stage 3
自己嘅 6 個 commit（逐個 `git show --stat` 加總），**唔用** `git diff
<Stage3前sha>..HEAD` 直接對數（嗰條會夾埋 2B 嘅改動）。收工前 `git status
--short | grep -v '^??'` 只見返其他 session（1B/2B/歌詞線）本身已有嘅 M 檔，
冇本次 session 遺留嘅 uncommitted 改動。

---

## 表

| 項 | 檔 | 行數 | 零引用證據（指令 + 正控輸出） | 回歸指令 + 結果 | commit | Opus5判定 |
|---|---|---|---|---|---|---|
| S3-1 | `frontend/hymn-app/App.js.{fullbak,v134-expo-av,v135-youtube,v138-bak}`、`index.js.bak`、`backend/hymns.db.{bak,backup-week1}`（7 檔） | 3,367 行（`.db` 二進位檔以刪除前 wc -l 計，數字對 DB 檔本身無意義，同 1A 記法一致） | `grep -rn --include='*.js' --include='*.cjs' --include='*.mjs' --include='*.sh' --include='*.json' --include='*.md' --include='*.plist' -e "<filename>" . ~/Library/LaunchAgents`（排除 node_modules/dist）——7 個檔名全部只喺 baseline/規劃文件被提及（`PERF-BASELINE-1A`、`REDESIGN-PLAN.md`、`PERF-IMPROVEMENT-PLAN`、`THIRD-PASS-REVIEW`），零 code/`require`/`import`/plist 引用。正控 `resolveAudio` = 195 hit，證 grep 有效。 | `git rm` 後 `git status --short` 確認 working tree 乾淨；呢批係前端/DB 備份檔，冇 node --check 適用對象（非 backend JS route），改動範圍純 git 層面。 | `d8b7f04` | |
| S3-2 | backend root 14 個舊 script（bulk_insert_hymns.js check_hymns.cjs e2_cn_batch.cjs e2_final.cjs expand_batch.cjs expand_hymns.cjs expand_hymns_v2.cjs fetch_songs.js fix_dead_ytdlp.cjs fix_missing.js generate_hymns.js seed.js update_db.js update_hymn_link.js）+ `backend/db.js`（15 檔） | 1,660 行 | 逐個檔名 grep（`package.json`/`ops/`/`~/Library/LaunchAgents/*.plist`/`*.js\|*.cjs\|*.mjs\|*.sh\|*.json\|*.md`）：全部零真實引用，13 個自我命中係檔案內部 usage comment（`// Run: node xxx.js`），`update_hymn_link.js` 喺 `check_hymns.cjs:166` 只係一句 console 建議文字（非 `require`）。`backend/db.js` 單獨再 grep `require(['"]\./db` / `from ['"]\./db` 全 repo 只得 `backend/seed.js`（同批刪除）一個 require 者，刪咗 14 個之後零引用。正控 `resolveAudio` 195 hit。 | `node --check` 全 `backend/{server.js,routes/*.js,lib/*.js}` 過（零輸出=零語法錯）；scratchpad harness（dynamic import `routes/{home,search,category,audio}.js`+`lib/serverDb.js`，唔起 `server.js`）：`/api/hymns` 回 200、`data.length=6405`；四個已 410 化 route 仍 410；`/api/home/daily-verse` 仍 200。 | `5baf3e1` | |
| S3-3 | `backend/scripts/fetch{Keen,MusicBrainz,Sop,Tianyun,Xiaoyang,Xinxin}Catalog.js` + `oneoff-retireParkedInstrumentals-20260823.mjs`（7 檔） | 1,074 行 | 逐個檔名三管道 grep：零真實引用；`backfillAlbumFrom{Catalog,MusicBrainzCatalog}.js` 只喺 comment/console.error 提示文字提過對應 fetch 腳本名（例：「請先跑 node scripts/fetchSopCatalog.js」），冇 `require`/`import`，backfill 腳本本身冇改動、繼續讀 catalog JSON。正控同上。 | `node --check` 全 backend 三類檔過；harness 再跑一次 `/api/hymns` 仍回 6405 首。 | `3227fc3` | |
| S3-4 | `backend/routes/home.js`（唔刪 search.js/category.js/audio.js——2A `ebe29ba` 已經將呢三檔全部 handler 換成 410 stub，本身已冇 inline `queryDb()`/舊 handler/sql.js import 殘留，S3-4 檢查後確認唔使再改） | home.js：-25/+5（淨減 20 行，剷走 `queryAll`/`queryOne` 兩個 helper + `getDb` import） | `grep -rn "queryAll\|queryOne" backend --include='*.js' --include='*.mjs'` 排除 node_modules：淨係 `backend/routes/home.js` 自己識用（9 條已 410 化 route 只用 `gone`，`/daily-verse` 直接讀 `bible-verses.json` 唔經呢兩個 helper）。 | `node --check backend/routes/home.js` 過；harness：`/api/home/daily-verse` 仍 200、`/api/home/daily-quote` 仍 410、`/api/hymns` 仍 6405 首。 | `b13088f` | |
| S3-5 | `frontend/hymn-app/src/deviceId.js :: __resetForTest` | -6/+0 | `grep -rln --include='*.js' --include='*.jsx' --include='*.ts' --include='*.tsx' -e "__resetForTest"` 全 repo（含 `tools/ota-harness`、`tools/react-harness` 兩個 harness 目錄）：淨係 `deviceId.js` 自己（定義處），零 import/call。正控 `getOrCreateDeviceId` = 3 file hit（App.js/perfMarks.js/deviceId.js），證 grep 有效。 | `npx expo export --platform ios --output-dir <scratchpad>/s3-export`：exit=0，7099ms bundle，1407 modules，hbc = 3,720,357 B；`ls -la` 存 `ops/perf/stage3-20260902/s3-5-export-hbc-size.log`。 | `bbaeec8` | |
| S3-6 | `YTDLP-UNIFY-PLAN-20260822.md`（4 處）、`THIRD-PASS-REVIEW-20260822.md`（2 處）加註「（2026-09-02 Stage 3 已移除）」；`HANDOFF.md` 全文 grep 冇提及任何被刪檔名，唔使改 | 净 0（純加註解字串，段落唔刪） | — | — | `2c47252` | |

---

## 保留、未刪嘅項目（同原因）

| 項目 | 原因 |
|---|---|
| `backend/hymn-check-report.txt`、`backend/title-norm-diff-{applied,round2}-20260821.csv`（3 個 tracked 產物檔，執行單原列為候選） | 雖然生成者 `check_hymns.cjs` 已刪、程式碼零引用，但 `docs/HISTORY.md`（`hymn-check-report.txt` 更正記錄）、`docs/SUPERVISION-LOG.md`、`docs/LYRICS-CATCHUP-LEDGER.md`（title-norm 兩個 CSV，記錄咗真實 deploy gate 卡住事故）有實質提及，屬執行單「如果有 docs 提及（例如 title-norm 驗收記錄），保留」嘅例外，冇刪。 |
| `backend/routes/search.js`、`category.js`、`audio.js` 本身唔刪檔 | 執行單明文要求（Opus 建議等 access log 跑一日先判斷有冇未知 client），2A 已經將呢三檔 handler 全部換成 410 stub，S3-4 檢查後確認冇殘留 unreachable code 可再剷。 |
| `frontend/hymn-app/App.js`、`src/**`（除 deviceId.js 一個 export） | 執行單明文禁止，2B 同時段正在改動並 build。 |
| 碟上 11GB `hymns.db.bak-*`/`*.webm`、`.git` gc | 執行單明文禁止，等 Eric 拍板。 |

---

## 總計（本次 Stage 3 六個 commit，逐個 `git show --stat` 加總；唔用 base..HEAD 直接對數，理由見頁首共用 worktree 聲明）

- 檔案改動：**35 個檔**（S3-1: 7、S3-2: 15、S3-3: 7、S3-4: 1、S3-5: 3、S3-6: 2，逐項唯一檔案冇重複）
- 插入：**30 行**（全部嚟自 S3-4/S3-5/S3-6 嘅小改動；S3-5 含 2 個新 raw log 檔）
- 刪除：**6,138 行**（S3-1: 3,367 + S3-2: 1,660 + S3-3: 1,074 + S3-4: 25 + S3-5: 6 + S3-6: 6）
- 淨減：**6,108 行**
- 移除檔案總數（唔連 S3-4/S3-5/S3-6 嘅原地編輯）：**29 個檔**（7 + 15 + 7）

Commits（按執行順序）：
| commit | 內容 |
|---|---|
| `d8b7f04` | S3-1：7 個 git tracked 舊備份檔 |
| `5baf3e1` | S3-2：backend root 14 個舊 script + db.js |
| `3227fc3` | S3-3：6 個目錄爬蟲 + 1 個 oneoff script |
| `b13088f` | S3-4：home.js 剷 queryAll/queryOne/getDb import |
| `bbaeec8` | S3-5：deviceId.js 剷 __resetForTest |
| `2c47252` | S3-6：文檔加註 |

---

## 限制

1. **共用 worktree 交錯**：2B 三個 commit（`4321f46`/`297bf52`/`8a2e729`）同本次六個 commit 喺同一段時間交替出現，`git log --oneline base..HEAD` 顯示嘅 9 個 commit 入面有 3 個唔屬於本次任務。本報告全部統計已改用逐 commit `git show --stat` 加總，避免夾帶 2B 嘅改動；但如果之後有人直接用 `git diff <base>..<HEAD_at_report_time>` 對數，會攞到偏大嘅數字，要留意呢一點。
2. **S3-4 冇改動 search.js/category.js/audio.js**：呢三個檔喺 2A（`ebe29ba`）已經被剷到最簡（淨返 router 掛載 + `gone` stub + header comment），S3-4 讀完之後確認冇殘留 inline `queryDb()`/舊 handler/sql.js import 可再剷，屬「檢查完發現冇嘢好做」，唔係漏做。
3. **`backend/routes/search.js`/`category.js`/`audio.js` 本身冇刪**：跟執行單指示保留（等 access log 一日真流量），呢個係刻意決定，唔係遺漏；`[deprecated-route]` log middleware（2A A-3）已經喺度，理論上第日有真流量會留痕。
4. **3 個 tracked 產物檔冇刪**（見上表）：因為 docs 有實質歷史記錄提及，跟執行單「保留」條款處理，唔屬於「零引用」清單但屬於「有文檔佐證，故意保留」。
5. **S3-1 嘅 `.db`/`App.js.*` 備份檔行數**：`hymns.db.bak`/`hymns.db.backup-week1` 係二進位 SQLite 檔，`wc -l`/diff line count 對呢兩個檔無實際意義（跟 1A A4 記法一致，純粹記錄 git 顯示嘅刪除行數）。
6. **回歸驗證嘅覆蓋範圍**：全部 backend 回歸用 scratchpad harness（dynamic import route/lib module，唔起 `server.js`），跟 2A 同一套方法論（避開 `server.js` `app.listen()` callback 嘅無 guard precache burst）。呢個唔係「一鏡到底」跑成個 `server.js` 嘅端到端驗證，同 2A 報告嘅限制一致。
7. **S3-5 冇再驗證真機/模擬器行為**：`expo export` 只證明 bundle 編譯通過，唔係證明 app 喺 runtime 行為不變（`__resetForTest` 本身係死 export，理論上冇 runtime 風險，但冇額外做 iOS 模擬器人手驗證，跟執行單「成功=bundle 編譯通過」嘅要求一致）。
