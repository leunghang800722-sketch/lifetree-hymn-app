# O1 + O2 重新規劃(2026-08-19,Fable 5)

> 背景:`FRONTEND-CODE-REVIEW-20260819.md` 嘅 O2(useCachedHymns singleton)第一次落實(c9bd715)
> 撞咗 P0 race 被 revert(4f8b369);O1(拆 PlayerContext)執行 session 評估風險太高冇做。
> Eric 要求:診斷清楚、重新規劃,先規劃唔落手。本文件係規劃產物,未有任何 code 改動。

---

## 0. TL;DR

- **O2**:改用 React 內建 `useSyncExternalStore`(下稱 uSES)。呢個 hook 正正係為「訂閱遲過
  broadcast」呢類 external-store tearing 而設計:mount effect 入面會自動對比「render 嗰刻讀到嘅
  snapshot」同「而家 store 嘅 snapshot」,唔同就強制 re-render。上次出事嘅時序喺呢個設計下
  **結構上唔存在**,唔係靠彩數。已逐步推演(§2.3)。
- **O1**:**推翻 review 原建議**(拆 `PlayerProgressCtx` + `useMemo` 大 value + `React.memo` 三個
  tab——嗰套要 30 個 field 逐個入 dependency array、逐個 callback 包 `useCallback`,正正係
  stale closure 高危區)。重新診斷發現每秒 re-render 嘅**唯一**源頭係 `setCurrentTime` 呢粒
  React state;所以正確修法係**將 currentTime/duration 搬出 React state、搬入同一個 external
  store**,進度條抽做一個細 component 自己訂閱。改動面窮舉得晒(§3.2),仲可以拆兩步 commit,
  每步獨立 revert。
- 兩項共用一個 ~20 行嘅 `createExternalStore` helper,O2 先行,幫 O1 探埋路。
- 驗證:execution session 可以用 react-dom + jsdom 喺 Node 起真 React reconciler harness
  (Opus5 上次就係咁實錘個 bug),**先對住 c9bd715 舊 code 跑到紅、再對住新設計跑到綠**;
  O1-B 收尾嗰步另需 emulator/真機 5 項 smoke(§4.3)。

---

## 1. 事實基礎(今次診斷實測)

- React `19.2.3` / RN `0.85.3` / Expo 56(`frontend/hymn-app/package.json`)→ uSES 內建可用;
  `react-dom@19.2.3` 已喺 node_modules(expo web 依賴)→ Node harness 有得行。
- `useCachedHymns()` 得兩個 consumer:[App.js:2770](frontend/hymn-app/App.js:2770)(AppContent,parent)
  同 [MineScreen.js:57](frontend/hymn-app/src/screens/MineScreen.js:57)(child)。兩個喺同一個 commit
  一齊 mount(三 tab keep-mount)。MineScreen 只攞 `hymns` reference 落 dep array 做「歌庫改咗」
  訊號([MineScreen.js:67](frontend/hymn-app/src/screens/MineScreen.js:67))。
- `usePlayer()` 全 app 得 **3 個 consumer,全部喺 App.js**:MiniPlayer(1929)、
  FullScreenPlayerOverlay(2118)、AppContent(2769)。`src/` 底下零引用(grep 實證)。
- `currentTime`/`duration` state 嘅全部讀寫位(grep 窮舉):
  - 寫:poll loop [App.js:1357](frontend/hymn-app/App.js:1357)/[1365](frontend/hymn-app/App.js:1365)、
    seek [App.js:1832](frontend/hymn-app/App.js:1832)
  - 讀:context value [App.js:1854](frontend/hymn-app/App.js:1854)、
    `handleProgressBarPress` [App.js:1828-1831](frontend/hymn-app/App.js:1828)、
    overlay 進度區 [App.js:2238](frontend/hymn-app/App.js:2238)/[2353-2354](frontend/hymn-app/App.js:2353)
  - context 入面嘅 `setCurrentTime, setDuration`([1862](frontend/hymn-app/App.js:1862))**零 consumer**,係死 export。
- 每秒 re-render 嘅 driver 分析:
  - `setCurrentTime(pos)` — **每秒必變** → PlayerProvider re-render → inline context object 全新
    → 3 個 consumer 連埋三個 keep-mount tab 全部 reconcile。呢個係唯一每秒源頭。
  - `setTrackState(val)`(state poll)— 播放穩定期 val 唔變,React same-value bailout,唔會引發每秒 render。
  - `setDuration` — 只喺轉歌先變(仲有 `>0` 先寫嘅 B14 guard)。
  - 其餘 poll 產物(drift/stall/watchdog)全部寫 ref。
- **結論:唔使拆 context、唔使 memo 三個 tab —— 淨係將 currentTime/duration 移出 React state,
  每秒 re-render 就完全消失**(PlayerProvider 本身都唔再每秒 render,連 inline value object 都唔再
  每秒重造)。

---

## 2. O2 重新規劃:`useCachedHymns` → uSES singleton

### 2.1 上次點死(c9bd715 時序,Opus5 reconciler 實錘)

1. AppContent render:`useState(sharedHymns)` 讀到 `null`
2. MineScreen render:同樣 `null`
3. React flush effects,**child 先過 parent**:
   - MineScreen 訂閱 effect:加 `setHymns_child` 入 listener set
   - MineScreen kick effect:`refreshKicked=false→true`,**同步**讀 MMKV →
     `broadcastHymns(parsed)` + `broadcastLoading(false)` —— 呢刻 listener set 得 child 一個
4. AppContent 訂閱 effect 先至行 —— **已經錯過咗 broadcast**
5. 之後 refresh() 行到網絡段:version 冇變 → `canSkip` → 永遠冇下一次 broadcast
6. → parent 永遠 `hymns=null, loading=true` → 首頁永久轉圈、詩歌庫永久空白,
   而且中招嘅係「有 cache + version 冇變」呢條**日常開機最常見路徑**

根因一句講晒:**「用 useState 影一份 snapshot + 用 effect 補訂閱」呢個 pattern 喺
render 同 effect 之間有窗口,任何同步 broadcast 跌入窗口就蝕咗**。手動補一句「mount 嗰刻
追讀 shared 值」(方案 a)修到今次,但等於自己手寫 uSES 嘅核心邏輯,冇理由唔直接用官方嗰個。

### 2.2 新設計(建議採納:uSES)

**新檔** `frontend/hymn-app/src/hooks/externalStore.js`(O1/O2 共用,~20 行):

```js
import { useSyncExternalStore } from 'react';

// 通用 external store:一份 module-level state + uSES 訂閱。
// ⚠️ getSnapshot 必須回傳同一個 object reference 直至 setState 換咗佢,
//    唔可以喺入面即場砌新 object(uSES 會無限 re-render)。
export function createExternalStore(initialState) {
  let state = initialState;
  const listeners = new Set();
  const subscribe = (fn) => { listeners.add(fn); return () => listeners.delete(fn); };
  const getSnapshot = () => state;
  const setState = (partial) => {
    state = { ...state, ...partial };
    listeners.forEach((fn) => fn());
  };
  const useStore = () => useSyncExternalStore(subscribe, getSnapshot);
  return { getSnapshot, setState, useStore };
}
```

**改寫** `useCachedHymns.js`(fetch/version/MMKV 邏輯全部唔郁,只換 state 載體):

```js
const hymnsStore = createExternalStore({ hymns: null, loading: true });

let refreshKicked = false;
function kickRefreshOnce() {
  if (refreshKicked) return;
  refreshKicked = true;
  // === 現有 83-137 行嘅 effect body 原封搬入嚟,只做三個機械替換 ===
  // setHymns(x)      → hymnsStore.setState({ hymns: x })
  // setLoading(x)    → hymnsStore.setState({ loading: x })
  // (hadCache/canSkip/loading 語義、retry、catch 路徑逐行保留)
}

export function notifyHymnsChanged(serverDataVersion) {
  // 現有 67-81 行不變,尾行 hymnsListeners.forEach(...) 改為
  // hymnsStore.setState({ hymns: fresh });
}

export const useCachedHymns = () => {
  const { hymns, loading } = hymnsStore.useStore();
  useEffect(() => { kickRefreshOnce(); }, []);   // 第一個 mount 嘅 instance kick
  return { hymns: hymns || [], loading };        // API 對外完全不變
};
```

刪走:`hymnsListeners` set、兩個舊 effect。兩個 consumer(App.js / MineScreen)**一行都唔使改**。
`hymns || []` 語義照舊(MineScreen dep-array 訊號行為不變)。`refreshKicked` 永不重置,
dev StrictMode double-effect 都只 kick 一次。

### 2.3 針對「parent+child 同一 commit mount」逐步推演(上次死因場景)

有 cache + version 冇變(上次中招路徑):

1. AppContent render:uSES 讀 snapshot **S0**(`{hymns:null,loading:true}`)
2. MineScreen render:同樣 S0
3. Effects flush(child 先):
   - MineScreen 嘅 uSES 內部 effect:訂閱 + 對比「render 讀嘅 S0」vs「store 而家嘅 S0」→ 冇變
   - MineScreen 嘅 kick effect:`kickRefreshOnce()` 同步讀 MMKV → `setState` → store 換成 **S1**
     (`{hymns:[...6232],loading:false}`);呢刻 listener 只有 child → child re-render 攞到 S1
4. AppContent 嘅 uSES 內部 effect 行:訂閱,**同時對比「render 讀嘅 S0」vs「store 而家嘅 S1」
   → 唔同 → uSES 強制 AppContent re-render,攞到 S1** ← 上次漏咗嘅缺口,而家由 React 內部包底
5. 之後 refresh() 網絡段 canSkip,冇再 broadcast —— 但 parent 已經攞咗 S1,冇嘢等緊

其餘場景:冷開機冇 cache(唯一 broadcast 喺網絡返嚟之後,兩邊早已訂閱好,照舊);version 有變
(MMKV 快 broadcast + 網絡慢 broadcast,兩次都經 setState,任何時序都冇窗口);
admin `notifyHymnsChanged`(mount 完好耐先發生,普通訂閱路徑);unmount(uSES 自己 cleanup)。

點解 uSES 包到底:佢喺 mount effect 入面**規定咗**要 `checkIfSnapshotChanged`,「broadcast 早過
訂閱」由 bug 變成 spec 內處理咗嘅正常情況。呢個唔係我哋自己手寫嘅補丁,係 React 承諾嘅行為。

### 2.4 O2 驗收(execution session 自己做到,唔使 emulator)

用 Node + `react-dom/client` + jsdom 起真 reconciler harness(Opus5 上次實錘 bug 用嘅同款方法):

1. scratchpad 起獨立 npm 項目,裝 `jsdom`;`react`/`react-dom` 直接 require app 嘅
   node_modules(保證 19.2.3 同版)
2. 複製 `useCachedHymns.js` 入 harness,**只改頭兩行 import**(`react-native-mmkv` → in-memory
   mock;`../config.js` → stub),diff 出嚟證明其餘逐 byte 相同
3. mock `fetch`:`/api/version` 回 `v1`;`/api/hymns` 回固定列表
4. 場景 A(killer path):預先種 MMKV `allHymns` + `allHymnsVersion='v1'`,render
   `<Parent><Child/></Parent>` 兩個都用 hook,`act()` flush 完 assert **兩邊**都
   `hymns.length>0 && loading===false`
5. **先用 c9bd715 舊版 code 跑場景 A,必須見紅**(證明 harness 捉得到原 bug),再換新設計見綠
6. 加場景:冷開機冇 cache、version 有變、`notifyHymnsChanged`、unmount 後 setState 冇 warning

風險評估:**低**。邏輯零改動、載體換官方 API、驗收 harness 對正死因場景。剩返嘅殘餘風險
(uSES 喺 RN 0.85 行為)極低 —— RN 官方 renderer 本身就係用呢個 hook 生態。

---

## 3. O1 重新規劃:每秒全 app re-render

### 3.1 點解推翻 review 原建議

原建議(拆 `PlayerProgressCtx` + context value 包 `useMemo` + 三 tab 包 `React.memo`)嘅問題:

- `useMemo` 個 value 有 **30 個 field**,入面十幾個係每次 render 重造嘅 function;要 memo 有效
  就要逐個包 `useCallback`,每個 dependency array 都係一個 stale closure 地雷 —— 正正係
  執行 session 判斷「靜態推演睇唔出、要真機先見到」嘅嗰類風險,而且改動面大到 review 唔窮舉得晒。
- `React.memo` 三個 tab 會改「幾時 re-render」嘅語義,任何漏咗嘅 prop/context 依賴都變 stale UI。
- **而且根本唔需要**:§1 已證明每秒 re-render 得一個 driver(`setCurrentTime`)。拔咗佢,
  PlayerProvider 唔再每秒 render,inline value object 唔再每秒重造,三個 consumer 自然靜晒 ——
  30 個 field 一個都唔使掂。

### 3.2 新設計:progress 搬出 React state(O1-B),分兩步 commit

用返 §2.2 同一個 `createExternalStore`:

```js
// App.js 頂層(PlayerCtx 附近)
const progressStore = createExternalStore({ currentTime: 0, duration: 0 });
function usePlayerProgress() { return progressStore.useStore(); }
```

**Commit B1(行為完全不變,純結構準備):**

1. 加 `progressStore`;poll loop 每 tick **雙寫**:照舊 `setCurrentTime`/`setDuration`,
   另加 `progressStore.setState({...})`(1357/1365 兩位,保留 `duration>0` 嘅 B14 guard)
2. 將 overlay 進度區([App.js:2344-2356](frontend/hymn-app/App.js:2344) + 2238 嘅
   `progressPercent`)抽出做 `function ProgressSection()`,內部 `usePlayerProgress()` 攞
   currentTime/duration,`usePlayer()` 攞 `formatTime`/`handleProgressBarPress`;
   FullScreenPlayerOverlay 原位換成 `<ProgressSection />`
3. seek([App.js:1832](frontend/hymn-app/App.js:1832))雙寫 store

→ B1 之後 UI 數據流已經行 store,但每秒 setState 仍在;純加法,靜態 review 可以完全確認。

**Commit B2(真正拔掣):**

4. 刪 [App.js:325-326](frontend/hymn-app/App.js:325) 兩粒 useState;poll/seek 只寫 store
5. `handleProgressBarPress` 讀 `progressStore.getSnapshot().duration`(module-level 讀,
   永遠新鮮,仲順手消滅咗原本讀 state closure 嘅隱性 staleness)
6. context value 剷走 `currentTime, duration, setCurrentTime, setDuration` 四個 field
   (§1 已 grep 實證 `setCurrentTime/setDuration` 零 consumer、`currentTime/duration`
   唯一 consumer 係已抽走嘅進度區)

→ 效果:播放中每秒得 `ProgressSection` 一個細 component re-render;overlay 收埋時
(`display:'none'` + 冇 mount)**零訂閱者,連一個 re-render 都冇**。呢個結果好過原建議嘅
context 拆分。B2 出事可以單獨 revert B2,B1 留低無害。

### 3.3 O1-A:poll 慢速(獨立細 commit,可以最先做)

[App.js:1429](frontend/hymn-app/App.js:1429) 嘅 `setTimeout(1000)` 改做自適應:
`trackStateRef.current` 唔係 Playing/Buffering 時目標 2500ms,但**用 500ms 分片瞓、每片檢查
state**,一轉活躍即刻返 1s 節奏(唔會撳完 play 要等 3 秒先郁進度)。Watchdog 唔受影響
(佢哋只喺 Playing/Buffering 計 tick,嗰陣時仍然係 1s)。drift 探測([1342](frontend/hymn-app/App.js:1342))
改為減「今次實際目標間隔」而唔係寫死 1000。隔籬個 state poll loop(1438)可以同款處理。
風險:極低,靜態確認得晒。

### 3.4 明確唔做(除非日後實測話唔夠)

- ❌ 拆 `PlayerProgressCtx`(store 方案已冚)
- ❌ context value 包 `useMemo` / 十幾個 function 包 `useCallback`
- ❌ 三個 tab 包 `React.memo`
- 做完 O1-B 之後,AppContent/三 tab 只會喺真實播放事件(轉歌/播停/隊列變)先 re-render,
  嗰啲每幾分鐘一次,唔值得為佢哋加 memo 風險。

### 3.5 O1 風險評估

| 部分 | 風險 | 靜態確認得到? |
|---|---|---|
| O1-A poll 慢速 | 極低 | ✅ 完全 |
| B1 雙寫+抽 ProgressSection | 低 | ✅ 完全(純加法) |
| B2 拔 state+剷 context field | 低-中 | ✅ 改動面窮舉得晒(§1 grep 實證),但「進度條/seek 實際跳唔跳」呢類視覺行為要裝置 smoke 收尾 |

殘餘風險集中喺 B2 嘅視覺確認,見 §4.3。

---

## 4. 執行分工同順序

### 4.1 建議順序

1. **O2**(連 harness,§2.4)—— 順手起咗 `externalStore.js`,幫 O1 探路
2. **O1-A** poll 慢速
3. **O1-B1** → 靜態 review → **O1-B2**
4. 裝置 smoke —— **已拍板延後**(§6):唔喺呢輪做,歸入 batch 1-4 全部完成後嘅
   consolidated testing 一次過測

每步一個 commit,獨立 revert。⚠️ 多 session 共用 worktree:跟返「唔用 `git add -A`、
commit 用 pathspec」嘅現行紀律。

### 4.2 execution session 可以直接做(唔使裝置)

- `externalStore.js` + O2 改寫 + §2.4 harness 全套(**紅→綠流程係硬性驗收條件**:
  先對 c9bd715 版跑到 fail,再對新版跑到 pass,兩份輸出都要留底)
- O1-A、O1-B1、O1-B2 嘅 code + 每步之後 grep 證明:
  `grep -n "setCurrentTime\|setDuration" App.js` 淨返 store 路徑;
  context value 冇殘留 progress field;`usePlayer()` consumer 冇人再讀 `player.currentTime`

### 4.3 要額外驗證先可以出街(O1-B2 之後)

> ⚠️ 2026-08-19 已拍板(§6):呢 5 項**唔喺執行輪做**,執行 session commit 完 B2 就停,
> 5 項歸入所有 batch 完成後嘅 consolidated testing 清單。

裝置 smoke 5 項(emulator session 或者 Eric 真機,~2 分鐘):
1. 播歌,全屏播放器進度條每秒郁、時間 label 啱
2. 進度條 seek,UI 即時跳到位、聲跟到
3. mini player 播/停 icon 即時反應
4. 自動轉歌後標題/封面/總長更新正常
5. shuffle 一下,總長冇彈 0:00(B14 guard 仍生效)

O2 本身 harness 已冚死因場景,可以跟正常 OTA 流程出;但建議同 O1 一齊等埋 smoke 先推,
一次過驗。**唔好喺 Eric QA 進行緊嗰陣部署**(現行紀律)。

### 4.4 Rollback 預案

- O2 出事:revert 單一 commit 即返 4f8b369 現狀(consumer 零改動,冇連鎖)
- O1-B2 出事:單獨 revert B2,B1(雙寫)自己企得穩
- O1-A 出事:單獨 revert

---

## 5. 開放問題 —— 已全部拍板,見 §6

(原問題:①裝置 smoke 邊個做幾時做;②harness 要唔要入 repo。)

---

## 6. 拍板結果 + 執行指引(2026-08-19,Eric 拍板 / Fable5 補充)

### 6.1 拍板一:O1 裝置實測延後

Eric 原則係「做晒全部先一次過出街」:§4.3 嘅 5 項裝置 smoke **唔喺呢輪執行做**。
執行 session 落實到 O1-B2 commit 為止就**停**,5 項成為之後 consolidated testing
(batch 1-4 全部完成後一次過測)嘅清單項目。呢輪唔開 emulator、唔搞 OTA、唔叫 Eric 驗機。

### 6.2 拍板二:harness 正式 commit 入 repo(Fable5 建議,理由如下)

**決定:要,位置 `frontend/hymn-app/tools/react-harness/`。**

理由:
1. **呢個位已經實證係 regression 陷阱**——`useCachedHymns` 出過一次 P0 revert(c9bd715),
   而佢嘅失效模式(訂閱/broadcast 時序)正正係「靜態 review 睇唔出」嗰類;將來任何 session
   再掂呢個檔(或者 O1 之後共用嘅 `externalStore.js`),冇裝置都可以 30 秒重跑 harness 做 gate。
2. **scratchpad 係 session 專屬、會清走**——唔入 repo 嘅話,下一個要改呢忽嘅 session 得返
   文件描述,要由零重砌 harness 先驗到,實際上等於冇。
3. **成本極細、零污染**——自包含目錄:自己嘅 `package.json`(得 `jsdom` 一個 dev dep,
   `react`/`react-dom` 直接 require 上層 app 嘅 node_modules 保證同版),唔掂 app 嘅
   dependencies、唔入 Metro bundle、唔影響 build。
4. **佢係死因場景嘅可執行文檔**——比起 prose 描述個 race,一個跑得嘅紅→綠測試先係最準確嘅記錄。

維護要求(執行 session 要跟):
- runner script **運行時**先由 `src/hooks/useCachedHymns.js` 複製現行源碼、sed 只改頭兩行
  import(MMKV mock + config stub)——永遠測「而家嘅 code」,唔准 commit 一份會過時嘅 copy;
  sed 前後 diff 必須只有嗰兩行,唔係就 fail
- 測試 fail 要 exit non-zero,俾將來 session 直接當 gate 用
- 目錄入面加個簡短 README:點跑、cover 咩場景、紅→綠驗收流程係乜

### 6.3 交俾執行 session(Sonnet5)嘅工作範圍

**要做(全部唔使裝置):**
1. `src/hooks/externalStore.js`(§2.2 helper)
2. O2:`useCachedHymns.js` 改寫(§2.2)+ harness 入 `tools/react-harness/`(§2.4 + §6.2)
   —— **紅→綠流程係硬性驗收**:先對 c9bd715 舊版跑到 fail,再對新版跑到 pass,兩份輸出留底
3. O1-A:poll idle 慢速(§3.3)
4. O1-B1 → O1-B2(§3.2),每步之後跑 §4.2 嘅 grep 證明
5. 每步獨立 commit(用 pathspec commit,唔准 `git add -A`——多 session 共用 worktree)

**去到邊度停:**
- O1-B2 commit 完、grep 證明齊 → **停**。唔做裝置 smoke、唔推 OTA、唔 restart backend。
- 有任何一步 harness 轉唔到綠、或者 grep 證明對唔上 → 停低回報,唔准自行改設計硬推。
