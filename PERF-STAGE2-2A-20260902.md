# PERF-STAGE2-2A-20260902 — backend（Sonnet 5，§2A A-1..A-6）

執行者：Sonnet 5。範圍：`PERF-STAGE2-EXEC-20260902.md` §2A（A-1 至 A-6，唔含 2B frontend）。
本檔淨係列數字同觀察，唔判 PASS/FAIL——「Opus5判定」一欄留白。
Raw 全部喺 `ops/perf/stage2-20260902/`（檔名與下表對應）。

## 0. 環境設置聲明（讀 server.js 嘅結論）

讀 `backend/server.js` 全文（615 行）搵開機背景 burst：`URL_KEEPWARM=0` 可以停埋兩個
keep-warm timer（`startKeepWarm()`／`warmColdBacklog()`），**但 `app.listen()` callback
入面嘅背景 pre-cache 區塊（`PRECACHE_MAX=200`、concurrency 2、`resolveAudioUrl()` →
真實 yt-dlp → YouTube）完全冇 env guard，係無條件執行**。呢個就係 CLAUDE.md 記錄過嘅
「全機 IP 曾經因為呢種爆發式流量俾 YouTube ban」嗰種模式——起第二個完整 `server.js`
instance 喺 PORT=3002 會即刻觸發呢個 burst，唔可以起。

按執行單許可嘅 fallback：改用直接 import route handler 嘅 harness
（`/private/tmp/.../scratchpad/harness-server.mjs`，唔喺 repo 內，唔會撞 deploy gate）。
呢個 harness：
- 用 `createRequire(backend/package.json)` 解析 `express`/`compression`/`cors`（backend 自己
  嘅 node_modules），用 `import(pathToFileURL(...))` 動態 import backend 嘅真實檔案
  （`lib/serverDb.js`、`routes/home.js`、`routes/search.js`、`routes/category.js`、
  `routes/audio.js`，A-2 驗證時額外加 `routes/stream.js`／`routes/hls.js`）——**唔重新
  實作邏輯**，改完真實檔案之後重啟 harness process 就即刻反映新代碼。
- **完全唔行** `server.js` 自己嗰段 pre-cache / keep-warm timer / daily-warm cron
  bootstrap（呢啲邏輯只存在於 `server.js` 本身嘅 `app.listen()` callback,harness
  唔 import `server.js`,結構上唔會觸發）。
- `/api/stream`、`/api/hls` 驗證用 id=42(youtube_id=`PG_J_0gsMXA`)——同 1A A1 一樣嘅
  warm id,`backend/cache/resolve-cache.json` 開工時仲有 72 分鐘先過期(`expire=1788339735`,
  量度開始 `1788335403`),讀嘅係碟上已有嘅 cache,冇再打過 YouTube。

## 1. A-1 — `/api/hymns` response cache（keyed on `getDataVersion()`）

改動：`backend/server.js` — 加一個 module-level `hymnsResponseCache = { dataVersion, json }`，
`dataVersion` 唔變就直接 `res.send()` 返上次 `JSON.stringify()` 完嘅 string，唔再行
SELECT/getAsObject/stringify。`reloadDb()`（admin 寫入後必 call）令 `getDataVersion()`
讀出新值，自然觸發下一次 miss——冇加獨立 invalidation flag。

| 項 | 改前 min/med/max (ms) | 改後 min/med/max (ms) | raw 檔 |
|---|---|---|---|
| `curl /api/hymns` total ×5 | 78.5 / 83.1 / 131.8 | 12.3 / 12.5 / 134.9（run1=134.9 係冷啟動首次 compute，run2-5 全部係 cache hit） | `2a-a1-before.log` / `2a-a1-after.log` |

**正控（dataVersion 變 → cache miss）**：唔准掂真 `hymns.db`，改用 `hymns.db` 嘅**碟上複製**
（`fs.copyFileSync`,讀完即刪,冇留低任何新檔），跑 `lib/serverDb.js` 嘅
`computeDataVersion()` 同 `server.js` 個 cache 判斷嘅逐字複製演算法：
```
[before reload] dataVersion=1788335333291.3855-61054976 r1.hit=false r2.hit=true computeCount=1
[after touch]  dataVersion=1788335333294-61054976 changed=true r3.hit=false r4.hit=true computeCount=2
PASS: cache hit/miss follows dataVersion exactly (miss→hit→miss-on-change→hit)
```
raw：`2a-a1-positive-control.log`。收工後 `git status --short backend/hymns.db` 確認真
DB 冇被呢個測試碰過（見下面「共用 worktree 聲明」）。

commit：`06d0cb8`

## 2. A-2 — `compression` middleware（gzip JSON，排除媒體 route）

`npm i compression`（backend/package.json + package-lock.json）。全域掛
`compression({ threshold: 1024, filter: ... })`，filter 對 `/api/stream`、`/api/hls`、
`/api/audio`、`/app.apk`、`/downloads` 一律 `return false`。

| 項 | 改前 | 改後 | raw 檔 |
|---|---|---|---|
| `/api/hymns`（`Accept-Encoding: gzip`）payload | 5,567,648 B | 1,474,227 B（-73.5%） | `2a-a1-before.log`／`2a-a2-after.log` |
| `/api/hymns`（`Accept-Encoding: gzip`）total ×5 (ms) | 78.5–131.8（同上,呢個係無 compression 嘅基準） | 107.7 / 108.8 / 232.8 | `2a-a2-after.log` |
| `/api/hymns`（冇帶 `Accept-Encoding`）| size=5,567,648，冇 content-encoding | size=5,567,648，冇 content-encoding（行為不變——冇 gzip 支援嘅 client 照收 plain） | `2a-a2-after.log` |
| `/api/health`（<1KB，threshold 應該唔壓） | — | `Content-Length: 15`，冇 `Content-Encoding` | `2a-a2-after.log` |

**stream/hls 唔受影響驗證**（真實 `routes/stream.js`/`routes/hls.js`，警id=42 warm）：

| endpoint | code | content-encoding | 其餘 header | raw |
|---|---|---|---|---|
| `/api/stream/42` range 0-1023，帶 `Accept-Encoding: gzip` | 206 | （冇） | `content-range: bytes 0-1023/5646616`、`content-length: 1024` —— 同 1A warm 數字一致 | `2a-a2-stream-hls-unaffected.log` |
| `/api/hls/42.m3u8`，帶 `Accept-Encoding: gzip` | 200 | （冇） | `Cache-Control: no-store`、`Content-Length: 2301` —— 同 1A 一致 | `2a-a2-stream-hls-unaffected.log` |

commit：`8f56b02`

## 3. A-3 — access log middleware

`app.use()` 加喺**所有 `/api/*` route mount 之前**（第一版擺錯位放喺 mount 之後，
harness 一測即發現冇任何 `[access]` 行輸出，已改位；順手改埋 harness 對應位置）。
排除 `/api/stream`、`/api/hls`、`/api/client-log`。

**過程中揪到一個 Express 陷阱並修正**：`res.on('finish')` callback 入面讀 `req.path`
會攞到俾內部 router 派發時裁走 mount prefix、又冇 `next()` 落去復原嘅殘餘值
（`/api/home/daily-verse` 讀出嚟變咗 `/daily-verse`）。改用喺 middleware entry 即刻讀一次
`req.path` 存落 local var，`finish` handler 用嗰個 local var 而唔係再讀一次
`req.path`。

正控輸出（harness console，同一批 curl）：
```
[access] 2026-09-02T07:53:18.174Z GET /api/version 200 3ms 45b
[access] 2026-09-02T07:53:18.183Z GET /api/home/daily-verse 200 1ms 185b
[access] 2026-09-02T07:53:18.402Z GET /api/category/cantonese 200 211ms 15971383b
```
（`/api/stream/42`、`/api/hls/42.m3u8` 同一批打完，冇對應 `[access]` 行 —— 見
`2a-a3-after.log` 全文，佢哋只有自己嗰套 `[stream]`/`[hls]` log。）

raw：`2a-a3-before.log`（改前：完全冇 `[access]` 輸出）、`2a-a3-after.log`。
commit：`5943880`

## 4. A-4 — `/api/search`、`/api/category`、`/api/audio` 全部 + `/api/home` 9/10 → 410

**唔刪檔**——四個 route 檔嘅 router 結構/掛載位置原封不動，淨係將每個 handler body
換做 `410 Gone` + 一行 `[deprecated-route]` log，**唔再執行**任何 inline `queryDb()`
（`initSqlJs()`+`fs.readFileSync(61MB)`+`new SQL.Database()`）或 `lib/serverDb.js` 查詢。
`/api/home/daily-verse` 保持原有邏輯（讀 `data/bible-verses.json`，唔碰 DB）。

| 項 | 改前 | 改後 | raw 檔 |
|---|---|---|---|
| `/api/category/mandarin` code/total ×5 | 200 / 0.278–0.308s / 35,145,812 B | 410 / 0.0005–0.009s / 113 B | `2a-a4-before.log`／`2a-a4-after.log` |
| harness process RSS，打 5 次 `/api/category/mandarin` 前 → 後 | 713,920 KB → 871,536 KB（+157,616 KB） | 64,192 KB → 65,840 KB（+1,648 KB） | 同上 |
| `/api/category/cantonese`、`/api/search/all`、`/api/audio/:id`、`/api/audio/cache/stats`、`/api/home/genre-recommendation` | 全部 200（真 DB 查詢） | 全部 410，<1ms | `2a-a4-after.log` |
| `/api/home/daily-verse`（唔可以變） | 200，185 B | 200，185 B（不變） | `2a-a4-before.log`／`2a-a4-after.log` |

⚠️ RSS 嘅絕對數值細過 1A 記錄嘅「+400MB」（1A 打嘅係真 :3001 live process，累積咗
15.6 小時 uptime 嘅其他狀態；呢度係一個乾淨新 spawn 嘅 harness process），但方向同
量級一致：一樣係「每次 request 都重新 readFileSync 61MB DB 檔」帶嚟嘅線性 RSS 增長，
改後歸零。

commit：`ebe29ba`

## 5. A-5 — `Cache-Control` header

`/api/hymns`：`private, max-age=0, must-revalidate`（cache-hit 同 fresh-compute 兩條分支都加）。
`/api/version`：`no-cache`。純記錄，唔期望 CF 因為呢句由 DYNAMIC 變 HIT（1A A1：57 個
endpoint×target 組合全部 DYNAMIC，呢個係 CF route 設定嘅事）。

| 項 | 改前 | 改後 | raw 檔 |
|---|---|---|---|
| `/api/hymns` headers | 冇 `Cache-Control`，有 `ETag` | `Cache-Control: private, max-age=0, must-revalidate`，**同一個** `ETag`（`W/"54f4a0-..."`，Express 自動計，冇被碰過） | `2a-a5-before.log`／`2a-a5-after.log` |
| `/api/version` headers | 冇 `Cache-Control` | `Cache-Control: no-cache` | 同上 |

commit：`77fa5ee`

## 6. 舊 client 相容性驗證（貫穿 A-1..A-5）

Pretty-print + sort-keys 之後，逐 byte diff **live `:3001`**（未改動，process 記憶體入面
仲係舊代碼）同**harness（累積 A-1 到 A-5 全部改動）** 嘅 `/api/hymns` response body：

```
diff line count: 4
128104c128104
<     "dataVersion": "1788274394187.32-61054976"
---
>     "dataVersion": "1788313502009.5908-61054976"
```

除咗 `dataVersion` 呢個字串本身，**零其他差異**——`{data, dataVersion}` envelope、
17 個欄位、`kids→lang` 墊、`real_lang` 全部一致。`dataVersion` 唔同**唔係**呢次改動
造成:係因為有另一個 session 而家真係喺度寫緊 `backend/hymns.db`(session 開工前
`git status` 已經見到 `M backend/hymns.db`,唔關 2A 呢次改動事),呢個差異反而證明
`dataVersion` 機制本身係即時追蹤緊真檔案變化,冇食死。raw：`2a-oldclient-bodydiff.log`。

## 7. A-6 — 提案（唔實作）：`/api/hymns?lite=1` + `/api/hymns/lyrics`

**動機**：1A A1 欄位級拆解，`lyrics` 一個欄佔 `/api/hymns` payload 23.85%（1,327,724 /
5,567,646 bytes），但 84.1% 嘅歌（5,387/6,405）先有非空 lyrics，其餘 15.9% 呢個欄位淨係
送緊空字串。前端目前每次開機都要拉埋呢 130 萬 bytes，但 lyrics pill 顯示邏輯（App.js §3.4）
淨係喺用戶撳開某一首先真正用到內容。

**設計**：
1. `/api/hymns?lite=1` —— 同 `/api/hymns` 一模一樣嘅 SELECT，淨係唔揀 `lyrics` 呢個
   欄（其餘 16 個欄位 + `real_lang`/`kids→lang` 墊全部不變），response envelope 格式
   （`{data, dataVersion}`）不變。**唔改** `/api/hymns` 本身嘅行為 —— 冇 `?lite=1` 就照
   舊版全量返（向後兼容,舊 client 完全唔受影響）。
2. `/api/hymns/lyrics` —— 新 route,`SELECT id, lyrics FROM hymns WHERE lyrics != ''`,
   回 `{ [id]: lyricsString }` 咁嘅 map(或者 `[{id, lyrics}]` 陣列,睇前端點消化方便)。
   一樣行 dataVersion cache(複用 A-1 嘅 pattern)。
3. 前端要改 `useCachedHymns.js`:開機攞 `?lite=1`,起播/入到某首歌先按需要攞
   `/api/hymns/lyrics`(或者一次過攞埋成個 lyrics map,但延後過首屏)——**呢部分屬
   2B frontend 範圍,2A 唔動任何前端碼**。

**Bytes 估算**(基於 1A A1 已有嘅欄位表,rough estimate,冇實跑呢個未寫嘅 route):
- lite(冇 lyrics)未壓縮:約 5,567,646 − 1,327,724 ≈ **4,239,922 B(≈4.04MB)**,
  少咗 23.85%。
- lyrics-only route 未壓縮:約 **1,327,724 B(≈1.27MB)**(呢個係淨 lyrics 字串總和,
  真 route 會加返 `id`/JSON 結構 overhead,實際會高少少)。
- 兩者都行 A-2 嘅 compression:用本次實測嘅全量比率(5,567,648→1,474,227,
  壓縮到 26.47%)做粗略比例外推 ——
  - lite + gzip ≈ 4,239,922 × 0.2647 ≈ **1,122,300 B(≈1.07MB)**
  - lyrics + gzip ≈ 1,327,724 × 0.2647 ≈ **351,500 B(≈343KB)**
  - ⚠️ 呢個外推假設兩類內容嘅 gzip 壓縮率同全量一致,lyrics 屬自然語言文字,
    重覆詞頻可能令實際壓縮率更高(bytes 更細),純音樂/短欄位可能較低——**呢個
    係粗估,唔係實測**,冇跑真 route 冇量過。

**風險/待決**:呢個改動要等 2B 診斷(D-1 已量到 `hymnsTtfb`/`hymnsBody`/`hymnsParse`
三段分拆)睇 lyrics 究竟喺邊一段(下載 vs parse)貢獻幾多先值唔值得做——如果
`hymnsParse`(JSON.parse 5.5MB string)先係主要成本,單純減少 payload bytes 對「熱開」
場景嘅幫助有限(bytes 細咗但 JSON 結構複雜度、物件數量都一樣係 6,405 個)。留返
Opus 5 驗收 2B 之後先拍板做唔做。

**未實作**——冇任何 code 改動,冇 commit。

## 8. 限制 / 做唔到嘅項目

1. **無法喺真正嘅 `server.js` 上驗證 A-1..A-5 嘅端到端行為**——因為 pre-cache
   burst 冇 env guard,唔可以起第二個完整 instance(§0 已解釋)。全部量度用直接
   import route handler 嘅 harness,唔係「一鏡到底」跑成個 `server.js`。已經用
   §6 嘅 old-client body diff 交叉驗證 harness 同真 live process 行為一致(除
   dataVersion 呢個預期會變嘅欄位)。
2. **A-4 嘅 `/api/audio/:youtubeId`**——1A A4 記錄咗 `backend/check_hymns.cjs`
   (一個零引用嘅維運腳本)有一句真 `fetch('/api/audio/...')`。執行單 §2A A-4
   明文將 `/api/audio` 全部列入 410 範圍,本次跟單執行;如果第日有人手動跑
   `check_hymns.cjs` 會撞 410,呢個係已知、明文記錄嘅影響範圍,唔係漏改。
3. **A-4 RSS 數字唔可以同 1A(live process)直接比較絕對值**——1A 打嘅係已經
   跑咗 15.6 小時、有其他背景狀態嘅真 process,呢度係乾淨新 spawn 嘅 harness,
   方向(每次全 DB reload 令 RSS 線性增長,改後歸零)一致但幅度唔同,已喺 §4
   註明。
4. **A-6 純設計,冇任何量度**——bytes 估算全部用 1A 已有欄位表外推,唔係對住
   一條未寫嘅 route 實跑,已喺 §7 逐項標明「粗估」。
5. **`ops/deploy/backend-restart.sh --dry-run`**——見下面 §9,gate 喺第一步
   (approved.json sha 對唔上)就 abort,冇跑到後面嘅 working-tree 乾淨檢查
   （`git -c core.quotepath=false status --porcelain -- backend/`那段邏輯）。冇
   跑 `ops/deploy/approve.sh`(冇授權/唔喺呢次任務範圍)。
6. **`backend/routes/home.js` 遺留咗 `queryOne` 呢個 helper function 而家完全
   冇被任何 route call**(9 條已停用 route 淨係用 `gone`,`queryAll`/`queryOne`
   淨係俾停用咗嘅舊 code 用)——刻意留低,Stage 3 執成個檔先一次過清。

## 9. `ops/deploy/backend-restart.sh --dry-run`

```
git HEAD: 77fa5eefda35aba131d45b3f557466b7fc5adac2
❌ abort:HEAD (77fa5eefda35aba131d45b3f557466b7fc5adac2) 唔等於已批准嘅 backend.sha
(84f2e0315c949edf808d2ea6b89d36674fa195a3)。
...
如果呢啲 commit 已經攞到 Eric go,請先跑:
  ops/deploy/approve.sh backend 77fa5eefda35aba131d45b3f557466b7fc5adac2 --confirm
```
Gate 喺第一步(approved sha 檢查)就 abort,列晒由已批准 sha 到而家 HEAD 之間 11 個
commit(包含本次 A-1..A-5 五個 + 2B 嗰邊嘅 D-1 instrumentation commit + 之前已有嘅
player fix commit)。**冇跑 `approve.sh`**——冇攞到授權,亦唔屬於呢次 2A 任務範圍。
raw：`2a-backend-restart-dryrun.log`。

## 10. 共用 worktree 聲明

開工前 `git status --short` 已經見到 `backend/hymns.db`、`ops/deploy/backend-restart.sh`
等一系列 M 檔(同會話開頭嘅 gitStatus context 完全一致)——全程未寫過呢啲檔案,五個
commit 全部用 `git commit -m ... -- <pathspec>` 逐個指定檔案。收工前 `git status --short |
grep -v '^??'` 結果同開工時比對,新增嘅 M 行淨係 `backend/server.js`、
`backend/routes/{search,category,audio,home}.js`、`backend/package.json`、
`backend/package-lock.json`——全部已 commit 落 A-1..A-5,再跑一次 `git status`
已經冇呢啲檔案留低做 uncommitted(見下面 commit list)。冇撞過 `.git/*.lock`。

## 11. Commits

| commit | 內容 |
|---|---|
| `06d0cb8` | A-1:`/api/hymns` dataVersion-keyed response cache |
| `8f56b02` | A-2:`compression` middleware + npm 依賴 |
| `5943880` | A-3:access log middleware + Express path-capture bug fix |
| `ebe29ba` | A-4:search/category/audio + home 9/10 → 410 |
| `77fa5ee` | A-5:`/api/hymns`/`/api/version` Cache-Control |
