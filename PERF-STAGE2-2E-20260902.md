# PERF-STAGE2-2E-20260902 — 三個小修 + E-5(回應 Opus 5 2D 驗收)+ 一張截圖

執行者：Sonnet 5。範圍：Stage 2E 執行單(E-1/E-2/E-3/E-4)+ 執行期間 Fable 5.1 追加嘅
E-5(回應 `PERF-STAGE2-2D-OPUS-20260902.md` 出街前必修三項)。

裝置：iPhone 17 模擬器 `E0416618-B662-41D2-A253-5260FA0CF556`(iOS 26.5)。全程 hold 檔
(`/tmp/claude-ios-cleanup.hold`)保護,Release build,`Expo.plist` 兩個 build 都 patch 咗
`EXUpdatesCheckOnLaunch=NEVER`(源頭 `ios/Odely/Supporting/Expo.plist` 冇動,`ios/` 本身
唔受版本控制)。收工已 `simctl uninstall`+`simctl shutdown`+殺 `idb_companion`+刪 hold 檔。

**執行者唔判 PASS/FAIL/快/慢/冇問題 —— 呢欄留白俾 Opus 5。**

原始證據全部喺 `ops/perf/stage2-20260902/2e-*`。

---

## 0. 改動總覽(五個 commit)

| # | commit | 內容 |
|---|---|---|
| E-1 | `b0e3411` | `fetchHymnsTwoStage`/`fetchLyricsMap` 嘅 body-timeout 段 `await r.text()` 改做 `Promise.race([r.text(), abortReject])`,`abortReject` 監聽 `controller.signal` 嘅 `'abort'` 事件主動 reject `AbortError`。 |
| E-3+E-4 | `0875920` | 更正 `useCachedHymns.js` 頂部已被 Opus 5 撤回嘅 F-1 註解;`perfMarks.js` `PERF_MARKS_ENABLED` 保持 `true`,加拍板註解。 |
| E-2 | `a829ed8` | `AppContent` 加 `InteractionManager.runAfterInteractions`+500ms 之後自動 `setLibraryEverVisited(true)`,mark `libIdleMount`。 |
| E-2 補 | `925c98f` | 發現 `libIdleMount` mark 冇接落任何 beacon,補落 `schedulePerfHomeBeacon` 嘅 `libIdle=` 欄。 |
| E-5 | `1b66931` | (1) lyrics fetch 失敗時 `s.delete('allHymnsVersion')`(P0);(2) lyrics fetch 延遲到 `InteractionManager.runAfterInteractions`+8s 先開始(上行讓路);(3) 剷走 `perfMarks` beacon 嘅重複欄 `liteMs`。 |

`PlayerProvider`(420-2958 行)全程零改動——`git diff` 逐個 commit 核過,五個 commit 改動位置
全部喺 `AppContent`/`useCachedHymns.js`/`perfMarks.js`,冇一個字掂到起播/stall/nudge/watchdog/
HLS/queue 邏輯。

---

## 1. E-1 正控(harness,冇模擬器)

Opus 5(`PERF-STAGE2-2B-OPUS-20260902.md` §2.6)讀 `ExpoFetchModule.swift`/`.kt` 源碼實錘:
`response.text()` 淨係監聽 `.bodyCompleted`,`controller.abort()` 之後底層 task 會取消但
JS 側 `await r.text()` 永遠唔 settle。

正控(`ops/perf/stage2-20260902/2e-e1-harness-server.mjs`+`2e-e1-harness-test.mjs`)自寫
express route `/hang`(headers 即回、body 永遠唔 end)配一個 `nativeBuggyText()`(模擬 native
吞 abort 錯誤、永遠唔 settle 嘅語義),對比有冇 E-1 嘅 race:

| 場景 | 結果 |
|---|---|
| 有 race,`/hang`,bodyMs=2000(縮短測試用,生產碼 30000) | 2059ms 內以 `AbortError` reject |
| 冇 race,`/hang`,同一 bodyMs | 6000ms 觀察窗都未 settle(證明冇 race 會一直掛住) |
| 有 race,`/normal`(正常快速回應) | 10ms 攞到正確內容,唔受影響 |

三項斷言全過(`2e-e1-harness-output.log`,exit 0)。

---

## 2. E-5 P0 正控(node harness,冇模擬器)

場景:第一次開機成功 merge(`allHymnsVersion='v1'`)。第二次開機 lyrics fetch 失敗,但
DB 版本冇變(`serverVersion` 仍然係 `'v1'`)。

用 `ops/perf/stage2-20260902/2e-e5-p0-harness.mjs`(模擬 MMKV + 逐字抄 `canSkip` 判斷式):

| 場景 | `allHymnsVersion` 失敗後 | 落次開機 `canSkip` |
|---|---|---|
| 冇修(舊碼,唔 `delete`) | 仍然係 `'v1'`(stale) | `true`(bug:永久卡喺 lite-only) |
| 有修(`s.delete('allHymnsVersion')`) | `undefined` | `false`(落次會重試) |

兩個場景都印證(`2e-e5-p0-output.log`,exit 0)——bug 重現同修法生效同時證明咗。

**限制**:E-5 (1)(2) 兩項嘅 code path(lyrics fetch 失敗分支、8s idle 延遲)喺呢次真機/
模擬器量度**完全冇被踩到**,因為 live production backend(3001,經 `api.odemusics.com`
tunnel)到執行呢份報告嗰刻仍未 restart 落 C-1 lite 分支——`?lite=1` 俾舊 `server.js`
忽略,response 照樣帶齊 lyrics(`isFull=true`),`finalizePrimaryResult()` 直接行
`isFull` 分支,完全跳過 `fetchLyricsMap()`。10/10 cycle 嘅 seed `perfMarks` 都印證呢
點:`merged=1` 但 `lyrMs=-`/`lyrBytes=-`(冇打過 lyrics endpoint),`byt` 全部
`3657732`(full payload 嘅 char 數,唔係 lite 嗰個更細嘅數)。E-5(1)(2)嘅有效性**只
靠上面嘅 harness 證明**,呢次冇亦唔可能有機上證據——backend restart 唔喺呢份執行單
範圍內。

---

## 3. E-5 item(3)—— beacon 截斷:剷走 `liteMs` 有慳,但未完全解決

真機證據(`ops/perf/stage2-20260902/2e-e5-truncation-evidence.log`,同一組 seed
`perfMarks`,真實 cold-fetch 場景,`fetch=` 欄有 10-13 個 entry):

| 標籤 | detail 長度 | 尾段 |
|---|---|---|
| A(已剷 `liteMs`) | **300**(頂格) | `...fetch=10(...,api/hom`(`api/home:1` 被切一半) |
| B(仲有 `liteMs`) | **300**(頂格) | `...fetch=13(...,api/stre`(`api/stream:` 被切) |

兩個 build 嘅 seed(真網絡)`perfMarks` **都撞正 300 字上限**,`fetch=` 尾段都被切。
剷 `liteMs` 慳咗 12 個字元(`liteMs=XXXX ` 一段),但呢個 cold-fetch 場景(app 一開
機仲有 `api/stream`/`api/home` 等背景 fetch 一齊撞埋)本身內容量已經逼近甚至超過
300,**單剷一個重複欄唔足夠完全解決截斷**——warm 路徑(數字大部分係 `-`)冇撞到
呢個問題(280-289 字),但**seed/cold 路徑(F-1 最想量到嗰個場景)依然會截**。
呢個發現如實記錄,唔係話 E-5(3)冇做——佢做嘅事(剷重複欄)本身係啱嘅,但「確保
唔再截斷」呢個宣稱冇完全達成。

---

## 4. S2 熱開 ×5 交錯 A/B(10 cycle:BABABABABA 起手 A,seed+warm 各一)

**BEFORE** = commit `d375f9a`(2D 嘅 AFTER,即 E 系列改動之前)。
**AFTER** = commit `1b66931`(2E HEAD,含 E-1..E-5)。

兩個 build 都係 `xcodebuild ... -configuration Release -sdk iphonesimulator`,共用
derivedData 增量編譯,`ios/`(gitignore)源檔用短暫 `git checkout <sha> -- <3個JS檔>` →
build → `git checkout HEAD -- <同3個檔>` 還原嘅做法(跟 2D 一致),每次還原後
`git status --short` 核實乾淨。`main.jsbundle` byte 級唔一樣:
BEFORE-2E.app=3,725,280B、AFTER-2E.app=3,726,969B(E-5 commit 之後嘅最終版)。

**每個 cycle**:`simctl uninstall`→`install`→`get_app_container` 核 `main.jsbundle`
byte 數(10/10 `match=YES`,`ops/perf/stage2-20260902/2e-run-{A,B}-seed-{1-5}.log`)→
seed launch(全新冇 cache)等 40s(E-5 加咗 8s idle 延遲,margin 加大過 2D 嘅
15s)→terminate→warm launch(唔 uninstall,食返 seed 留低嘅 MMKV cache)→2s 後用
`idb ui tap 201 796`(points 座標,`idb describe` 讀 `width_points=402
height_points=874`,crop 螢幕截圖底部 300px 精確量出「詩歌庫」tab 中心)撳詩歌庫
tab→27s(俾 `perfHome`@5s/`perfMarks`@25s/`perfNav`beacon 晒)→terminate。

Beacon 全部經 production `API_BASE`(`https://api.odemusics.com`,tunnel 落呢部機
`backend/server.js:3001`)POST `/api/client-log`,持久化落
`backend/logs/client-log/client-log-2026-09-02.jsonl`,**讀呢個現有檔案係唯一操作,
冇 restart/冇改 backend**。deviceId 每次 uninstall+install 都會重新生成(AsyncStorage
被清),10 個 cycle 對應 10 個獨立 deviceId,結構上唔可能撞;`liteMs`/`byt` 欄嘅有冇
仲構成第二重判別式(2B Opus §5.1 教訓)——5 個 A deviceId 嘅 `perfMarks` 全部**冇**
`liteMs` 欄,5 個 B 嘅全部**有**,同 A/B 分組 100% 對應。

### 4.1 結果表(逐 cycle,`warm` 欄=perfNav 之後緊接嗰對 perfHome/perfMarks)

| cyc | deviceId(前8位) | seed hymnsMs | tapToMount | tapToPaint | warm home | warm verSkip | warm libIdle |
|---|---|---|---|---|---|---|---|
| A1 | 7e9407a6 | 8251 | 30ms | **63ms** | 398 | 1 | 863 |
| B1 | 90412aa2 | 5026 | 222ms | **531ms** | 415 | 1 | — |
| A2 | 3b543f6f | 4179 | 32ms | **57ms** | 422 | 1 | 872 |
| B2 | 7bc91261 | 3771 | 229ms | **545ms** | 385 | 1 | — |
| A3 | 34b5fefd | 3532 | 33ms | **54ms** | 430 | 1 | 897 |
| B3 | c04f1517 | 3432 | 231ms | **562ms** | 474 | 1 | — |
| A4 | 72da7f54 | 6851 | 31ms | **49ms** | 408 | 1 | 858 |
| B4 | c54771e5 | 8376 | 229ms | **530ms** | 414 | 1 | — |
| A5 | 3e1c933d | 7712 | 34ms | **67ms** | 422 | 1 | 889 |
| B5 | 86de0b4c | 6210 | 229ms | **544ms** | 396 | 1 | — |
| **median A** | | | **32ms** | **57ms** | **422** | 5/5 | **872** |
| **median B** | | | **229ms** | **544ms** | **414** | 5/5 | — |

**正控**:10/10 `n=6405`;10/10 byte match=YES;10/10 `verSkip=1`(warm 路徑正確 skip
網絡,證明 canSkip 判斷冇被 E-5 改壞);deviceId/`liteMs` 欄雙重判別式 5A/5B 100%
對應;A 組 `libIdle` 喺 seed 同 warm 兩個階段(10/10 launch)全部落喺 683-897ms 範圍
內,一致喺 `perfHome` 嘅 5000ms beacon 窗口內完成,冇一次缺失。

### 4.2 A1 一個異常要交代

A1 嘅 raw log(`2e-run-A-warm-1.log`)睇落乾淨,但實際上我喺搵「詩歌庫」tab 準確
座標嗰陣,喺呢個 device 嘅同一次 install 上面試咗三次 warm launch(第一次 timestamp
指令有 bug、第二次誤中一首歌嘅播放掣、第三次先撳中 tab)。呢三次都用緊同一個
`AFTER-2E.app`(v3,已含 E-1..E-5,byte match 已核實),`deviceId` 全部一樣
(`7e9407a65472621112d573a8ea6f7b39`),`client-log` 因此對呢個 device 錄咗 9 條
entry(2 條屬 seed + 4 條屬頭兩次 warm 摸索 + 3 條屬最終嗰次)而唔係其他 9 個
device 嗰種乾淨 5 條。**表格入面 A1 一列用嘅係「最後一次 `perfNav` 之後緊接嗰對
perfHome/perfMarks」**——呢條規則對其餘 9 個 device 一樣適用(佢哋本身都係乾淨
5 條,呢條規則揀出嚟嘅同「順序上第二對」係同一樣嘢),唔存在挑數據嘅問題。原始
9 條 entry 全部喺 `2e-s2-clientlog-window.log` 留底,可以獨立覆核。

---

## 5. P2 截圖

`ops/perf/stage2-20260902/2e-screenshots/2e-p2-library-idle-mount-after-tap.png`——
A1 warm cycle 撳「詩歌庫」tab 之後即時截圖:「詩歌庫 6405 首」、搜尋欄、語言 chip
(全部/粵語/國語/英文/兒童/純音樂)、團體 chip(全部團體/讚美之泉/約書亞樂團/新心
音樂事⋯)、歌曲列表(封面+歌名+歌手)全部正確渲染,底部 tab bar「詩歌庫」高亮顯示
已選中。

---

## 6. 限制

- **L1**:E-5(1)(2)嘅 code path 冇喺呢次真機/模擬器量度被踩到(見 §2),原因係
  live backend 未 restart 落 C-1。呢兩項嘅有效性單靠 harness 證明,唔係機上證據。
- **L2**:E-5(3)「確保唔再截斷」冇完全達成——見 §3,cold-fetch 場景仲係頂格 300
  字被切。
- **L3**:S2 嘅 `home`/`hymnsMs` 呢類時間數字全部喺**同一部模擬器、同一條迴環/
  區域網絡**量,唔代表真機蜂窩網嘅絕對耗時(呢個限制同 2D §L2 一致)。
- **L4**:tapToPaint/tapToMount 呢兩個數字嘅絕對值受模擬器 CPU/GPU 模擬效能影響,
  唔可以直接當真機數字用,但 A vs B 嘅**相對差距**(57ms vs 544ms,約 487ms)喺
  同一部機、同一個 build pipeline、10 cycle 交錯量到,方向性同量級應該可信。
- **L5**:A1 一個 device 嘅 raw log 帶有額外嘅摸索記錄(§4.2),已如實交代同留底,
  唔影響最終表格入面嗰行數字嘅來源可追溯性。

---

## 7. 產出清單

- Commits:`b0e3411`(E-1)、`0875920`(E-3+E-4)、`a829ed8`(E-2)、`925c98f`(E-2 補
  libIdle 欄)、`1b66931`(E-5)。
- `ops/perf/stage2-20260902/2e-e1-harness-{server,test}.mjs` + `2e-e1-harness-output.log`
- `ops/perf/stage2-20260902/2e-e5-p0-harness.mjs` + `2e-e5-p0-output.log`
- `ops/perf/stage2-20260902/2e-e5-truncation-evidence.log`
- `ops/perf/stage2-20260902/2e-ab-runner.sh` + `2e-warm-cycle.sh`
- `ops/perf/stage2-20260902/2e-build-{after,after2,after3,before}.log`
- `ops/perf/stage2-20260902/2e-run-{A,B}-{seed,warm}-{1-5}.log`(20 個)
- `ops/perf/stage2-20260902/2e-s2-clientlog-window.log`(10 device 完整 raw)
- `ops/perf/stage2-20260902/2e-screenshots/2e-p2-library-idle-mount-after-tap.png`
